# 改进路线图 · 执行进度记录

> 2026-09-22（历史清理）：按用户要求，提交信息里的工具署名（`🤖 Generated with Codebuff` / `Co-Authored-By:`）一律去掉；main 上 2026-09-18 起的 84 笔提交经 `filter-branch --msg-filter` 重写并强推（内容 tree 逐笔未变），因此本文件与 `evals/` 里 9-18 之后的短 SHA 已整体映射为新值（旧 SHA 在远端不再可达）。重写前的 main 留在本机分支 `backup-pre-footer-scrub-20260922`（@ 旧 `265d310`）。
> 暂停点：2026-09-22（**北极星第 6 步收官 MVP P0/P1 已落地**：T1 委派观测下放 `3f5079e` + T2 删除代价显性化 `e59e002` + T3 团队卡 `3ca03dc`/`8f85399`；全量 TS 2542 / Rust 226 全绿。剩余：S1 样本过门槛（人工跑会话+收割）、T4 成本视图、13.1、13.2 完整版、13.4。MVP 设计见 `docs/step6-closing-mvp.md`）。此前 2026-09-21：6.4 MCP OAuth `a394274`…`bf5c4f5`、接力流水线 `a18a489`…`8d1fe0e`、命令沙箱默认关 config v16 `a9ec0ef`；2026-09-20（**MVP 三步 + 北极星第二步落地**：阶段 12 一期暂停/续跑 `bd5dd92`、13.2 外部角色装载 `7692991`、13.2 建议一键生成角色草稿 `4a137e3`、**插话通道进子 agent** `1cc5b1d`；12 二期/三期与 13.2 完整版未实施）。此前 2026-09-20 上半场：北极星总设计定稿；2026-09-19：v5 基线入库 + 设置页打磨；阶段 1–8 收尾，自我进化 E 轨道全部收尾。权威摘要以本文末尾最新的「⏸ 暂停点」节为准。
> 本次会话（2026-09-19 续）：① `bc793e5` 修「provider 未连通被记成平凡失败」+ 加 1.6 extreme 两档（套件升 v5）② 三模型跑完 v5：DeepSeek 15/15、GLM 5.3 15/15、GLM 4.5 13/15 —— **pass/fail 第一次拉开**；`evals/BASELINE.md` 重写 + `eval:snapshot` 三行快照入库（`d697690`）③ 设置页滚动条体系重做 + 双主题对比度整改（`aa5e445`），真实浏览器 e2e PASS。
> 下一步：① 攒数试用，回看 E1.1 / E1.3 / E1.4 三条阈值型建议的误报率，攒仪表盘趋势真实形状（2026-09-19 用户定：顶上当第一优先）② 阶段 6.4 MCP OAuth；阶段 7 Skills 签名、阶段 10 沙箱按 2026-09-17 优先级缓做。（Qwen 基线格 2026-09-19 用户拍板划掉：不开 DashScope，矩阵定格三 provider，见 1.4 行。）③ **阶段 11 输入纠错（错别字容错）**：11.1（风险词表 + 无损归一化）与 11.2（路径纠错 + 原文可撤销）已落地（2026-09-20）；11.3 歧义提示、11.4 记忆闭环待排——见「📐 方案定稿」节。

---

## ✅ 已完成（每步独立提交，均可回滚）

### 阶段 1：评测基线
| 步骤 | 状态 | 提交 |
|---|---|---|
| 1.1 评测 fixture（扩到 8 个，含失败恢复 / 权限拦截 / 长会话压缩） | ✅ | `86074c7` |
| 1.2 评测挂进 CI 作第四道门禁（eval:sanity） | ✅ | `944c8bb` |
| 1.3 CHANGELOG 发布模板加评测基线节 | ✅ | `74fd7db` |
| 附带修复：长会话压缩钉住 user 消息，追问附件路径不丢 | ✅ | `605b738` |

### 阶段 2：用户 hooks
| 步骤 | 状态 | 提交 |
|---|---|---|
| 2.1 `~/.pure/hooks.json` schema + 三层加载 | ✅ | `ff6c389` |
| 2.2 HookRouter：pre_tool 否决 / post_tool 注入 / turn_complete | ✅ | `c972626` |
| 2.3 hooks 过权限门（首次确认 + 按命令缓存） | ✅ | `f1ded02` |
| 2.4 文档 + 3 个官方示例 hook | ✅ | `ed2e427` |

### 阶段 3：git 写工具 + 提交前评审
| 步骤 | 状态 | 提交 |
|---|---|---|
| 3.1 `git_commit`/`git_branch` schema + 编译期三件套 | ✅ | `9c25780` |
| 3.2 双端 shell git 实现 | ✅ | `49e41c1` |
| 3.3 按用户决定简化：NORMAL 全自动放行，不加新守卫 | ✅ | `d443235` |
| 3.4 提交前自动评审（code_reviewer 结论进 VERIFY） | ✅ | `9f92a20` |

### 阶段 4：worktree 并行会话
| 步骤 | 状态 | 提交 |
|---|---|---|
| 4.1 会话 ↔ worktree 自动绑定 | ✅ | `ea3420e` |
| 4.2 队列单飞按 worktree 粒度化（跨 worktree 真并行） | ✅ | `cc9b677` |
| 4.3 并行任务卡片 dock | ✅ | `2466ea1` |
| 4.4 收尾合并流（diff 预览 / 一键合并 / 丢弃 / 删除会话留存提示） | ✅ | `34a36e9` |

### 阶段 5：本地异步执行
| 步骤 | 状态 | 提交 |
|---|---|---|
| 5.1 关窗 → 隐藏到托盘继续跑 | ✅ | `8187829` |
| 5.2 系统通知（完成 / 需确认 / 失败，点击跳回会话） | ✅ | `3beb522` |
| 5.3 托盘运行中任务列表 + 快速取消 | ✅ | `dd0e1a4` |

### 阶段 6：MCP 深化
| 步骤 | 状态 | 提交 |
|---|---|---|
| 6.1 MCP resources：`resources/list` + `resources/read` → `<mcp_resources>` 上下文片段 | ✅ | `16bc61d` |
| 6.2 MCP prompts：`/mcp-prompt` 命令 + 输入框补全候选 + CLI `/prompts` | ✅ | `3ea890c` |

**6.2 实现要点**：
1. **两个不同的调用点**：GUI 在 `sendMessage`（清空输入框之前）展开，所以展开失败时草稿还在；`ChatController.send()` 里也有一份，但仅当调用方没有传独立的显示文本时才跑（队列 / 插话 / 托盘等程序化路径）。CLI 在 REPL 命令链里展开。
2. **展示与执行分离**：聊天记录里显示用户敲的命令，模型拿到的是模板正文（走已有的 `displayUserText`/`doSend(displayOverride)` 机制）。
3. **草稿不丢**：必填参数缺失、模板名打错、服务器报错——三种失败都在清空输入框之前拦下并提示。模板名不存在时错误信息会列出可用项。
4. **补全候选只读**：`listMcpPrompts()` 只报告已连接客户端已知的模板，**不会为了一次补全拉起第三方子进程**（打字就应该零副作用）；只有用户真的敲了 `/mcp-prompt` 命令才允许按需连接（20s 上限），并且复用 `deferredInitDone`/`mcpSessionId`/`mcpConfigSnapshot` 记账，首次 send 不会重复重连。
5. **同一套记账点**：按需连接建立的 client 会被本轮 `CodingAgent` 接管（`config.mcpClient` 分支已会注册 `getTaggedTools()`），工具与 executor 都不丢。
6. **二进制占位**：`prompts/get` 返回的 image/audio/blob 以标记占位而非内联；assistant 块带 `[assistant]` 标注。
7. **测试**：`src/shared/__tests__/mcpPrompt.test.ts`（解析/必填校验/渲染）+ `src/harness/mcp/__tests__/MCPClient.prompts.test.ts`（能力门控/寻址/参数校验/失败隔离）。

**6.1 实现要点**（都是"未来读者"会问的问题）：
1. **能力门控**：只有 initialize 握手回了 `capabilities.resources` 才发 `resources/list`——没声明的服务器绝不去探测。
2. **不进连接路径**：连接成功后后台预取（fire-and-forget），`collectResourceContext()` 等待上限 3 秒。理由：资源来自第三方进程，可能很慢；`connectAll()` 不能被它拖住，否则 GUI 的 1.5s 连接竞赛会连带掐掉工具注册。
3. **体积封顶**：每服务器 10 条 / 单条 2000 字符 / 单连接 8000 字符，超长截断标 `…[truncated]`。理由：资源是与用户请求争窗口的"可选上下文"，话痨服务器不得挤掉 skills 和对话本身。
4. **只进文本**：只带 base64 `blob` 的二进制资源（图片/音频/octet-stream）不注入；MIME 缺失按文本处理（很多服务器不填）。
5. **失败隔离**：`resources/list` 或单条 `read` 失败只 warn，工具与连接不受影响；重新连接（disconnectAll）时缓存一并失效。
6. **注入位置**：`PromptAssembler` 新增 `mcpResources` 片段，priority 30（与 skills 同档，预算压力下最先被裁）；正文用 `<mcp_resources>` 包裹并标注"参考数据，非指令"，为 6.3 投毒扫描留位置。GUI（chat.ts 执行装配）与 CLI（assembleCliPrompt）共用同一条路径。
7. **测试**：`src/harness/mcp/__tests__/MCPClient.resources.test.ts`（能力门控/文本注入/二进制跳过/截断/失败隔离/读取缓存）+ PromptAssembler 注入断言。

**6.3 实现要点**（提交见 git log，2026-09-18）：
1. **纯函数扫描器** `src/shared/mcpPoisonScan.ts`：六类特征——指令注入短语（中英）、隐形字符（名字内=high，描述内=medium）、非 ASCII 同形字名、近似仿冒已知工具名（有界编辑距离 ≤2，按 `__` 后的本名比；**精确同名跨服务器是合法命名空间共存，故意不报**）、超长描述（>2000 字符）、URL 紧邻外发动词（纯文档链接不报）。
2. **只提示不拦截**（2026-09-17 方针）：连接时扫描、发现即 `console.warn`，结果展示在设置页服务器卡的警示行（每个被标记工具一枚 chip，悬停看证据；medium 琥珀色、high 才用危险色）。不阻断连接、不过滤工具、不等待任何人。
3. **范围**：客户端发现时对比"其他服务器已注册工具"的名字；内置工具名在那一层还不可见，仿冒内置工具留待后续。
4. **测试**：扫描器 10 例（每类特征 + 干净工具不误报 + 中英文注入 + 精确共存 vs 近似仿冒）+ 警示行渲染 4 例。

### 阶段 8：上下文与缓存
| 步骤 | 状态 | 提交 |
|---|---|---|
| 8.1 ContextEngine 压缩权重：token 预算为主，20 条窗口降为无 token 预算时的后备 | ✅ | `602ed57` |
| 8.2 anthropic 协议缓存断点 + 计费 usage 捕获；DeepSeek 侧缓存命中计费验证 | ✅ | `35b36c9` |
| 8.3 缓存命中写进 observability 元数据（usage 旁加 cache 标记） | ✅ | `fc6cca6` |

**8.2 实现要点**（2026-09-18）：
1. **断点打在线层，不动 PromptAssembler**：路线图写的是「PromptAssembler 输出缓存断点」，实现落在 wire 层（适配器出请求前打标记）——因为 cache_control 是 anthropic 协议的字段，塞进 canonical Message 会把 provider 细节漏进共享类型。系统提示词（转 blocks）、工具块（最后一个工具带标记）、倒数第二条消息的最后一个 content block（可复用前缀的终点）各打一个 `cache_control: {type: ephemeral}`，共花 3/4 个断点额度；单条消息的请求不打（还没什么可缓存的）。
2. **两份镜像实现**：Rust `apply_anthropic_cache_breakpoints`（GUI 生产路径 chat_stream）+ TS `applyAnthropicCacheBreakpoints`/`cacheableAnthropicRequest`（CLI / 评测的 SDK 适配器）。测试两边各一份。
3. **anthropic 协议此前完全没有 usage**：计费拆在 `message_start`（input + cache 读/写）和 `message_delta`（最终 output）两个事件里，两个传输层都补了捕获，翻译成 openai 风格（cache_read→hit，未命中含 cache 写入→miss，总输入=三者之和）后走既有 usage 通道——会话统计的缓存命中率 / 成本对 anthropic 协议直接点亮，下游零改动。只在 message_delta 发一次，避免 prompt 双计。
4. **顺手修了一个同行的老 bug**：`system_parts.join("\\n\\n")` 是字面反斜杠 n（Rust 源码如此），多条系统消息（压缩摘要路径）会被字面 `\n` 文本粘连——已改成真换行 `"\n\n"`。
5. **DeepSeek 侧验证零代码**：openai 协议走自动缓存，`prompt_cache_hit_tokens` 早已归一化进会话统计并按 cache-hit 费率算成本——同会话第二轮在统计里就能看到命中率与输入费用下降。
6. **注意**：`cache_control` 在真实 Anthropic 上省钱（cache hit ≈ 1/10 价）；DeepSeek 的 anthropic 端点兼容该字段（Claude Code 就带它跑）但其缓存计费遵循 DeepSeek 自己的费率。实测数字仍等 1.4 的 key。

**8.3 实现要点**（2026-09-18）：
1. `AgentRunObservation` 在 `usage` 旁新增 `cache?: { hitTokens?, missTokens?, hitRate }`（hitRate 百分比一位小数）：provider 报了缓存拆分才有标记；全零拆分给 `hitRate: null` 而不是假 0%；完全没概念的 provider 不写字段——记录里没有 `cache` 就是"无话可说"。
2. 随 records()/JSONL trace 自动落盘（本轮 eval 对比缓存效果时直接 grep 这个字段），不存任何 prompt 文本。

**8.1 实现要点**（2026-09-18）：
1. **权重方向**：生产路径永远会从 prompt budget 解析出 `maxTokens`（`createDefaultHarnessConfig` / chat.ts 兜底构造都传了），所以改动后实际生效的行为是：**短消息再多也不再被 20 条窗口截断**，保留多少完全由真实 provider 窗口（token）决定；`maxMessages` 只在拿不到 token 预算的独立/测试场景生效。
2. **语义不变的两件事**：tool-call 组原子（assistant + 它的 tool 结果同进同退、悬空片段照旧驱逐）；pinned user（不占保留预算、超预算时从最旧开始 shedding 进摘要器）。
3. **触发门槛同步改**：仅条数超限而 token 未超 → 不再触发压缩（`compacted: false` 原样返回）。
4. **测试**：新增两例双向锚定——`maxMessages: 2` + 大 token 预算时 60 条全保留；`maxMessages: 1000` + 小 token 预算时仍按体积驱逐最旧组。原 21 例压缩测试一行未改全绿（token 路径和 count 后备路径都有既有用例盖着）。
5. **验收口径**：`eval:sanity` 全绿含 `long-context-q3-report`（control 从种子失败 + golden 通过，证明 fixture 管线无恙）；**真实 agent 分数重测仍卡在 1.4 的 API key**——token 为主只会保留更多上下文，分数理论方向只升不降，但实测数字要等有 key 才能补。

### 计划外专项：授权确认取消（2026-09-17 你的指示）
✅ `d2daea0`，行为变化三点：
1. 高风险操作**不再弹确认卡等批准**——评估卡照常展示风险，闸门自动过，直接执行
2. **计划批准暂停取消**——计划一出本轮立即开工，不再有"等你回复开工"
3. **工具授权默认全放行**——权限模式默认"自动放行"（YOLO），写文件 / 执行命令自动批准默认开

### 计划外专项：关窗策略 A（2026-09-17 试用反馈）
✅ 已提交推送，行为改为：
- **没任务在跑**：点关闭 = 真退出（不再留托盘）
- **有任务在跑**：藏到托盘继续跑，并弹一条系统提示"还有 N 个任务在跑"；进度和取消都在托盘菜单
- 开了多个窗口时，关掉其中一个只关那一个

### 计划外专项：子 Agent / 工具卡片区分 + 浮窗去灰底（2026-09-17 试用反馈）
✅ `be4d1ac`，两处改动：
1. **子 Agent 卡片一眼可辨**（三个线索同时上）：
   - 七个编制内子 Agent（资料调研/代码评审/项目体检/任务规划/代码修改/深度思考/界面设计）统一 🤖 图标 + 中文角色名；普通工具保持各自的 emoji + 英文动词名
   - 名字旁边加青色"子 Agent"胶囊徽章，和右侧活动栏的徽章同一视觉语言
   - 卡片行有青色调的描边和底色，悬停提示里能看到原始 agent id
   - `bash_executor` 仍算工具——和活动栏同一裁决：那是"穿了 agent 外壳的 shell 命令"
2. **右侧浮动小卡片不再有灰底**：原来卡片背景是 72/62/52% 的半透明混色，聊天文字从底下透出来，看起来像蒙了层灰。现在三个断点全部改成不透明的卡片底色，浮层位置和 z-index 不变

**试用注意**：首次启动会跑 v15 配置迁移，把旧配置里的"每次询问"一次性翻成自动放行（之后你在设置里改的值不会被覆盖；"只读模式"等更强选择不受影响）。
**仍保留的确认**：删除会话、丢弃 worktree（不可逆）；手动选计划/构建模式时的确认卡（那是模式本身的语义）。

---

### 计划外专项：修复 2.2.6-alpha 发布失败（2026-09-17）

`gh run view 35198444896` → Release 的 Windows 作业在 `bun test` 挂了 9 例（macOS/Linux 的 CI 全绿），流程没走到构建就中断。三类根因（已修，`348af47` / `f7da00e` / `35bb357` 已提交推送）：

1. **PowerShell 的 CLIXML 噪声进了 hook 的 stderr**（ToolExecutionCoordinator veto 1 例 + userHookRunner 4 例）
   - 现象：否决理由变成 `#< CLIXML … Preparing modules for first use.`，hook 自己写的消息被整个盖掉。
   - 根因：Windows 上 hook 走 `powershell.exe`，预热进度记录以 CLIXML 序列化进 stderr；`ToolExecutionCoordinator` 的理由取 `(stderr || stdout)`，第一个非空的就是这坨 XML。
   - 修法：`stripPowerShellStartupProgress` 从 `NodeToolAdapter` 提到 `shared/powershellOutput.ts`（两条链路共用，GUI 包不能反向依赖 adapter/node），并改成“只摘掉 blob、其它文字无论先后都保留”而不只是原本的“blob 独占→整段清空”，否则 blob 后面跟着的真消息会被吞。
   - 另：5 例里有 4 例是测试自己用了 bash 语法（`pwd`/`cat`/`yes|head`），改为平台原生命令；cwd 绑定不再比路径字符串（Windows 同一临时目录有 8.3 短名/长名两种形态），改用“相对路径找到标记文件”证明。
2. **git 在 Windows 输出正斜杠路径**（resolveSessionWorkspace 2 例 + describeWorktree 1 例）
   - `rev-parse --show-toplevel` / `worktree list` 给出 `C:/Users/…`，而 `repoRoot` 要跟用户选的本机路径比较、还会经 `onReleased` 存进最近工作区。
   - 修法：`worktreeBinding` 新增 `toNativePath`（靠盘符/反斜杠特征判断，不用 `node:path` —— 该模块要进浏览器包），worktree 目录名的 basename 改为按两种分隔符切。
3. **fixture 被 core.autocrlf 改写**（mergeWorktreeBack 1 例 + 干扰 1 例）
   - Windows git 默认 `core.autocrlf=true`，`worktree add`/合并时把 LF fixture 改成 CRLF，`toBe('one\n')` 之类的内容断言失败（日志里两边看起来一模一样，其实是 
）。
   - 修法：两个 worktree 测试在自己的临时仓库里 pin `core.autocrlf=false`（与仓库根 `.gitattributes` 的意图一致：fixture 不该受平台 EOL 策略影响）。

**过程修复**：CI 新增 `windows` 作业（typecheck + `bun test`），与 Release 的 Windows 前置检查对齐——这类平台特有问题应该在 main 上就红，而不是等到打 tag。

**第二轮**（`348af47` 推上去后，新 Windows 作业在 main 上抓出来的）：9 例减到各平台各 1 例，但性质不同——

4. **Windows：捕获到的 stderr 只剩一行 `#< CLIXML`**
   - 现象：`userHookRunner > drops the PowerShell CLIXML warm-up blob` 失败，`Received: "#< CLIXML"`——正好是头行，文档一个字节都没有。
   - 根因：wrapper 是分两段写的——流被重定向时先落头行，记录序列化时才写 `<Objs>` 文档——进程退出途中两段会各自落单（头行无文档，或文档被截断在大尾巴上）。上一版只认“完整的预热文档”，裸头行就照旧透传到调用方（on_pre_tool 把它当否决理由）。
   - 修法：`powershellOutput` 增加两条独立规则——截断的预热文档（无 `</Objs>`，尾巴归属该文档，用 tempered 模式避免吞掉前面无关的 CLIXML）、没有文档跟随的裸头行（紧跟 `<Objs>` 的头行留给文档规则，避免把头行从看不懂的文档上摘掉）；早退门由“必须有预热标记”放宽为“流里得有 CLIXML 痕迹”，不可归属的形态照旧原样返回。
5. **Linux：hook 不读 stdin 时 EPIPE 逃逸成异常**
   - 现象：`captures stdout and runs in the bound cwd` 在 ubuntu 作业抛出 `EPIPE: broken pipe, write`（`userHookRunner.ts` 的 `proc.stdin.end()`），2.5ms 就挂。
   - 根因：payload 是给 hook 的“顺水人情”，不是契约——`command -v prettier` 这类探针根本不读 stdin，且进程先退出关掉了管道。Linux 上 bash 约 2ms 退出、写入还在路上，每次必挂；macOS 我本地 40 次尝试一次都没复现（这解释了为什么此前看不见）。
   - 修法：stdin 的 write/end 包住（同步抛与 bun 返回的 promise 两种形态都吞），payload 没被读不是失败——hook 的退出码与自己的输出仍是唯一事实来源，这正是 runner “永不抛异常”契约该有的样子。
   - 顺带补了一条明确的用例（不读 stdin 的 hook 仍报出其结果），因为这条路径以前没有任何测试直接盯着。

**一点判断依据**：这两个都不是“测试不适应平台”，而是平台暴露出真实产品缺陷（否决理由被 XML 盖掉、hook 异常中断整条工具链路）。所以修的位置都在 `src/` 而不是断言里。

**留意**：本地全量跑出现过一次与本次改动无关的抖动（`keywordSearch helpers > searchMemories ranks multi-keyword matches above single-keyword ones`），单文件 5/5 绿、随后两次全量也绿，未复现；该用例的排序依赖 `Date.now()` 的毫秒差，值得单独看一眼。

### 发布体检：latest.json 合并已改为显式步骤（2026-09-17 晚排查 + 修复）

Windows 修复本身全部生效——重发那次的 **windows-release 作业绿了**。但 macOS 作业在**传完三个产物之后**报失败，产物实际都在 release 上（dmg / tar.gz / sig 更新于 11:41）。根因（修正版）：

1. **两个作业是串行的**（`macos-x64-release` `needs: windows-release`），不存在并行撞车。设计意图是 tauri-action 内置的"读已有 latest.json → 保留 windows 条目 → 合入 darwin 条目 → 重传"，但这条内部路径在 11:41 **静默失败**——错误文本连 `--log-failed` 都没抓到，`latest.json` 停留在 Windows 作业 11:34 传的版本。
2. **连带后果 1**：release 上的 `latest.json` 只有 `windows-x86_64` 条目，**没有 darwin-x86_64**——macOS 用户的自动更新看不见这个版本（手动下载 dmg 不受影响）。
3. **连带后果 2**：失败步骤之后的「Build CLI binary (macOS x86_64)」被跳过，release 上**缺 macOS CLI 二进制**（Windows 的 `pure.exe` 在）。

**修复（本次提交）**：绕开 action 的合并黑盒——
- macOS 作业的两个 tauri-action 步骤（signed/unsigned）都加 `includeUpdaterJson: false`，action 不再碰 latest.json
- 作业末尾新增「Merge darwin-x86_64 into latest.json」步骤：下载 Windows 建的 latest.json + 本次上传的 `pure_x64.app.tar.gz.sig`，jq 注入 `darwin-x86_64` 条目（jq 逻辑已用真实 latest.json 本地干跑验证），`gh release upload --clobber` 覆盖回 release
- 步骤只在前序全部成功时运行，签名永远是本次构建的

**重发操作**：删掉半残的 release（避免同名资产覆盖的不确定性）→ tag 移到最新 main（`8d93a5a`）→ 推 tag 触发 Release。
**重发结果（2026-09-17 14:09 触发，run `35231725769` ✅ success）**：8 个资产全齐——Windows 安装包 + CLI（`pure.exe`）、macOS dmg + updater 包 + **CLI（`pure_x86_64`，上次缺的）**；`latest.json` 三个平台键齐全（`darwin-x86_64` / `windows-x86_64` / `windows-x86_64-nsis`），合并步骤产出验证无误。

**抖动用例复查**：进度记录里那个 `searchMemories ranks multi-keyword…`（依赖 `Date.now()` 毫秒差）当晚连跑 15 次全绿，仍未复现。属潜在抖动，不影响当前发布；后续值得单独把排序断言改成注入时钟。

**代码体检结论**（同晚）：工作区干净（仅 5 份未跟踪文档按约定不提交）；typecheck / lint / 全量 2038 测试 0 失败；`TODO|FIXME|XXX|HACK` 全仓 0 处；三处版本号一致（2.2.6-alpha）；Rust 侧自版本号提交（`d06c7c0`）后无逻辑改动且本地 release 构建已编译通过。

### 自我进化起步包：P1 + P2 + E0.1（2026-09-18）

按《自我进化系统设计.md》落地第一批：两条纯提示词收益 + 修掉"观测只写不读"。

- **P1 现成方案优先**（`adaptiveControl.ts`）：任务识别为代码类已知问题（involvesCode 且非 quick）且无 procedure 命中时，`<adaptive_strategy>` 附加"先搜索现成开源方案/官方文档再动手"。踩坑记录：初版门槛含 `complexity !== 'trivial'`，关键词复杂度把"实现导出 Excel"这类短代码请求打成 trivial 挡住了提示——门槛去掉复杂度条件，顺手小改由 quick 兜住。
- **P2 动手前核对**（`agentBehavior.ts` workflow 第 1 步）：加一句"开始前先核对 `<session_memory>` 中的 procedure 与错误教训"。
- **E0.1 观测落盘**（修"只写不读"）：
  - `promptObservability.ts` 新增 `PromptObservationSink` + `setSink()`，store 落记录时同步镜像到 sink，失败吞掉不影响运行循环；
  - GUI 挂 `tauriObservationSink.ts`（新）→ 走新 Rust 命令 `append_observation`，记录以不透明 JSON 字符串过桥，Rust 侧不耦合 TS 结构；落 `~/.pure/observations/app.jsonl`；
  - CLI 挂 `FilePromptObservationStore`（`cliHarness.ts`）→ 落 `~/.pure/observations/cli.jsonl`；
  - `FilePromptObservationStore` 重写：默认上限 2k → **50k**，从"每次 append 全量读文件轮转"改为"廉价追加 + 体积门控（64MB）整文件压缩"，长寿命进程才可承受；
  - Rust `append_observation` 同样按体积轮转（保留最近 5 万条），带轮转/空记录单测。
- **验证**：typecheck ✓；TS 2070 项（新增 7）✓；Rust 218 项（新增 2）✓；CLI 打包 ✓。统计 UI 按设计推迟到 E4.2，不做。
- **设计文档**：《自我进化系统设计.md》（v2 定稿）随本批一并入库。

### 自我进化第二批：E0.3 + E1.2（2026-09-18 续）

- **E0.3 按阶段模型路由**（原路线图 9.1 前移，`9fd2e9f`）：
  - `EngineContext` 新增 `llmFor?(phase)`：THINK / HANDOVER / REFLECT 三相可选各自 adapter，缺省回落 `ctx.llm`，不传时引擎行为逐字节不变（VERIFY 走 `ctx.verifier`，不经 adapter）；
  - 引擎两个消费点接通：THINK 主流式、策略停机的 handover 总结轮（这轮不面向用户流式，天生适合便宜模型）；REFLECT 留给 E1.1 反思器从接缝处取用；
  - 验收测试：THINK 走相 adapter 且默认 adapter 零调用；handover 用相 adapter；不传 `llmFor` 单 adapter 行为不变。
- **E1.2 FailurePolicy 跨会话加速**（零 LLM 成本，反思器未先行）：
  - `FailureHistory` 同步快照接口（`lookup(tool, class) → {count, lesson}`），`DefaultFailurePolicy` 构造注入，`decide()` 保持同步签名、核心无状态可测；
  - 阶梯前移：历史见过的坑（同工具+同错误类）**首错即 reflect**（附历史教训文本）、**两错即 degrade**（skip 继续）；新坑照旧 2/3/5；
  - 关键门控：仅对**非 generic** 错误类生效——generic 报文无迁移信号，上个会话的无关一次性失败不许抬升今天的首错；快照工厂同样跳过 generic 记录；
  - 快照来源 = `memoryStore.list({ type: 'error_pattern', activeOnly: true })`（同步、一次查询），从记录里的 `(tool: x)` 尾注还原工具名；CLI（cliHarness）与 GUI（chat.ts，受 memoryEnabled 门控）各自装配，子代理经共享 policy 自动继承；
  - 验收测试：带历史首错即 reflect 且含教训文本；二错即 degrade；新坑/无历史保持原阶梯；generic 不加速；快照聚合/跳过/空列表。
- **验证**：typecheck ✓；TS 2081 项（本批新增 11）✓。两笔均已推送远端。
- **下一步**：E0.2（评测 `--with-memory` + `--compare`，L 量级，含 §2.1 缓存冻结决策落地）——完成前进化改动只能看步数/重试等结构指标。

### 自我进化第三批：E0.3 单模型加固 + E0.2（2026-09-18 续2）

- **E0.3 单模型兜底契约**（用户指出：只配一个大模型时按阶段路由不能炸）：
  - 抽 `llmForPhase(ctx, phase)` 集中兜底：resolver 未传 / 返回 undefined / 抛异常三种情况一律回落 `ctx.llm`，两个消费点共用；配 2 个锁定测试（部分配置下 handover 落回默认 adapter；抛异常 resolver 不影响跑完）。
- **E0.2 评测 `--with-memory` + `--compare`**：
  - **缓存冻结（§2.1）**：`Harness` 新增 `sessionSystemPrompt` 字段——`run()` 组合一次即冻结，`continueTurn` 逐字复用（恢复会话的首个续轮现算并同样冻结）。此前每轮续轮都重搜记忆+重跑策略+重编译系统提示词，provider 侧缓存断点必被打穿；会话中途的新教训仍走 FailurePolicy 提示通道（引擎 user 消息）进上下文，不动冻结区。测试：续轮不重搜记忆（searchCalls==1）+ 系统提示词逐字节相同；恢复会话路径同样先冻结后复用。
  - **eval 执行器接记忆**（`codingAgentExecutor.ts`）：新增 `memory` 选项直通 `CodingAgent`（它本来就支持，执行器一直没传）；新增 `evalProjectKey(fixtureId)` = `pure-eval://fixture/<id>`——FSMemoryStore 按 projectPath 分桶，而 eval 每遍 workspace 都是 mkdtemp 新路径，按路径隔离会让 A/B 全平，按 fixture id 则两遍共享同一桶（真实文件操作仍在各自 workspace，工具看不到这个假 key）。
  - **run-evals 双遍驱动**：`--with-memory`（单遍，库冷启动正常沉淀）、`--compare`（超集：pass A 冷库播种 → pass B 复用同一批 store 实例检索 A 的沉淀）；store 生命周期在驱动层 `Map<fixtureId, FSMemoryStore>`，suite 层零改动；临时记忆目录收尾清理（`--keep-workspaces` 时保留）；缺 `--agent` 直接报错。对比表按 fixture 列 Δtools / Δprompt / ΔcacheHit，全零时提示（mock 确定性输出属预期）；`--report` 落 `{mode:'memory-compare', cold, warm, fixtures}`；`--strict` 门禁 warm 遍。缓存命中指标走 Usage 里现成的 `cacheHitTokens`，无需新管道。
  - **冒烟**：`--agent mock --compare` 全链路 ✓（两遍执行、表渲染、全零警告、报告结构、临时目录清理）；`--with-memory` 单遍 metadata `memoryPhase: single` ✓；无 flags 默认路径无记忆痕迹 ✓。
- **验证**：typecheck ✓；TS 2081 项（冻结新增 2）✓；全量 2081 过 0 败。
- **验收对照设计**：同 fixture 两模式对比表有非零差异 → 表结构 + changed 判定已就位（真实 provider 差异数值待跑真基线）；缓存断点不被记忆注入打穿 → 冻结测试逐字节锁定 + ΔcacheHit 列可观测。
- **下一步**：E0.4（CLI embedding onnx spike，S）→ E1.1（turn 后反思器，L，接 E0.3 的 REFLECT 相）。

### E0.4 spike：发行版 CLI embedding 可行性验证（2026-09-18 续3）

**结论：可行**——但不是走 transformers.js，而是绕过它直连 `onnxruntime-web`（WASM）+ `@huggingface/tokenizers`（纯 JS）。已用最小脚本实证：`bun build --compile` 单文件二进制，在 node_modules 被移走的环境下完成 3 文本批量嵌入推理（session 470ms / 推理 53ms / 384 维），语义分离正确（正对 cos 0.40 > 检索阈值 0.2，负对 0.03）。

**依据**（三个实验）：
- **S1 裸编译（复现已知阻断）**：去掉 `--external @huggingface/transformers` 直接 `bun build --compile` → 死在 `onnxruntime_binding.node`——bun 无法把 N-API 原生绑定打进单文件。这就是 cli:build 标 external 的根因，"纯构建配置修不动"属实。
- **S2 transformers.js 路线（不可行，不必再试）**：v4.2 在 `src/backends/onnx.js:23` **静态** `import * as ONNX_NODE from 'onnxruntime-node'`（无条件）；node 判定 `env.js:44` 的 `process.release.name === 'node'` 是模块内冻结常量；官方逃生口 `globalThis[Symbol.for('onnxruntime')]` 注入后 `supportedDevices` 保持为空——显式 `device:'wasm'` 抛 Unsupported、缺省 `DEFAULT_DEVICE='cpu'` 同样抛。加上 sharp 静态导入也要 stub。要过编译得连打三处内部补丁，升级即碎。放弃。
- **S3 直连路线（可行，已实证）**：ort-web 的 exports 把 wasm 资产做成**官方子路径导出**（`onnxruntime-web/ort-wasm-simd-threaded.wasm` / `.mjs`），bun 的 `with { type: 'file' }` 内嵌进二进制（wasm 12.3MB + 工厂 24KB），启动时解包到运行时目录喂给 `env.wasm.wasmPaths`；`import * as ort from 'onnxruntime-web/wasm'`（wasm-only 构建，纯 JS）+ `new Tokenizer(json, config)`（@huggingface/tokenizers，纯 JS 81KB）。q8 量化模型 23MB（现 GUI 路线下载 ~80-90MB 的 1/4），首次使用时下载到 `~/.pure` 缓存——与今天的模型下载故事一致。dev 与编译产物 cosine 逐位一致。模型走 huggingface.co 不通的机器可换 hf-mirror.com（本机实证）。

**后续量级建议：M（1–1.5 天）**：
- 新 `OrtWebEmbedder`（~150 行）：tokenizer/模型懒加载下载、batch 推理（沿用 WASMEmbeddingStore 的 chunk-8 + 让出事件循环节奏）、mean pooling + L2 归一；
- 接线：cliHarness 用 build-cli 已有的 `PURE_CLI_VERSION` define 判定编译产物，注入 `embedBatch` 到 `WASMEmbeddingStore`（注入缝现成，`embed`/`embedBatch` 选项就是为此留的）；dev `bun run cli` 维持 transformers.js 现状；
- 失败路径免费：注入 embedder 抛错即走 wrapper 现成的关键词兜底；
- wasm 资产平台无关，CI 交叉编译矩阵不受影响；
- 待验项：与 transformers.js 的向量逐点对齐（q8 vs 默认 dtype 的检索质量差）、numThreads>1（现单线程，3 文本 53ms 已够用）。

### E0.4 实施：OrtWebEmbedder 落进发行版 CLI（2026-09-18 续4）

按 spike 的 M 方案落地。发行版二进制现在开箱即有语义记忆检索：q8 MiniLM（23MB，首用时下载、之后缓存）+ 内嵌 ort-wasm runtime（零下载），dev `bun run cli` 维持 transformers.js 路线不变。

**新增**：
- `src/adapter/memory/OrtWebEmbedder.ts`（~200 行）：懒加载单飞 init（失败自清、下次搜索重试，节奏由 wrapper 的 60s 冷却管）、chunk-8 批推理（块间 `setTimeout(0)` 让出事件循环，给 Harness 的记忆搜索超时留活路）、mean pooling + L2（与 transformers.js `pooling:'mean', normalize:true` 同一数学，向量可比）；资产下载 huggingface.co 优先、hf-mirror.com 兜底（`HF_ENDPOINT` 可覆盖）；内嵌 wasm 运行时首用时解包到缓存目录。
- `src/adapter/memory/ortEmbedderMath.ts`：纯数学 helpers（截断/池化/分块/端点解析），单独成文件是为了让测试不碰带资产导入的模块。
- `src/adapter/memory/ortAssets.d.ts`：两个 wasm 子路径导入的类型声明。
- 测试 ×11：math 手算用例（mask 加权池化 12 位小数对齐）+ embedder 行为（全端点不可达的干净拒绝与回退顺序、失败后可重试、全缓存时零网络且运行时已解包）。全套 2096 过。

**接线**：
- `memoryFactory.ts` 的注入缝补上 `embed`/`embedBatch` 透传；`cliHarness.ts` 以 `PURE_CLI_VERSION`（build-cli 的 `--define`）判编译产物 → 动态 import OrtWebEmbedder 注入；加载失败打印一行提示后落到原 wrapper（编译产物里 transformers 本来就装不上，自然退化关键词，即 pre-E0.4 行为）。
- `onnxruntime-web` / `@huggingface/tokenizers` 提为直接依赖（exact pin），不再蹭 transformers 的传递依赖。

**关键一课（编译产物专属）**：必须显式设 `ort.env.wasm.wasmPaths = { wasm, mjs }`（`pathToFileURL(...).href`）——否则 ORT 相对可执行文件解析 wasm 工厂（二进制内是 `/$bunfs/root/...`，不存在），报 `no available backend found`。smoke 第一轮就死在这，spike 里配过、重写时丢了，捡回来才通。`numThreads=1`（单线程 8 文本 ~50ms，够用）。

**验证**：
- typecheck 干净；2096 tests / 0 fail（+11）。
- 二进制 70.6MB → 79.8MB（+9.2MB，bun 压缩内嵌资产，好于预估的 +13MB）。
- 编译产物真 smoke（node_modules 移走、空缓存）：镜像回退下载 3 资产 → 解包 runtime → dim=384 / norm=1.000000，cos(正对)=0.4047 / cos(负对)=0.0287，与 spike 逐位一致；热批 12ms。首用 59.8s 里 ~50s 是 23MB 模型下载（慢镜像、一次性）。
- 真实 `pure` 二进制启动时 TLA 已动态导入 embedder 模块（无 fallback 提示行）——门控加载在真产物里也通；完整 CLI 会话级 e2e 留给打包试用（要走真实 provider）。

### E1.1：turn 后反思器（Lesson Reflector）（2026-09-18 续5）

Completed 之后，满足触发条件的回合（有失败，或工具调用 ≥3 的非平凡多步）用**便宜快模型**跑一次结构化反思，产出带证据的 lesson 落库，替代模板拼接；模板版保留为反思失败/未触发时的兜底和降级路径。fire-and-forget：反思在后台跑，回合照常收尾，`settleReflections()` 供入口退出前排空（one-shot 收尾处、REPL /exit 各接了一次）。

**防幻觉纪律（结构性强制，不信任模型自觉）**：反思只能引用本轮证据目录里的调用哈希（sha256(toolName::args) 前 12 位，Harness 自己算好喂给模型）；目录外的 id 一律剥掉，剥完什么都不剩 → `confidence:'low'` 强制。low 条目注入端默认跳过（composeMemoryPrompt 过滤），evolution 健康分可信度维度减半（自然沉底），条目仍在库里供设置页/仪表盘查看。

**成本护栏**：每日上限默认 20（直接数库里当天的 `reflect:` 条目，不引入第二份状态文件）；触发门控（0 工具调用的纯聊天永不反思）；`reflection.enabled` 一键关。反思 LLM 调用 60s 超时，超时/坏回复/传输失败统统退回模板。

**改动**：
- 新 `src/harness/LessonReflector.ts`：证据提取、触发判定、transcript 构造、宽松 JSON 解析 + 严格证据校验、每日计数、`reflectTurn` 往返封装。 Harness 只做编排和落库。
- `Harness.ts`：HarnessConfig 新增 `llmFor`（E0.3 的按阶段 resolver 正式接进 buildContext，THINK/HANDOVER 路由从今天起可配）和 `reflection`；两个 Completed 缝（run/continueTurn）切到 `writeSessionLesson` 分发；`settleReflections()`；注入端 low 过滤。
- `types.ts`：MemoryEntry 加可选 `confidence`，MemoryLesson 加可选 `evidence`（纯增量，旧条目不受影响）。
- `evolution.ts`：confidence low → 可信度 ×0.5。
- `CodingAgent.ts`：`llmFor`/`reflection` 透传给 Harness（GUI 同享）。
- `cliRepl.ts`：one-shot 收尾与 /exit 接 settle（5s 上限，不挟持退出）。
- **E2.1 顺车**：反思 schema 带 `procedure` 字段，验证通过的成功多步任务额外落一条 procedure 记忆（≤600 字符，检索/衰减全走现有链路）。

**验证**：typecheck 干净；2119 tests / 0 fail（+23：LessonReflector 单测 17 + Harness 集成 6——触发替换模板、真实 id 高置信、模型编造 id 强制 low、反思失败退模板、开关/每日上限、low 不注入）。测试里踩了一个坑：没有 tools adapter 时引擎一轮就收（无法积累多步证据），集成测试挂了个成功的 read_file 才触发反思路径。

**待验项**：REFLECT 阶段尚未有任何入口配便宜模型（`llmFor` 缝已通，CLI/GUI 设置面还没暴露按阶段模型配置）——配之前反思回落主模型，功能正确但烧主模型的钱；反思产出质量需真实语料观察（同 E0.4 的 q8 待验项一起，留给试用反馈）。

### E1.3：工具/服务级自修正建议（2026-09-18 续6）

**机制**：扫库里的 error_pattern 条目（四个 Harness 失败写入点的 content 都带 `(tool: xxx)` 标签），提取工具名 + `classifyFailure` 归类错误，按 **工具 × 错误类** 跨项目聚簇。14 天窗口内同类失败 ≥3 次 → 出一条建议卡：英文 caution note（`Caution: web_fetch hit 3 network failures in 14 days on this machine.` + 分类对应的中文提示）。

**采纳制，永不自动落库**：建议只是展示；GUI 设置页「工具使用建议」卡点「采纳」→ 写一条机器级 `tool_preference`（GLOBAL_MEMORY_SCOPE + platform 标签 + dedupeKey `tool-note:<工具>::<错误类>` 防重），复用现有「机器级常驻注入」通道进 `<session_memory>` 的 tools 段——**零新记忆类型、零新注入通道**。已采纳的 (工具,错误类) 组合不再重复建议。CLI 侧只读：启动时 stderr 提示最多 3 条（指引去 GUI 采纳），CLI 自己从不写。

**实现简化（相对设计稿的诚实记录）**：设计提示"复用 memoryDecayTimer 空闲调度"，实际扫描做成了纯同步 O(条目数) 函数——设置页渲染工具清单时顺手扫、CLI 启动时扫一次，没有后台定时器的必要。建议上限 8 条，按簇大小降序。

**改动**：
- 新 `src/adapter/memory/toolCorrections.ts`：`scanToolCorrections`（聚簇/窗口/阈值/已采纳排除/排序截断）+ `approveToolCorrection`（落库形状）+ `detectPlatform`。
- `FSMemoryStore.listAllEntries()`：枚举全部项目桶（工具注意事项是机器级的，单看当前项目会漏）。
- GUI：设置页工具区新增「工具使用建议」卡（i18n zh/en、行渲染、采纳按钮 + 失败静默保卡、styles.css 采纳态样式）。
- `cliHarness.printToolCorrectionHints()`：stderr 只读提示；`cliRepl` 在 one-shot 预算诊断后和 REPL 欢迎语后各调一次（piped 输出保持干净，整段 try/catch 吞错）。

**验证**：typecheck 干净；2126 tests / 0 fail（+11：scanner 单测 10——跨项目聚簇、窗口过滤、分类隔离、排序截断、无工具标签跳过、已采纳排除、落库形状、300 字符上限——加 approve→注入集成 1：采纳后新会话系统提示词在零 token 重叠查询下仍带 note）。

**待验项**：聚簇阈值（3 次/14 天）是否合适需真实语料观察；CLI 提示文案的语气看试用反馈再调。

### E2.2：经验→技能沉淀，用户触发（2026-09-18 续7）

对话里说「把这个做法沉淀成技能」（英文 save/turn this into a skill 同样识别），pure 就把最近的过程记忆用一次 LLM 调用扩写成标准 SKILL.md，落到 `~/.pure/skills/auto-<name>/`——下个会话 `<skills>` 注入自动带上。设计明确**不做自动触发**（"使用后成功"的归因要等 E4.1），parseSkillMarkdown / `<skills>` 注入链路零改动。

**触发面**：CLI one-shot、CLI REPL、GUI 聊天三个入口都拦。短语锚定正则（中英各一组），宁可漏配不可误触——误触会吞一条正常消息。生成中不接受（和 /undo 同规）；GUI 在 beginLiveTurn 之前拦，自己渲染气泡，不留 ghost 消息。

**实现简化（诚实记录）**：设计说"取最近命中的 procedure/playbook"，但"命中"归因数据不存在，第一版取**最近一条** procedure（E2.1 的产物），库里没有就退到最近一条反思 lesson（`reflect:` 前缀）。等 E4.1 有归因再升级成真"命中"。都没有就如实说"先完整跑一个多步任务"。

**防护**：技能名一律净化 + 强制 `auto-` 前缀（和手工装的区分开，也是防路径逃逸的单段安全名）；同范式重沉淀即覆盖更新；LLM 60s 超时、坏回复/传输失败一律不写盘，人话报错。扩写提示词明确"只准用过程记忆里的步骤，不许编"。

**改动**：
- 新 `src/shared/skillDistill.ts`（纯函数核心：指令正则、来源挑选、提示词、解析净化、LLM 往返）+ `src/cliSkillStore.ts`（CLI 落盘，目录解析和 loadAppSkills 同一公式）。
- `cliHarness.distillSkillFromMemory()`：编排（跨项目挑来源 → 扩写 → 落盘），结果用可判别联合返回，文案归 cliRepl。
- `cliRepl`：one-shot 在工作流机器前拦（跑完断 MCP 即退），REPL 在 /prompts 之后拦。
- GUI `chat.runSkillDistill()`：状态条反馈（进行中→成功/失败），经 Rust `write_app_skill` 落盘；浏览器模式如实说写不了。
- Rust `delete_app_skill`：与 write 同规格的安全名校验，只删用户技能目录里真实存在的目录。
- 设置页技能卡：`auto-` 前缀的用户目录技能出现「删除」按钮（手工装的/项目技能保持只读）；i18n zh/en。

**验证**：typecheck 干净；2142 tests / 0 fail（+16：指令正则中英+反例、来源挑选优先级、解析净化（围栏容忍/前缀强制/非法字符/兜底名/坏回复拒绝）、LLM 往返（成功/失败/超时）、落盘→parseSkillMarkdown 回读闭环（等于验了「下个会话可见」的读取端）、不安全名拒绝）；Rust `cargo test app_skill` 11 通过（含 delete 的安装→删除回环与路径逃逸拒绝）。

**待验项**：触发正则在真实语料里的误触率；沉淀出的 SKILL.md 质量（取决于 E2.1 procedure 的质量）；「最近一条」vs「最近命中」等 E4.1 归因数据。

### E2.3：计划层并行标注（2026-09-18 续8）

**机制**：策略层（adaptiveControl）早已输出 `parallelRoles`（本任务推荐里可并行的角色，per-request 粒度），这次把它接进计划链路：计划期用与 buildContext 相同的输入复算一次 `select`（纯计算无副作用），`markParallelPlanSteps` 把推荐角色的**域关键词**（调研/评估/审计/评审/界面设计）对到计划步骤上——命中 ≥2 步才标（单个标记没有组批对象）。计划卡步骤行出现「⫲ 可并行」徽章；批准后的提示词里同样的 ⫲ 记号 + 一条组批规则：这些步相互独立、把派发合并进同一轮，阶段标记仍按序写。

**收益边界（设计原话，如实复述）**：独立只读子代理今天已经能并行（ToolExecutionCoordinator 的 reads 并发池 + SubagentOrchestrator 的只读角色分类，两处都是现成的），真实增量就一条——计划层把"模型默认串行走"的步骤**识别**为可并行，省下的是那部分墙钟时间。运行端零改动。

**实现说明（诚实记录）**：步骤标注是纯规则（角色域关键词），不加 LLM 调用——标错顶多少并行/多并行一次只读派发，代价可控；角色→步骤的桥接靠步骤文本，因为规则生成的计划步骤本身不带角色字段。GUI-only：CLI 的计划不走确认卡，⫲ 标记暂不进 CLI 提示词（CLI 模型本来就被允许同轮多派发）。

**改动**：`PlanStep.parallel?` 字段；`Planner.markParallelPlanSteps()`；计划卡徽章（plan.ts + styles.css，zh/en i18n）；`formatPlanForPrompt` 的 ⫲ 记号与组批规则；chat.ts 计划期接入。

**验证**：typecheck 干净；2150 tests / 0 fail（+8：标注规则 5（并行 fixture 命中、域合并、无匹配、不足两步、入参不改写）+ 提示词标记/规则 2 + 卡片徽章 1）。

### E3.1：反思器顺带产出 convention/preference 草稿（2026-09-18 续9）

**机制**：搭 E1.1 反思器的便车，零新节奏——REFLECT 提示词加一个可选 `correction` 字段（`kind`: project_convention / user_preference + `statement` ≤200 字），只在用户**明确纠正过**输出风格/做法/项目规矩时输出（语气揣测不算，提示词里有硬规则）。解析端白名单校验（kind 出格、statement 不足 8 个有效字符的一律丢弃，宁可漏收不往仪表盘塞噪音）。命中的纠正写成一条 `confidence:'low'` 草稿：没有注入资格（注入端本来就过滤 low），落库只为仪表盘的待确认卡；dedupeKey 用内容哈希（`correction:<kind>:<sha12>`），同一句纠正跨会话只落一条，且**确认后**反思器再写同一句会撞在已确认条目上被去重，不会回头再冒草稿卡。

**确认即注入**：仪表盘记忆卡上，low 置信的 convention/preference 显示「草稿」徽章（虚线边，读作"还没生效"）+「确认」按钮。确认是安全方向的操作，不叠二次弹窗；实现是 removeById + 重写一条（IMemoryStore 没有 update）——confidence 抬到 'high'，健康分不再减半，注入端放行。确认后的 project_convention 走新增的注入片段（优先级 46，紧挨用户偏好——都是"用户亲口说的"）；确认后的 user_preference 走既有 User preferences 通道。E1.1 的 error_pattern low（防幻觉降级）不属于草稿，不显示确认按钮。

**砍掉的部分（设计允许）**：AGENTS.md 镜像不做——项目约定已经通过 `<project_conventions>` 片段（required 级）必达模型，再镜像一份是重复注入。正则收割层（既有，零成本）原样保留，LLM 层就是反思器顺带产出，不设独立节奏。

**改动**：`LessonReflector`（correction 字段 + 提示词 + 校验 + correctionDedupeKey）；`Harness.reflectLesson` 草稿写入；`Harness.composeMemoryPrompt` 收集 projectConventions；`PromptAssembler` 新片段 `memory_project_conventions`；`adapter/memory/correctionDrafts.ts`（isDraftEntry / confirmedDraftEntry 纯助手）；设置页记忆卡草稿徽章 + 确认按钮（zh/en i18n + 样式）。

**验证**：typecheck 干净；2165 tests / 0 fail（+15：correction 解析 4 + 草稿助手 6 + Harness 集成 5——草稿落库形状、内容哈希跨轮一致、无纠正不写、low 不注入→确认后两种类型都注入）。

### E4.1：策略效果记录（2026-09-18 续10）

**机制**：把"这次跑在哪档策略下"直接记到 agent_run 观测记录上 —— Harness 在 startRun 时把 `observeStrategy(currentAdaptiveStrategy)` 挂进记录（只记可切片的维度：exploration/verification/delegation/recovery/autonomy/complexity + confidence + intentTags + 推荐/并行角色 + priorArtHint；signals/rationale/directive 这些散文字段刻意不记——切不了还占 JSONL）。策略→结局的关联因此是**零 join**：同一条记录本来就带 toolCalls、verification、outcome。continueTurn 沿用 §2.1 冻结的会话策略，归因到它是忠实的（那确实是模型看到的 directive）。

**per-role 不需要新写路径**：子代理工具本来就是按角色命名的（researcher / code_reviewer / …），ToolObservation 的 toolName 即角色，聚合端按 KNOWN_SUBAGENT_ROLES 归类即可。

**聚合器**（新 `shared/strategyEffect.ts`，只读纯函数）：`summarizeStrategyEffects(records)` 一次给出 overall + 五个策略维度切片 + 角色切片；验证三桶互斥全覆盖（passed / failed+incomplete / 无记录或 not_run）；没有 strategy 的旧记录不进策略切片（但它的委派照进角色切片——角色统计不依赖策略）；每个维度的桶 runs 相加恒等于 overall.runs（测试锁了这条不变量）。E4.2 仪表盘直接消费，E1.4 per-role 自校正建议从这里起步。JSONL 落盘零改动（strategy 字段随 agent_run 行自动进盘）。

**改动**：`promptObservability.ts`（StrategyObservation + observeStrategy + startRun 透传）；Harness 两处 startRun 挂策略；`shared/strategyEffect.ts` 新聚合器。

**验证**：typecheck 干净；2173 tests / 0 fail（+8：聚合器 6（总体、验证三桶、五维度切片+分区不变量、角色聚合、无策略记录跳过、零时长省略均值）+ Harness 集成 2（run 记录带本轮策略+同记录结局；continueTurn 沿用冻结策略））。

### E4.2：进化仪表盘（2026-09-18 续11）

**机制**：设置页新增「进化」一栏（`data-category="evolution"`），把三处数据拼成一张可读的状态图：观测日志（周/月趋势）→ E4.1 聚合器（策略/角色切片）→ 记忆库（经验条目直达清理）。E0.1 当时推迟的"统计 UI"就在这里落地。

**口径**（都是"未来读者会问"的）：
1. **窗口 = 最近 7 / 30 个本地日历日**，桶覆盖窗口内每一天：没跑过的日子留空桶（null）而不是 0 —— 折线上是断点，不是假谷底；计数类指标（干预/工具失败）在 `runs === 0` 的那天同样按缺样本处理，不画一排零高柱子。
2. **干预 = 被中断 或 验证未通过** 的 run 数（同一 run 两种都占只算一次）；完成率 / 步数 / 缓存命中率的空窗口是 null，界面上显示 `—`。
3. **错误簇 = 窗口内失败的工具调用按 工具 × 错误类 聚簇**，次数降序取前 8，带会话数与最近发生时间。

**读取有界**：Rust 新增 `read_observations`（尾巴读 `~/.pure/observations/app.jsonl`，默认 16MB，且会丢掉被切在半途的首行）——64MB 的日志整段跨 IPC 不划算；`truncated` 为真时界面明说"本次只统计了最近 N"，不假装全量。

**实现说明（诚实记录）**：
1. **图表自己画 SVG**，没有用现成的 `echartsChart`：设置页是懒加载的轻量 chunk，为三张小趋势图拖进 ~400KB 的 echarts 不划算；折线/柱状 + 两条轴 60 行纯函数就够，而且能逐坐标断言（含"缺样本断线""空窗口不画"这类口径）。
2. **徽章/删除按钮复用记忆页的 CSS 类**（`memory-badge` / `memory-type-*` / `memory-life-*` / `memory-delete-btn`）—— 同一种记忆在两张页面上应该长得一样，不另造一套颜色。
3. **经验条目 = 进化产物**（error_pattern / procedure / successful_pattern / 带 lesson 的条目 / E3.1 待确认草稿），实时算健康分，最多列 50 条；删走与记忆页同一条通道（确认弹窗 → `removeById`），不直接写存储。
4. 记忆功能关着时与记忆页同一口径：不列卡片，只说清为什么空。

**改动**：新 `src/shared/evolutionDashboard.ts`（聚合器）、`src/ui/evolutionDashboard.ts`（纯渲染）、`src/ui/observationSource.ts`（Rust 桥）、`scripts/e2e-evolution-dashboard.ts`；`promptObservability.ts` 新增共享 JSONL 解析器（`FilePromptObservationStore` 改为复用）；Rust `read_observations` + `ObservationDump`；`index.html`/`settings.ts`/`styles.css`/`i18n.ts`（zh/en 全量文案）。

**验证**：typecheck ✓；2213 tests / 0 fail（+40：聚合器 11、渲染 23、sha256 4、解析器 2）；Rust 222 通过（+3 尾巴读）；`npx vite build` ✓；**真实浏览器 e2e**（`bun run scripts/e2e-evolution-dashboard.ts`）PASS —— 7 个汇总块 / 3 张趋势卡挂载、无漏翻的 i18n key、浏览器模式的空状态文案、周↔月切换、种一条 procedure → 经验区出现 → ✕ → 确认弹窗 → 记忆库真的少一条（全链路）。

**待验项**：趋势形状要攒几天真实数据才有意义；策略表结论需要足够 run 数（同 E4.1）；CLI 侧没有对应页（设计上就是 GUI 一栏）。

**本次补充（2026-09-19）**：
1. **五个策略维度改成维度切换**：E4.1 记的是五维（exploration / verification / delegation / recovery / complexity），仪表盘原来只展示验证 / 委派两档——探索 / 恢复 / 复杂度的数据一直烂在库里；直接五张表并排又会堆成一面墙。现在策略区顶部是维度胶囊切换器（复用周/月那套 `.evo-range` 视觉，默认验证档位，窄面板自动换行），一次只展示选中维度的表 + **常驻的角色表**（角色是另一个视角，不参与切换）；表内不再重复标题——紧贴上方的 tab 就是标题。可切维度以 `STRATEGY_DIMENSIONS`（`shared/strategyEffect.ts`，原本是聚合器的内部常量）为单一来源，tab 的顺序与短名由 UI 决定、覆盖同一组五个维度；补了 5 个维度短名（`evolution.dimension.*`，取代刚加的 `evolution.table.<dimension>`，避免留下死 key）+ 8 个档位标签的 zh/en 文案（broad / continue-with-evidence / switch-approach / use-verified-procedure / trivial / simple / moderate / complex）。空态分两层：窗口里没有任何带策略的记录 → 不渲染切换器（一排点不出东西的胶囊只是噪音）且只给那句"还在攒"；所选维度没记录 → 该维度空态而不是空表格。渲染测试 +6（tab 齐全且标出选中态、只渲染选中维度、维度内按 run 降序、维度空态、无策略记录时隐藏 tabs、无策略记录仍出角色表）；e2e 的浏览器模式空态断言也扩到"不出现死 tab"。
2. **e2e 把五个维度都跑一遍（真实浏览器）**：浏览器模式没有 Rust 尾巴读，所以 `observationSource` 加了一个**显式注入点**（`globalThis.__PURE_OBSERVATION_FEED__`，JSONL 文本、走与 Rust 尾巴读同一条解析器；没设的时候行为逐字不变，也不会短路 Tauri 路径）。e2e 种两条带策略的 `agent_run`（五个维度各两个档位），先验五个 tab 与默认选中项（验证），再逐维点过去断言"表里只出现选中维度的档位"，最后确认角色表仍常驻；`observationSource` +3 单测（无后端/注入解析并跳坏行/非字符串不炸）。
3. **Rust 计数口径修正**：上面那次验证是 222 通过；E4.2 的守护测试 `read_observations_serves_the_dashboard_from_the_real_home_dir`（HOME 解析 → 尾巴读 → IPC 字段名契约）写完后漏提交，本次与五维展示一并入库 —— 现在 `cargo test` 是 **223 passed / 1 ignored**。

### E1.4：子代理角色建议（2026-09-18 续12）

**机制**：E4.1 把每次派发的结局记进了观测记录（子代理工具就是按角色命名的，`toolCalls` 里 `toolName` 即角色），这里把它读成"哪个角色在反复掉链子"的结论 —— 窗口（默认 30 天）内某角色**派发 ≥ 3 次、失败 ≥ 2 次、失败率 ≥ 40%** → 出一条建议卡，展示在设置页「进化」区（紧跟在策略/角色表后面，同一个观测切片）。

**归因分两种**（同一组阈值，结论不同）：
1. **超时为主**（超时类失败 ≥ 2 且占失败过半）→ 这不是偶发报错，而是**任务体量不对**，建议拆小/换角色；
2. **失败率偏高**（其余）→ 泛化结论，卡片带上主导错误类（timeout/network/tool_error…）与平均耗时。

**卡片只说用户能拉的那个闸**（设计原话是"第一版只建议不自动改"，所以卡片刻意不给"一键修复"按钮）：
- 有技能开关的角色（researcher / code_reviewer / project_auditor / task_planner）→ 指到「设置 → 技能 → 网页搜索/代码审查/规划」；
- 没开关的角色（code_editor / deep_thinker / ui_designer）→ 如实说"这个角色没有开关，只能把任务描述写小、把这一步拆窄"，不假称有开关。

**排除项**：`bash_executor` 不算角色（"穿了 agent 外壳的 shell 命令"——与活动栏、工具卡片同一裁决：命令失败该修命令）；非角色工具（`read_file` 等）的失败永不进表。

**改动**：新 `src/shared/subagentAdvisory.ts`（扫描器）+ `src/ui/evolutionDashboard.ts` 的 `renderSubagentAdvice`；`settings.ts` 接线（复用同一次观测读取）；`index.html` + i18n zh/en + styles.css（严重度左侧色带）。

**验证**：typecheck ✓；2213 → **2228 tests / 0 fail**（+15：扫描器 11 + 渲染 4）；浏览器 e2e 的空状态断言已扩展到这个新区块，PASS。

**待验项**：阈值（3 次/40%）需真实语料观察；**"产出常被重做"这类信号还没有**——今天只有每次派的成功/失败/耗时，没有"交付物被打回"的归因，那要等更细的输出级指标；CLI 侧不做（建议只在 GUI 可见可操作）。

### 计划外专项：GUI 构建被 node:crypto 打挂（2026-09-18 续11，**发布阻塞级**）

做 E4.2 的浏览器 e2e 时撞出来的：**`npx vite build` 在 HEAD 上直接失败**，浏览器 dev 里整个 `main.ts` 模块图也断在同一个点（设置按钮点了没反应，e2e 抓到的原话是 `Module "node:crypto" has been externalized…` @ `LessonReflector.ts:1`）。

1. **根因**：E1.1 的 `LessonReflector` 用 `node:crypto` 的 `createHash` 算 sha256，而 `Harness` 在 GUI 路径上是**静态导入**的 —— 这不是"少个功能"，是 Vite 浏览器包根本解析不了 node 内置模块，rollup 直接报 `failed to resolve import "node:crypto"`。CLI 走 bun/node 打包，所以本地 `bun test` / `bun run cli` 全绿，问题只在 GUI 侧露头（E1.1 当天没发现，因为没跑过 GUI 构建）。
2. **修法**：新增共享内核的纯 TS SHA-256（`src/shared/sha256.ts`，FIPS 180-4 教科书实现，零依赖、同步），`LessonReflector` 改用它。**保留 sha256 而不是换成弱哈希**：证据 id 与 E3.1 的草稿 `dedupeKey` 都已落库，换算法会让旧 key 对不上新写入、同一句纠正重复冒卡。
3. **验证**：NIST 向量 + 与 `node:crypto` 逐位对齐（空串/abc/官方两段样例、55/56/63/64/119/127/128/1000 字节的 padding 边界、中文与 emoji、20 万字符长输入）。随后 `npx vite build` 通过（33.6s），浏览器 e2e 也从"点不动设置"变成 PASS。
4. **剩下两处 `node:crypto`**（`shared/userHookApprovals` / `adapter/memory/FSMemoryStore`）都是 CLI-only，不在 GUI 依赖图里 —— 构建通过即为证。
5. **教训**：E 轨道的新模块只要碰 node 内置能力（crypto / fs / path），就必须跑一次 `npx vite build`（GUI 是 Vite 打包、CLI 是 bun 打包，`bun test` 抓不到这类断链）。

### 1.4 真实 provider 基线第一格：DeepSeek（2026-09-19）

DeepSeek `deepseek-v4-flash` 跑完整套 9 个 fixture：**9/9 passed**（pass@1 9、successRate 1.0、meanScore 1.0），平均 15.1s/任务、6–19 次工具调用、估算成本 $0.0143（prompt 1.09M token 中 **95.8% 命中缓存** —— 顺手把 §8.2/8.3 的缓存链路在真实 provider 上点亮了）。报告 `evals/deepseek-v4-flash.json`（suite `pure-coding-baseline-v3`、fixtureHash `c80c0ab4`、revision `e73e865`、bun 1.3.14 / darwin），汇总与逐任务口径写进新文件 `evals/BASELINE.md`。

**诚实解读**：9/9 说明**这套 fixture 目前在该模型能力之下** —— 它是回归门禁（掉分=回退），不是区分度基准；要看"进化改动到底有没有用"仍需更难的题（1.5 Terminal-Bench 困难组）。GLM 有 key 但本轮未跑；Qwen 缺 DashScope key + workspace id，两个空格都如实留白而不是猜数、不用 mock 数字充数。

**怎么跑的（备查）**：`DEEPSEEK_API_KEY`（取自 `~/.pure/secrets.json`，只作为环境变量传给子进程）+ `--agent deepseek-openai --strict --report evals/deepseek-v4-flash.json`，全程约 2 分钟。**踩坑**：本机工具链会在命令返回时回收自己进程树里的后台进程（`nohup`/`setsid` 都护不住），长跑必须挂 tmux 会话，否则日志只有一行 bun 回显。

### 1.5 本地困难组：加了四个 hard fixture，pass/fail 仍未拉开（2026-09-19）

按"让基线真的有区分度"的目标加了一层困难组（`9b7d102`）：`difficulty` 类型扩出 `hard`，`CODING_TASK_SUITE_VERSION` 升到 `pure-coding-baseline-v4`（fixture 集合变了，hash 必须变），新增 4 个 fixture + 4 个 golden 解。四个都要求真推理，且只靠行为观测判分：

- `hard-bugfix-task-queue-leak`：队列两处交互缺陷（并发上限差一 + 失败任务不释放槽位）——只修表层那个仍然失败；
- `hard-refactor-break-cycle`：order ↔ pricing 真循环依赖，检查器走 import 图，任何结构性修法都接受；
- `hard-recovery-stale-cache`：症状是产物不对、根因是过期缓存；检查器连续构建两次（中间改输入），硬编码答案过不了；
- `hard-multi-step-api-migration`：按 docs 契约实现新 API + 迁移 3 个调用点 + 彻底删除旧导出，检查器扫描 src/ 且要求调用点还在。

`eval:sanity` 13 个 fixture 双绿（control 全部按种子失败、golden 全部通过）；全量 TS 2241 通过。

**实测**（写入 `evals/BASELINE.md`）：DeepSeek `deepseek-v4-flash` **13/13**（平均 39.5s/任务、$0.0184）；GLM `glm-5.3-flash` **13/13**（平均 87.6s、$0.4270）。**pass/fail 口径下这套题对两个模型仍然没有区分度** —— 困难组确实更费劲（DeepSeek 在 hard 组平均 95s vs 其它约 12s；迁移那个单任务 259s / 17 次工具调用），但两个模型都做对了。

**这轮并非白跑**：成本/耗时口径的区分非常清晰 —— 同一套 fixture、同一 harness，GLM 墙钟是 DeepSeek 的 **2.2 倍**、成本 **23 倍**（缓存未命中 147.5k vs 42.3k，prompt 量级相同）。所以 v4 的正确定位是**回归门 + 成本/效率基准**，不是难度阶梯：掉分会表现为失败，变贵会表现为这两列。

**结论与下一步**：再堆"同类更难的题"不解决问题 —— 四个围绕交互缺陷 / 结构重构 / 根因排查 / 破坏性迁移的题都被清掉，说明这两个模型都在这类题的射程内。要真拉开 pass/fail，得换维度（按优先级）：① **仓库级规模**（几十个文件、agent 得自己找相关代码）；② **硬资源约束**（性能/内存天花板，朴素实现必挂，注意跨机器确定性）；③ **长时程纠错**（要连续失败几次才能完成，考的是不反复撞墙）；④ 用更小的模型跑同一套题（弱模型很可能立刻在 hard 层掉分，这本身就是有用的矩阵列）。Qwen 那一格仍缺 key。

### 1.6 极端档 + v5 基线：pass/fail 第一次拉开（2026-09-19）

**修复（`bc793e5`）**：坏的模型代号（glm-5.2-flash，z.ai 网关不服务）让引擎以 `interrupted: true` 收尾且零工具调用，评分器把它记成普通失败——0/13、每任务约 1 秒、无 usage 无 error，报告里看不出任何原因。现在执行器遇到致命 Error 事件或 interrupted 收尾直接抛错，任务记为 `agent_error`；run-evals 在全部任务都是 `agent_error` 时拒绝写报告（exit 2）——配错的 provider 不能长得像一个"考了零分"的模型。

**新档位（同提交）**：1.5 的结论是"同类更难的题不再区分"，v5 直接补两个此前没覆盖的维度（套件 15 fixtures，`fixtureHash 6087e3be`）：
- `extreme-repo-scale-metrics-report`：45 文件仓库，题面只给一个错误数字、不点名任何文件，agent 得自己把数字溯源到唯一的聚合器；
- `extreme-perf-dedupe-scaling`：资源上限题——种子实现功能正确但是平方复杂度，检查器看**伸缩比**（4× 记录必须 < 7× 耗时）而不是墙钟绝对值，跨机器确定性成立。

**v5 实测**（报告 + 重写的 `evals/BASELINE.md` + `eval:snapshot` 快照均已入库，`d697690`）：

| 模型 | pass@1 | 均耗时 | 成本 | 缓存命中 |
|---|---|---|---|---|
| deepseek-v4-flash | **15/15** | 27.2s | $0.0237 | 97.3% |
| glm-5.3-flash | **15/15** | 113.5s | $0.5581 | 91.1% |
| glm-4.5-flash | **13/15** | 236.2s | $0.5707 | 97.8% |

**诚实解读**：区分度终于来了——glm-4.5-flash 的 13/15 就是 extreme 档存在的意义。但两处失败都是 `agent_error`，**根因是我们自己的评测硬上限（`EVAL_BUDGET`：30 轮 / 200k token / 20 分钟，设计使然、保证确定性），不是 provider 侧故障**：`hard-multi-step-api-migration` 是 `max_turns`（30 轮耗尽于 17.7 分钟，中断时 **bun test + 迁移检查双绿**，纯因 VERIFY 没来得及跑而记 0）；perf 题是 `Budget exceeded`（20.0 分钟整撞 `hardMaxTime`，伸缩检查没跑到，`verificationPassed: false` 是"没跑到"不是"失败"）。provider 全程健康（缓存命中 97.8%，中断时仍在推进）。所以 v5 的 pass/fail 区分混杂了"跑不完"与"做不对"，读数字时要知道这一点。仓库级搜索题三个模型全过（都在射程内）；perf 题是 GLM 4.5 死掉的地方（朴素平方实现就是陷阱）。成本/耗时仍是最硬的区分列：GLM 5.3 墙钟 4.2×、成本 24×；GLM 4.5 墙钟 8.7×。Qwen 格仍缺 key。

**快照链路**：`bun run eval:snapshot` 把三份 v5 报告折进 `src/shared/baselineSnapshotData.ts`（旧套件 3 份报告列入 excluded），进化仪表盘「评测基线」区在浏览器/桌面模式读同一组数字；守卫测试锁定快照的 suiteVersion/fixtureHash 与活 fixture 同步，升套件不重新生成会挂 `bun test`。

### 计划外专项：设置页滚动条重做 + 双主题对比度整改（2026-09-19，`aa5e445`）

试用反馈驱动的两块 CSS 整改（用 CDP 脚本逐项实测后落地，临时脚本已删）：

1. **滚动条体系**：标准属性与 `::-webkit-scrollbar` 规则在打架——`scrollbar-width` 一旦指定，Chrome 就改画**原生**滚动条，macOS 上是 overlay 条：不占槽位（offsetWidth − clientWidth 恒为 0）、不滚动就不显示，长列表读作"不能滚"。现在 webkit 上标准属性保持 `auto`（伪元素规则才生效、真占 6px 槽位），Firefox 没有伪元素可画，走 `@supports not selector(::-webkit-scrollbar)` 兜底给 thin 条。`#memory-list` 同一套规则；设置面板加 `scrollbar-gutter: stable`，页面长短切换不再左右跳 6px。
2. **长列表卡内滚动**：工具清单 / 工具建议 / 技能目录 / 用户技能 / hub 两区 / 进化三区 / MCP 服务器列表统一 `max-height: 420px` 卡内滚动（`overscroll-behavior: contain`），整页不再被几十条记录撑成长带；十个 ID 全部核对过在 index.html 真实存在。
3. **对齐与细节**：卡片直落内容（提示、诊断块、子列表）补到与 `.setting-row` 文字同一条左基准线（7+16=23px）；页面说明 620→760px、提示不再设 560px 上限；两字按钮（"检测"）不再被压成两行（`white-space: nowrap` + `flex: 0 0 auto`）。
4. **对比度**：两套主题 token 全面提亮——text-tertiary（浅色 2.5:1→4.7:1、深色 3.56:1→5.4:1）、border/border-light（可见性档）、accent 压深（白字 4.0→5.6:1）；实心按钮/胶囊文字走新 `--accent-contrast`/`--danger-contrast`（深色主题亮青 accent 上白字只有 1.5:1）；新增 `--warning` token（原 #d97706 白底 3.2:1）；provider 徽章文字色同步加深。

**验证**：`npx vite build` ✓（34s）；进化仪表盘真实浏览器 e2e **PASS**（含 3 行 v5 基线在仪表盘上的显示、五维切换、周/月、删经验全链路）；快照/仪表盘相关单测 43/43。

### 计划外专项：评测报告记录上限触发原因与轮数（2026-09-19 续）

上面 agent_error 的根因（`max_turns` / `Budget exceeded`）当时是靠 `agentError.chars` 反推的——执行器抛错消息前缀定长 39 字符，减一下就是引擎中断 reason。能反推说明报告缺字段，这次直接补上：

1. **`agentError` 增加 `code` / `reason`**：执行器抛错时把 `fatalError.code` 和引擎原始中断 reason 挂在 Error 对象上（`error.code` / `error.interruptReason`），`observeAgentError` 采集进报告；`reason` 只取第一个冒号前的固定形状（`max_turns` / `Budget exceeded` / `llm_stream_error`…），冒号后的自由文本（可能含 provider 报文）一律剥掉——延续"报告只带哈希不带报文"的脱敏纪律。旧测试的"不泄漏 secret-token"断言继续盖着这条路。
2. **`agent.turns`**：`Completed` / `Interrupted` 事件都带 `turnCount`，`collectAgentRunEvents` 记录终态轮数，与已有的 `toolCalls` / `usage` 并列——每任务"跑了多少轮"从此可直接看，不用从耗时猜。
3. **v5 报告不重跑**：三份 v5 报告早于本字段，BASELINE.md 里的反推过程作为史实保留；下次基线（Qwen 或预算放宽重跑）起自带。

**验证**：typecheck ✓；全量 TS **2265 / 0 fail**（+3：终态轮数采集、code/reason 落库 + 冒号截断脱敏、agent.turns 透传）；`bun run build` 全绿（typecheck + vite + CLI）。

### 9.2 按阶段模型路由配置面（2026-09-19 续）

E0.3 把 `llmFor` 接缝埋进引擎之后，阶段 9 一直停在"有缝没开关"的状态。这轮把 9.2 补上：**GUI 与 CLI 两端都能给 THINK / HANDOVER / REFLECT 三个相单独指定模型**（实验性），留空跟主模型走。v1 刻意只开同 provider 换模型——一个轮次一套 key/端点/代理，跨 provider 组合在接缝层依然可行，等真实数据说话再决定要不要放开。

- **共享归一**（`src/shared/phaseModels.ts`）：`phaseModelOverrides`（去空白、丢与主模型相同的值——同值再建一个 adapter 是纯浪费）、`mergePhaseModelConfig`（CLI 旗标按字段压过 config.json，与 provider/model 的 `--flag > config` 优先级同款）、`sanitizePhaseModelConfig`（存储层脏形状直接归 undefined，路由关掉、加载不炸）。
- **GUI**：`PureConfig.phaseModels`（loadConfig 清洗后透传）；chat.ts 每轮用共享工厂为覆盖相各建一个同 provider adapter（一轮建一次，缓存复用），经既有 `llmFor` 进 CodingAgent；设置页 → LLM 新增「按阶段模型路由（实验性）」区（虚线框 + 徽章），三个输入框挂 datalist，候选跟随**当前供应商**的模型库——相模型搭的是同一把 key，跨家模型 id 发过去也是错端点；中英文案照"省钱组合"的口气写（思考用强的、收尾和反思用便宜的）。
- **CLI**：`~/.pure/config.json` 认 `phaseModels`（GUI 写的那份，两端天然同源）；`--think-model` / `--handover-model` / `--reflect-model` 单次覆盖；cliHarness 经 `createAdapter({ ...args, model })` 重建相 adapter——端点覆盖、token 预算、provider 怪癖全部同主 adapter 口径，且主调用已验过 key，相重建不会再撞缺 key 退出。路由生效时 stderr 提示一行 `THINK→xxx, …`。
- **文档**：README_zh / README 的 CLI 用法各加一段（含示例 `--think-model deepseek-reasoner`）；标实验性。
- **验证**：typecheck ✓；`npx vite build` ✓（GUI 断链教训沿用，虽然本模块零依赖）；全量 TS **2272 / 0 fail**（+7：归一/合并/清洗 7 项 + loadConfig 级脏形状两项并入既有迁移测试）。

### 9.3 评测对比：强 THINK 不只救场，还倒赚（2026-09-19 续）

9.2 的开关装好了，9.3 要回答的问题很具体：**GLM 4.5 那两格 `agent_error`（30 轮耗尽 / 20 分钟撞墙，验证其实全绿）换成强模型只动 THINK 能不能救回来？** 跑了 `--model glm-4.5-flash --think-model glm-5.3-flash`（执行器 `thinkPhaseModel()` 仅在模型名不同才建 THINK adapter，相同值字节级等价于基线）：

| 指标 | glm-4.5-flash 基线 | + THINK→5.3 | 变化 |
|---|---|---|---|
| pass@1 | 13/15 | **15/15** | 两格 extreme 全救回 |
| 均耗时 | 236.2 s | 91.8 s | **−61 %** |
| 成本（费率表估价） | $0.5707 | $0.4197 | **−26 %** |
| 均轮数 / 均工具调用 | — | 6.5 / 9.6 | 不再空转 |

两个救回的格子尤其说明问题：`hard-multi-step-api-migration` 从 1060 s 撞 `max_turns`（验证全绿但 VERIFY 没跑上）变成 57 s 通过——前置规划强了，就不在原地打转；`extreme-perf-dedupe-scaling` 从 20 分钟撞墙变成 175 s 通过——强规划直接绕开了朴素平方实现的坑。**整条跑还更便宜了**：省下来的空转轮次远多于贵的 THINK 调用，缓存命中 95.0 %。这是矩阵里性价比最高的一次质量跃升：一个路由旗标，不改提示词、不改 fixture。顺带修了 `run-evals` 一个老 bug——`--model` 旗标在 BASELINE.md 复现命令里写了一路但从未被解析（一直在静默跑 provider 默认模型），现在旗标 > env，与其它参数同优先级。

- **实现**：`codingAgentExecutor.ts` 加 `thinkModel` 选项 + `thinkPhaseModel()` 归一（导出供测试）；`run-evals.ts` 加 `--think-model` / `PURE_EVAL_THINK_MODEL`，报告 metadata 记录 `thinkModel`（混跑永不冒充单模型基线行）。
- **验证**：typecheck ✓；全量 TS **2275 / 0 fail**（+3：不同模型建独立 adapter / 相同或空白退化为单 adapter / 带空白裁剪）；真实跑 15 任务 `Wrote evals/glm-4.5-flash.think53.v5.json`，EXIT_CODE=0。
- **BASELINE.md**：结果表、per-task 表、usage 表各加路由行；新增「9.3: strong THINK rescues both failures — and pays for itself」节；复现命令补 `--think-model` 示例。

**结论**：阶段 9 收官——`llmFor` 接缝（E0.3）→ 配置面（9.2）→ 数据验证（9.3）闭环，"强规划 + 便宜执行"从假设变成实测。攒数试用清单里值得加一条：日常任务试着开 THINK 路由，看体感。

### 插话重构：别动不动推倒重来（2026-09-19 续）

**问题**（用户体感 + 代码同证）：任务跑着的时候插一句话，pure 的反应不像人。老逻辑把插话只分"相关/不相关"两桶，而且**凡相关一律 abort + 重新规划**——一句"记得跑测试"也要把手头的活杀掉重来，烧掉已完成轮次、体感像任务重开了；而一句"哈哈"反而被当成新任务排进队列，等收尾后一本正经地"执行"它。分类器超时/挂掉的兜底还是"当相关 → abort"，最贵的一条路。

**设计**（换一个问法：不问"这句话相不相关"，问"一个埋头干活的人听到这句话会怎么处理"）。人只有两种情况会停下手里的活——对方说"别干了"，或者对方把方向掀了。其余都不值得推倒重来：

| 插话 | 处理 | 中断任务？ |
|---|---|---|
| stop（停止/stop/取消…） | 收尾当前任务 | ✅（正则直判，不占分类往返——停止等不起一次 LLM 分类） |
| goal-change（推翻重来/换方案…） | 停下，这句话作为新指令重新出发 | ✅（强动词正则快路；"改成X"这类模糊表述交给 LLM 带着上下文判） |
| **steer**（提醒/约束/小修小补） | **进引擎转向通道，下一个 THINK 轮顺路带上，活继续干** | ❌ |
| **question**（"跑到哪了"） | **侧路回答一句（优先 9.2 的 REFLECT 便宜模型），主循环毫无感知** | ❌ |
| task（不相干的新活） | 排队，当前任务收尾后启动（不变） | ❌ |
| **chatter**（哈哈/辛苦了） | **用户气泡上屏即可，不再排队当任务** | ❌ |

分类不可用时的兜底从"当相关→abort"改成**保守按 steer**：话一定送到引擎、活绝不重启——带着更多信息继续干永远比推倒重来便宜。

**实现**：
- **引擎转向通道**（协议安全的注入点）：`EngineContext.takeSteerMessages`，引擎在每个 **THINK 轮边界**（before_think hook 之后、模型调用之前）排空并入 `messages`——此刻前一轮所有 tool result 已就位，用户消息永远不会插在 assistant tool_call 和它的 result 之间（OpenAI 协议 400 的雷区），与失败策略注入同一边界、同一先例。ACT 中途注入不可能做到，THINK 边界是唯一干净的口。新增 `SteerInjected` 事件（仅观测用，UI 靠气泡呈现，不给消费方加全量 switch 负担——这条事件没人必须处理）。
- **五分类器**（Planner）：`classifyInsertion` 从 related 二元改为 `question/steer/goal-change/task/chatter` 五类（stop 不走 LLM），解析失败/未知 kind/LLM 抛错/空输入一律兜底 `steer`。
- **协调器**（DynamicInsertionCoordinator）：决策矩阵按上表重写，`shouldAbort` 只在 stop/goal-change 为 true；`CONSTRAINT_CHANGE_RE` 整个删掉——约束表述（"不要再加注释"）就是 steer，GOAL_CHANGE_RE 收窄到无歧义的推翻动词（推翻/重做/从头来/start over…）。
- **chat 分发**：steer → `pendingSteers` 队列 + 用户气泡 + 一行"已转达"；question → 用户气泡 + `answerMidrunQuestion()`（单次 LLM 调用，快照来自 buildInsertionContext，9.2 路由开着时走 REFLECT 相 adapter）；chatter → 用户气泡即可（同事埋头干活时你不会为一句"哈哈"停下回"收到"）；goal-change → fold-in 不变；steer 轮末残留 → dispatchDeferred 兜底转普通发送（一个字都不丢）。用户的话在 steer/question/chatter 路径上第一次以**自己的气泡**出现在对话流里，不再只藏在系统状态行里。
- 透传链：chat → CodingAgentConfig → HarnessConfig → EngineContext，全部 optional，CLI/子 Agent 不接 = 行为与旧版逐字节一致。

**验证**：typecheck ✓；`npx vite build` ✓；全量 TS **2288 / 0 fail**（+13：引擎转向注入两则——含"steer 必在 tool result 之后、整轮 transcript 配对完整"的协议断言、五分类器九则、协调器矩阵八则——老"不要=约束变更"用例反转为 steer 回归锚）。

### ⏸ 暂停点（2026-09-19 晚）：v3.0.0-alpha 收官提交

**本会话完成**（全部已提交，随后推送 GitHub）：
1. `2f650b7`：**9.2 按阶段模型路由**（GUI 设置 + CLI 旗标，见上节）
2. `2f85de1`：**9.3 强 THINK 实测**——15/15、−61 % 耗时、−26 % 成本，阶段 9 收官；顺手修 `--model` 旗标从未被解析的老 bug
3. `9bba208`：**插话重构**——五分类路由 + 引擎转向通道，"记得跑测试"不再推倒重来（见上节）
4. 版本号 **3.0.0-alpha**（package.json / tauri.conf.json / Cargo.toml / Cargo.lock）+ CHANGELOG 发布节（覆盖 v2.2.6-alpha 以来的 41 个提交：E 轨道收官、阶段 8、MCP 6.3、评测 v5、9.x、插话重构）
5. 攒数脚本 scripts/tmp-ui-*.ts 一并入库

**旧暂停点存档**（2026-09-19 白天）：v5 基线入库 `d697690` + 设置页打磨 `aa5e445`，见 git 历史。

**下一步候选**（按价值排序，2026-09-19 用户调整）：
1. **攒数试用**：E1.1 反思器 / E1.3 工具建议 / E1.4 角色建议的误报率回看；仪表盘趋势图攒真实形状；**新加一条：插话重构的体感回看**（跑任务时试试纠偏/提问/闲聊，看分类准不准、转向跟不跟得上）；**再试一条：日常任务开 THINK 路由**（设置页 → LLM → 按阶段模型路由），看省钱效果是否复现评测结论。
2. 阶段 6.4 MCP OAuth；阶段 7 Skills 签名、阶段 10 沙箱按 2026-09-17 优先级缓做。

（~~Qwen 基线格~~ —— 2026-09-19 用户拍板划掉：不开 DashScope，基线矩阵定格三 provider，Qwen 格永久留白。）

**试用时重点看**：① 设置页两个主题都过一眼（本次 CSS 大改：滚动条处处可见可滚、长列表卡内滚、按钮文字清晰、左右不跳）② 设置页 →「进化」→「评测基线」应显示三行 v5 数字与套件标签 ③ 反思器 / 纠正草稿产出质量（`reflect:` 条目、low 不注入）④ 发行版 CLI 语义记忆检索（q8 MiniLM 首用 ~23MB 下载，热批 ~12ms）⑤ 旧路线图遗留的关窗策略/系统通知/并行会话体验项（见下文打包试用节）。

## 📐 方案定稿（11.1 / 11.2 已实施）

### 阶段 11：输入纠错（错别字容错）——方案定稿（2026-09-20）

**起因**（用户体感）：GUI 里用拼音输入法敲提示词，错别字会改变整句话的意思，希望直接修正用户的输入。

**根因：不是"错字"，是"静默猜错"**——错字只是触发器，真正把它放大成事故的是三处：

| # | 放大器 | 证据 | 后果 |
|---|---|---|---|
| 1 | 安全门被错字绕过 | `assessIntent` 的高危判定是字面正则（`src/coding-agent/Planner.ts:704`：`删除\|移除\|清理\|销毁\|rm -rf…`），`preflight` 只认 `riskLevel==='high'`（`src/ui/preflight.ts:25`） | "把缓存目录**闪除**" → 判 low → 破坏性发送**不弹确认**（最贵的坑） |
| 2 | 目标定位错人 | 路径/符号名错一个字符，模型不报错，会挑一个最像的文件动手改 | 静默改错文件，比不改更糟 |
| 3 | 模型侧只有软容错 | `TYPO_TOLERANCE_PROMPT`（`src/shared/promptLayers.ts:126`）让它"自己猜 + 说明假设" | 猜错无人拦，用户也不读那句假设 |

**五条铁律**：① 语义文本永不静默改写（否定词 / 数量数字 / 单位 / 路径不在自动改写白名单）；② 自动改写只有两种资格——无损归一化，或有唯一确定性证据（workspace 内真实存在 + 候选唯一过阈值）；③ 风险判定取 `max(原文, 纠错后)`（fail-safe：宁可多弹一次确认，不可放过一个破坏性操作）；④ 可见 + 可撤销；⑤ 发送路径纯本地、零模型调用（`src/ui/preflight.ts:5-10` 记着"LLM 预分析从未稳定成功"的教训），不引新依赖、不建大词表。

**分层与接入点**：

```
main.ts sendMessage (src/ui/main.ts:1472)
  normalizeDraft  →  修复标识符  →  checkPreflight(max(原文, 修复后))  →  expandMcpPromptCommand  →  doSend(修复后, displayOverride=原文)
```

- **L0 无损归一化**（自动、无感）：不可见/零宽字符、Unicode 空格、全角 ASCII → 半角（全角块里的 CJK 惯用标点 `，。！？：；（）` 走 keep-list 原样保留）。**刻意不做折叠连续空格**：会破坏用户粘贴的代码缩进。
- **L1 标识符纠错**（自动 + 可见 + 可撤销）：三重与门——形如路径/文件名、workspace 内不存在、相似候选唯一且过阈值。索引走现成 IPC `glob_files`（`src-tauri/lib.rs:1880`）+ `buildContextCandidates()`（`src/ui/inlineAutocomplete.ts:71`）。绝不跨目录猜、绝不臆造目录。
- **L2 歧义提示**（只提示，不改）：composer 下方 inline hint"可能是 X / Y"，Tab 采纳、Esc 忽略、直接回车按原文发送；候选同时作为附注随消息给模型——澄清发生在模型侧，而不是篡改用户的话。
- **L3 风险词表**（只用于风险判定，不改字）：硬编码小词表，破坏性/迁移/重构词的错字变体 → 规范词，在 `assessIntent` 之前做一次"风险别名展开"。零误伤、不碰用户原文。
- **文件**：新增 `src/ui/inputRepair.ts`（纯函数、无 DOM，照 `preflight.ts` 的写法）+ `src/ui/__tests__/inputRepair.test.ts`；文案走 `src/shared/i18n.ts`。
- **跨层影响只有一处**：`doSend(text, displayOverride)` 是现成通道（原本给 `/mcp-prompt` 展开用），但"原文对照 + 可撤销"还要把 `repairs: {from,to}[]` 带进消息结构（`src/ui/chat.ts` + `src/ui/store.ts`）——落在 11.2，11.1 完全不碰。

**决策记录（2026-09-20 用户拍板）**：L1 取"**自动替换 + 可撤销**"。理由：只提示会被直接回车无视，等于没做；自动的前提是与门全绿，任一条不满足即降级为只提示。

**分期**：11.1 P0（0.5 天，先上）→ 11.2 P1（1–2 天）→ 11.3 P2（1 天）→ 11.4 P3（2 天，可选，复用 E3.1 草稿机制不新建存储）。

**落地记录（2026-09-20）**：11.1 + 11.2 已实现——`inputRepair.ts`（L0 归一化 + L3 风险别名表，别名只喂风险判定不碰用户原文）、`checkPreflight` 对原文与展开文本双读取取高档、`assessIntent` 底线扩词（清空/删了/覆盖/重置，负向断言保住清空输入框、重置密码、覆盖率这些良性用法）、`pathIndex.ts`（`glob_files` 后台索引 + 形如路径 ∧ 工作区不存在 ∧ 唯一近距命中三重与门）、`pathRepairNote.ts`（气泡下注记 + 「用我的原话」一键回填）；transcript 快照携带 `pathRepairs`，会话重放还原注记。附带修了输入法组字期间补全弹窗抢 Enter 的老毛病。11.3 / 11.4 未动。

**待验证 / 风险**：① 阈值调校——编辑距离太松会把用户**真实的新文件名**"纠正"成旧文件，所以"workspace 内不存在"这条与门不能省；② 不做 LLM 改写（延迟 + 失败模式 + 上述教训），纯本地信号不足时降级为提示；③ 归一化对粘贴代码的保护（见上，不折叠空格）；④ 现有 `src/__tests__/preflight.test.ts` 是回归锚，改动必须全绿。

---

## 📐 方案定稿（未实施）：阶段 12 多子 Agent 暂停/续跑（2026-09-20）

**起因**（用户体感）：多个子 agent 并行跑着时点暂停，现在等价于硬停——子 agent 被杀、
进度作废。期望：暂停停得住，继续接得上；暂停后去做别的事了，这些子 agent 不用恢复。

**方案一句话**：**暂停 = 存档，不是冻结**。手头工具安全收尾 → 全部落盘存档 → 干净停下；
点继续从存档原地接着跑；没人点继续，存档绝不自动复活。不做"真冻结"（工具没法冻、
全内存抗不住崩溃、闸门改动面大），而存档方案的关键零件已全有：稳定 sessionId
（同任务再委派即命中同一存档）、`subagent_interrupted` checkpoint、`engine.continue`
续跑入口——核心增量只是把"停止"分出"暂停"变体 + 「继续」按钮 + 单飞保护。

**完整设计**：`docs/subagent-pause-resume-design.md`（机制、边界情况表、三期分期、
一期验收口径五条）。一期改动面：中断 reason 传递、SubagentActivity 增
`paused`/`resumed` 事件、`chat.ts` 续跑入口；二期把 `subagentStore` 换文件 store 抗重启。

---

## 📐 方案定稿（未实施）：阶段 13 能力自扩展（2026-09-20）

**起因**（用户提问）：系统能否自己生成新工具、改进存量工具、生成新子 agent、改进
存量子 agent。审计结论：生成新工具 ❌（工具面编译期写死，唯一动态通路 MCP 挂的是
外部的）、改进存量工具 ⚠️ 半个（E1.3 只进化"怎么用"）、生成新子 agent ❌（名册
硬编码，E1.4 只建议）、改进存量子 agent ⚠️ 半个（行为随记忆漂移，prompt 本体无人改）。

**方案立场**：度量准入（生成便宜、准入严格——没有评测/使用数据支撑不转正）、
用户可见可控（一切落 `~/.pure/` 各自目录，删文件即回滚，总开关）、不新建安全面
（执行复用 execute_command 信任模型，权限按操作自动打标，与手写工具同一套门）、
用户触发优先（自动识别只出"建议生成"，不自己动手）。

**四期**：13.1 建议一键应用（E1.3/E1.4 收尾，零风险，顺手把建议层接进度量闭环）→
13.2 子 agent 生成（试用制准入：`~/.pure/subagents/<name>.json` 启动扫描注册，
5–10 例真实委派 ≥ 基线才转正）→ 13.3 存量角色 prompt 自改进（base + overlay 两层，
角色回归集 A/B 不输才落盘）→ 13.4 工具生成（脚本工具包 `~/.pure/tools/<name>/`，
试跑通过才注册，procedure 复用 ≥2 次才提议固化）。

**完整设计**：`docs/capability-self-extension-design.md`（现状审计表、存量接缝、
边界情况七条、实施顺序与理由、验收口径五条）。

---

## 📐 方案定稿（未实施）：北极星架构——多 Agent × 自进化智能体（2026-09-20）

**起因**（用户定调）：pure 的定位修正为 **多 agent + 自进化智能体**（不再是单纯
"多 agent 应用智能体"），并要求整体设计优化时守住一条约束：**后续修改和迭代不能
越改越麻烦**。

**核心洞察**：多 agent 是空间维（此时此刻一支专家团队并行干活），自进化是时间维
（这支团队随使用变聪明）——同一循环的两个维度，不是两条功能线。接缝是：运行留痕
（观测层）→ 归因生成改进物（进化层）→ 声明式落盘 `~/.pure/` → 启动扫描走既有
register 接缝注册回运行时（产物层）。多 agent 顺手解决自进化最缺的样本量（并行
派发天然是 A/B，E4.1 结局数据即对比实验），自进化回答"团队从哪来"（13.2 角色生成、
13.3 prompt 自改进、13.4 工具固化）。

**三条防缠原则**（"不越改越麻烦"的机制化答案）：
① **进化产物只有一种形态**——声明式文件 + 版本化 manifest，加载只有一条通路
（启动扫描 → 按类型分发到既有注册接缝）；新增一种可进化物 = 新增一个 manifest
类型映射，不新增加载路径。② **观测单向**——运行时只写观测、进化层只读，进化
一切动作异步 advisory，运行时不知道进化层的存在。③ **事件即接口**——子系统间
用事件流说话（EngineEvent / SubagentActivity 先例），新增协作能力 = 新增事件
类型；暂停/续跑、插话进子 agent、接力流水线全部落在这条上。

**分期路线**（取代此前"多 agent 转向"粗排）：一、阶段 12 暂停/续跑（P0 开工位）
→ 二、插话通道进子 agent（小，事件原则示范作）→ 三、阶段 13.2 角色生成 →
四、6.4 MCP OAuth → 五、接力流水线（待设计）→ 六、13.3 overlay → 13.1 →
团队可观测（待设计）→ 13.4。每步独立提交可回滚，间隔跑 v5 基线防进化伤回归。

**反面清单**：不做 agent 间自由对话网（收敛为声明式接力 + 父汇总）、不做运行中
热替换产物（下轮生效）、不做无度量的自动准入、不私拉跨子系统直调、不建 wasm
计算库（2026-09-20 评估：最重计算已在 ort-web wasm 上，无残留热点）。

**架构验收口径**：加一种可进化产物不碰运行时主循环；加一条进化回路运行时零改动；
v5 基线不倒退、关掉进化总开关后行为与纯运行时一致。

**完整设计**：`docs/multi-agent-self-evolving-architecture.md`（四层循环图、
产物×接缝映射表、蓝图落点清点、分期路线、反面清单、验收口径）。

---

## ✅ MVP 三步落地（2026-09-20）：暂停/续跑 + 外部角色装载 + 建议一键生成草稿

用户指令：「给一个 MVP 最小实现步骤的计划，按照计划推进，保证每一步不要阻碍后续的推进」。三步各自独立提交，互不阻塞，全部走既有接缝（北极星防缠原则一：产物唯一形态 + 唯一装载路径）。

| 步骤 | 内容 | 提交 |
|---|---|---|
| **S1 阶段 12 一期：暂停/续跑** | 暂停 = `AbortSignal.reason('pure:pause')` 走既有 abort 链，零新信号通道：引擎 THINK 边界 yield `Interrupted{reason:'paused'}`；协调器不把暂停转发给工具（在跑的干净收尾、排队的合成配对结果跳过）；orchestrator 识别暂停 → 存档 checkpoint + 状态 `paused`（不是 cancelled）。UI：Esc/⏹ 均为可续暂停，「继续」条走普通 send → 父任务重派同参子任务 → 稳定 sessionId 命中 checkpoint 续跑。老 webview 无 reason 自动退化为今天的硬停。修复超时误报：工具击杀与 deadline 竞速拆成双控制器，超时仍报 `timed out` | `bd5dd92` |
| **S2 13.2 装载半边：外部角色** | `~/.pure/subagents/*.json` 声明式角色 manifest（version 字段），启动扫描 → 编译 → 走既有 `config.subagents`/orchestrator 注册接缝，零新接线。唯一纯编译器 `externalSubagents.ts`：内建名保护（外部永不遮蔽核心角色）、重名先到先得、坏文件隔离（一坏不拖垮整目录）、`{token}` 模板替换、tag 白名单（`write` 主动选择进串行池）、超时钳制。GUI 走新 Rust 命令 `list_external_subagents`（Rust 只做 IO，镜像 `list_app_skills`），CLI node:fs 同一编译器。增删文件重启生效 | `7692991` |
| **S3 13.2 生成半边 MVP：建议一键生成草稿** | E1.4 建议卡上无技能开关的角色新增「生成收窄版角色草稿」：按失败画像确定性生成 `<role>_focused.json` 草稿（超时型教"拆小"+满预算；失败型教"带证据"）。三道门保留：草稿必须过 S2 同一校验器才许落盘、落盘前确认弹窗、同名文件绝不覆盖。完整版（模型起草 + 试用制准入）在此接缝上加厚 | `4a137e3` |

| **第四步 插话通道进子 agent（北极星第二步）** | 父任务被委派工具占住时，插话在队列里干等到委派结束——纠偏到得太晚。宿主的 steer 队列同时挂上子代理引擎 ctx：干活中的子代理在自己的 THINK 边界取走插话（drain 语义防重复，并行委派时先到先得、一条话一个收件人），随子代理 transcript 存档；`SteerInjected` → lifecycle `steered` 回执 → 活动卡 `📨 收到你的插话`。引擎零改动（复用既有 THINK 注入） | `1cc5b1d` |

---

## ⏸ 暂停点（2026-09-20）：MVP 三步 + 北极星第二步收官

- **已落地**：阶段 12 一期（暂停/续跑，进程内、重启即失）、13.2 装载半边（外部角色 JSON）、13.2 生成半边 MVP（建议卡一键草稿）、插话通道进子 agent。全量测试 2399 pass / 0 fail，typecheck 干净，cargo check 干净。
- **同日定调（用户）**：pure 的文件/命令能力不得只限应用空间 + 用户空间——绝对路径全盘可达是两层适配器（Rust path_policy / NodeToolAdapter）注释里写明的既有契约，后续开发不得重新加围栏；产物归 `~/.pure` 不受影响。
- **未实施**（按北极星六步分期顺延）：12 二期（文件态存档，重启可续）与三期；13.2 完整版（模型起草 + 试用制准入 + 归档）；MCP OAuth（6.4）；接力流水线；13.3 persona overlay / 13.1 角色级回归 / 可观测 / 13.4 工具生成。
- **打包提醒**：S2/S3 涉及新 Tauri 命令，试用前需 `bun run gui:build` 重新打包；纯 CLI 侧无需重打包即可用外部角色。
- **试用路径**：① 对话中途按 Esc → 出「继续」条 → 点击从断点接着跑；② 手写 `~/.pure/subagents/hello.json`（照 subagentDraft.test.ts 里的 schema）→ 重启 → 模型可委派；③ 跑出 E1.4 建议卡 → 点「生成收窄版角色草稿」→ 确认 → 改草稿 → 重启生效；④ 派一个长委派，跑的过程中插一句纠偏（如"顺便把 X 也改了"）→ 干活的子 agent 卡片应出 `📨 收到你的插话`，且父任务不会在委派结束后重复执行这句话。

---

## ✅ 稳定性打磨 + 子 Agent 辨识度收拾（2026-09-20 深夜 – 09-21）

用户实测 nemotron 一批派三路研究暴露的稳定性与 UI 问题：四个提交 + 一次结案 + 一次 UI 打磨。

| 项 | 内容 | 提交 |
|---|---|---|
| 多主题强制 fan-out | MULTI_AGENT_PROTOCOL + researcher 描述加硬规则：N 个独立子主题必须在同一批里发 N 个委派（禁止一个调用包办全部主题） | `0e11409` |
| 暂停即时反馈 | 点暂停立刻回执置灰、drain 后台收尾；修复第二次暂停无响应 | `45ab3a3` |
| 卡死 provider 快速失败 | 子代理 first-token 90s 上限（父任务保留 5 分钟），静默 30s 起 ⏳ waiting 心跳（「在等模型响应（可能是限流排队）——卡住会自动重试」），救不活就快失败进既有 failurePolicy 重试。根因：免费档并发排队把整批拖成无声灰卡 | `d22e97b` |
| Agent 运行 ID | 每次委派生成 `ag-xxxxxxxx`，随事件/结果/存档全程透传；浮动卡名字行挂角标、报错行前缀 `[ag-xxx]`，报错时可直接引用定位 | `8ed69e7` |
| 「5 卡 2 灰」结案 | 全量会话快照扫描：无任何孤儿 tool_call；新构建两轮（22:25 / 22:34）存档干净——3 委派、3 成功、带 ID、turns=2、发起 9 秒内三路齐启动。灰卡 = 修复前旧构建死轮在窗口 DOM 里的残骸，不在任何数据里，新开会话即净 | 结案，无代码 |
| UI 打磨（用户点名） | ① 会话窗子 Agent 卡片不再挂 ID 角标（ID 只留浮动卡 + 报错行）；② 浮动卡去掉（1）（2）（3）实例编号（instanceNo 保留为调度序数据）；③ 对话内表格去圆角（border-collapse 撞圆角会裁掉角上的边框，改直角）；④ 中断残卡自解释：被清扫的未完成卡补一行「本轮输出在此中断，该调用未执行完成」，live 清扫与历史回放两处一致 | 本次提交 |

**Next（北极星第 4 步）**：6.4 MCP OAuth——设计已定稿：TS 侧 oauth.ts（metadata 发现 / RFC 7591 动态注册 / PKCE S256 / 刷新，注入 http 便于测试）；MCPServerConfig.auth 字段；HttpTransport authProvider + 401 → MCPAuthRequiredError；token 落 `~/.pure/mcp/oauth/<server>.json`；Rust 侧 mcp_http_request 加 content_type 参数（OAuth 表单编码）+ loopback 回调命令；open_path 拉起浏览器；用户主动触发登录，不干活中途弹浏览器。

---

## ✅ 6.4 MCP OAuth 落地（2026-09-21）：HTTP MCP 服务器的完整授权链

北极星第 4 步收官，四个提交。用户在服务器配置里加 `auth` 字段即可对受 OAuth 保护的 HTTP MCP 服务器（Linear、Notion 等）一键登录。

| 提交 | 内容 |
|---|---|
| `a394274` | **OAuth 核心**（纯 TS，注入 HTTP 全测）：RFC 9728 protected-resource 发现（401 challenge → 元数据 → 授权服务器）+ RFC 8414 well-known 探测；RFC 7591 动态客户端注册（无注册端点可填静态 clientId 兜底）；PKCE S256；code 交换/refresh 走 RFC 6749 表单契约；发现 resource 元数据时带 RFC 8707 `resource` 参数。存储形态 `{tokens, client}`——client 身份随 token 持久化，重启后 refresh 才拿得到 client_id；refresh 失败清存储让 UI 重新登录；`completeLogin` 校验失败不烧流程（乱入的回调不影响真回调） |
| `f9a41fc` | **Rust 侧**：`mcp_http_request` 加 `content_type` 参数（OAuth 表单体），envelope 模式下非 2xx 也回信封并带上 `www-authenticate` / `content-type` 头（登录流要看 401 的 challenge）；新增 loopback 回调接收器（start/wait/cancel：127.0.0.1 绑端口、接住浏览器唯一一次 redirect、给浏览器回「请回到 pure」页）+ token 存储（`~/.pure/mcp/oauth/<server>.json`，0600，同 secrets.json 契约，token 不进 WebView 存储） |
| `c2eecd1` | **传输层接线**：HttpTransport 挂 authProvider，fetch / legacy /message / Rust 中继三条路都带 Bearer 头，401 统一收成类型化 `MCPAuthRequiredError`（带 challenge）；Rust 中继路径改按信封 status 分支（替代错误字符串正则，404/405 legacy 探测随之迁移）；MCPClient 按 `auth` 配置建 McpOAuthSession，401 置 `needsAuth`、重连成功清除，`onAuthRequired` 回调供设置卡片 |
| `bf5c4f5` | **设置界面 + 登录流**：带 `auth` 的 HTTP 服务器卡出 OAuth 行——登录态徽章（读 token store）、「OAuth 登录」按钮（loopback → 发现/注册 → 浏览器 open_path → 回调 → 交换 → 0600 落盘）、进行中阶段文案、登出；probe 的 401 显示「服务器要求 OAuth 登录」而非笼统连接失败；登录流全注入可测（fake invoke + fake OAuth 服务器）。**登录仅用户触发**：唯一入口是设置按钮，绝不在干活中途弹浏览器 |

测试：OAuth 核心 15 例 + HttpTransport 401 两例 + 登录流 6 例；全套 2429 过 / 0 挂。

**Next（北极星第 5 步）**：接力流水线（pipeline 模式：上游 Agent 产出直接作为下游输入串行推进）。

---

## ✅ 北极星第 5 步接力流水线落地（2026-09-21）：声明式接力 + orchestrator 交接

「接力流水线是委派参数的声明式扩展，不是 agent 间私开 socket」——按架构文档原则三原样落地。串行依赖链（研究 → 规划 → 实现）不再逐跳经过主会话中转，三个提交：

| 提交 | 内容 |
|---|---|
| `a18a489` | **接力图 + 编排接线**：新模块 `engine/relayPipeline.ts`（纯函数：声明解析 / 校验 / Kahn 拓扑分层 / 参数替换 / 收据文本）；`ToolExecutionCoordinator.executeStream` 升级为接力编排器——原并发池原样抽成 `runPool`，无 relay 的批次行为一字不变；有 relay 的批次按拓扑分层推进，同层保持「读并发 / 写串行」语义 |
| `d29a596` | 测试夹具修正（三元查表 undefined） |
| `8d1fe0e` | **模型侧教学**：委派工具 schema 出口注入保留参数 `relay`（`getSubagentTools` 是 CodingAgent / CLI / GUI 全部模型可见路径的单一咽喉；克隆注入，共享定义不被污染）；`MULTI_AGENT_PROTOCOL` 加 Relay rule 教学段（链路已知 upfront 用 relay 一次声明整条链，步骤结果会改变下一步走向时才回落到逐轮委派） |

**设计要点**：

- **声明形态**：调用参数里的保留字段 `relay: {as: "阶段名", from: {阶段名: 参数名}}`——`as` 把本调用产出登记为阶段，`from` 把上游阶段完整产出直接灌进本调用的指定参数槽。仅同批调用间生效（同一条 assistant 消息）。
- **交接不经父级**：上游产出（字符串结果 / SubagentResult.output / 对象序列化）在 coordinator 内替换进下游参数，父会话不当中转站。
- **观察消隐（接力真正的收益）**：上游产出被下游**全部成功**消费后，父级观察替换成一行收据（约 N 字符已直交下游 X、Y，完整内容在活动卡与存档）；**任一消费者失败则回灌完整产出**——父级永远拿得到原料做重规划。
- **失败语义**：fail-fast。上游失败 → 下游一律不执行，跳过错误带上游死因（截 200 字）；声明重名 / 未知上游 / 依赖成环（含自环）→ 配对错误结果，transcript 不悬空。
- **已知边界**：引擎连续去重看的是替换前参数（同参重发的窄窗口语义归去重契约管，不值得为它开跨层通道）；relay 作用域仅同批，跨轮回依赖本来就已在父上下文里。

测试：纯函数 14 例 + 编排 8 例（直灌/收据/失败传播/回灌/菱形分流/独立并发不受影响/校验错误）+ schema 与协议 4 例；全套 2459 过 / 0 挂，typecheck 干净。

**Next（北极星第 6 步）**：13.3 persona overlay → 13.1 角色级回归 → 团队可观测 → 13.4 工具生成。本步改了协议文本，建议动第 6 步前先间隔跑一次 v5 评测基线防进化伤回归。

---

## ✅ 命令沙箱默认关闭（config v16）+ 会话侧栏体感收拾（2026-09-21）

用户实装装项目时踩到：pure 放行了命令，沙箱却在内核层把往 `~/.agent-reach-venv` 的写入拍成 EPERM。根因是**两层闸门各管各的**：权限模式（TS 审批层，v15 起默认自动放行）管"允不允许跑"；`sandboxCommands`（Rust 侧 sandbox-exec Seatbelt，**默认开**）管"跑起来之后能碰哪"——自动放行 + 内核写围栏是自相矛盾的默认组合。按 2026-09-21 决定修正：**沙箱默认关，存量配置 v16 一次性强制复位**（该旗标从未有设置界面控件，持久化的 true 是 gatherForm 回填不是用户本意，用户在 UI 里根本关不掉）；机器机制原样保留作显式 opt-in（配置里手写 `"sandboxCommands": true`）。提交 `a9ec0ef`（含 config 默认值/迁移/chat 门改 `=== true`/TauriToolAdapter 构造默认/迁移测试升 16）。

同日侧栏体感三连（`fb95714` / `97f2aab` / `2a8258a` / `327bb8a`）：子 Agent 卡片计时去掉「已运行」前缀只留裸秒表；右下角并行卡点击切换会话后侧栏高亮不跟随的 bug 修复（`makeActive` 收口新增 `onActiveSessionChanged`，托盘/通知切换路径同享）；侧栏会话记录加 meta 行（唯一短 id 徽章可点击复制完整 id · 相对更新时间 · token/成本），按最后更新排序本就有、现在可见；卡片初版带底色边框被反馈"突兀"，`327bb8a` 去框回归融入式行样式只留 meta 行。顺手把抄了三份的 `relativeTime` 收拢进 `shared/format.ts`。UI 测试 958 过 / 0 挂。

**Next**：与上节相同（北极星第 6 步前建议先补跑 v5 基线；`~/.pure/eval-env` 的 key 就绪后随时可跑）。

---

## ✅ 13.3 persona overlay（part 1 + part 2）（2026-09-22）：装载合并 + 角色级回归 A/B 门槛

北极星第 6 步开工。13.3 拆两半都已落地：

- **part 1 装载合并**（`551f206`）：`~/.pure/personas/<role>.overlay.md` 启动扫描 → 纯编译（文件名→角色、≥8 字符、≤4000 防预算膨胀、未知角色拒、重名先到先得）→ `SubagentOrchestrator` spawn 时追加在 base persona 之后、机械性汇报说明之前；无 overlay 文件时 prompt **逐字节不变**（base 层一字不动，GUI/CLI 宿主只做 IO）。删除文件 = 下次启动回滚，运行中会话不热载。
- **part 2 角色级回归 + A/B 门槛**（`599a586`）：overlay 改的是文本产出，v5 命令验证套不上，改用**内容断言**判卷（`must`/`mustNot`，大小写空白不敏感，空产出必挂）；裁决规则=验收口径——任一侧样本 `< 5` → **DENY 不落盘**（防 2/2 撞大运），overlay 通过率 ≥ base（不输）→ **ALLOW**，更低 → REJECT。`run-role-regression.ts` 用**真实 SubagentOrchestrator.execute** 对同一批 case 跑 base/overlay 两侧（一次性临时工作区，适配器复用 eval 基线工厂，两侧同一条 provider 管线）；退出码 0/1/4 对应 allow/reject/deny，报告 JSON 含逐 case 断言明细。`evals/roles/` 现有 code_reviewer / task_planner 各 3 个**手写种子**，刻意不达阈值——门在真实派发样本（E4.1 沉淀）替换种子前保持关闭。冒烟：mock A/B 两侧逐字节同行为 → DENY 分支 ✓ 退出码 4 ✓。

**Next**：13.3 part 3（reflector 起草流：E4.1 outcome → draft overlay → 建议卡 → A/B 门槛 → 落盘，dashboard 标记哪些角色带 overlay）→ 13.1 角色级回归接真实样本 → 团队可观测 → 13.4 工具生成。（补跑 v5 基线仍等 `~/.pure/eval-env` 的 key。）

---

## 🔨 13.3 part 3 起步 + 真实样本收割基建（2026-09-22，已提交 `6ed5629`/`0b7b579`/`4f6b722`/`4722ec2`）

part 3 完整闭环仍在进行中；本轮先落地两块可独立验证的基建，外加两处文档修正：

- **真实样本收割**：新纯模块 `src/evaluation/roleSampleHarvest.ts`（从会话存档 `~/.pure/sessions/<id>/session.json` 用 toolCallId 精确配对抽 `{role,args,output}`，丢弃失败/中断/空产出，按角色+args 去重，排除 bash_executor）+ `src/evaluation/roleAssertionDraft.ts`（从角色契约+真实产出用便宜模型起草 must/mustNot，回复校验与 LessonReflector 同纪律）+ 脚本 `scripts/harvest-role-samples.ts`（`bun run eval:harvest`，含 `--dry-run` 与 **自洽门**：断言必须让 base 侧跑通才收录）。测试 +16（纯函数）。
- **起草器核心**：`src/harness/personaOverlayReflector.ts`（失败画像 → 提示词 → 解析校验，≥8/≤`MAX_OVERLAY_CHARS`，模型回 `NONE` 表示无可起草）+ 测试 +8。落盘用的 A/B 门槛与 UI 接线未接。
- **文档修正**：`evals/roles/README.md`「真实样本来自 E4.1 委派 args」**不成立**——`ToolObservation` 不存 args 也不存产出；改为会话存档来源。设计文档补 13.3 part 3 实现机制。
- **关键发现（门槛为何还关着）**：本机去重后每个角色真实样本最多 4 条（researcher / code_reviewer），**达不到 ≥5 门槛**——A/B 门与 part 3 落盘路径在更多**不重复**的真实委派积累前保持 DENY。这是数据问题，不是实现问题。

**part 3 接线已落地（同日续）**：
- 共享 A/B 编排核心 `src/evaluation/roleRegressionRun.ts`（runCase 注入，判卷/裁决只有一份定义），`run-role-regression.ts` 改用之；测试 +5。
- 建议卡新增「起草 prompt overlay」按钮 → GUI 处理器（`settings.ts`）：便宜模型起草 → `compilePersonaOverlays` 同一校验器 → **写盘前强跑 A/B**（样本 <5 一律 DENY）→ 确认 → 写 `~/.pure/personas/<role>.overlay.md`（复用 `write_file`，同名不覆盖）。
- Rust `list_role_cases(role)`（运行时 fixtures 落在 `~/.pure/roles/<role>/`，单段安全名校验；`PURE_ROLES_DIR` 可覆盖）+ 单测。
- 仪表盘角色表加 overlay 徽章（`list_persona_overlays` + `compilePersonaOverlays`）。
- i18n zh/en 全量文案。
- **注意**：写盘路径现在真的会在 GUI 里跑 A/B，但本机样本 <5 → 点按钮会得到 DENY（设计使然，非 bug）。
- 验证：typecheck ✓ · vite build ✓ · cargo check/test ✓ · lint ✓ · 新增测试 32 项全绿。

**收割/起草代码审查 + 修复（同日续）**：用真实语料（21 个会话）独立复算验证配对与去重——25 个角色委派 **100% 配对成功**（unmatched=0），重复 toolCallId 存在但内容逐字节相同，去重 `raw 19 → 14` 与独立实现一致。按发现修三处（均已加回归测试）：
- **P1 fixtures 落点不一致（真 bug）**：`eval:harvest` 默认写 `evals/roles/`，而 GUI 门槛读 `~/.pure/roles/`（Rust `list_role_cases`）→ 永远 DENY。现 harvest 默认写运行时目录 `~/.pure/roles/<role>/`（`--cases-root` 可指回仓库）；`eval:roles` 默认先读运行时、空则回退仓库 `evals/roles/`。
- **P2 去重键非规范化**：`sampleDedupeKey` 改用递归键排序的 `stableStringify`，键序不同不再漏去重（数组顺序仍保留）。
- **P3 重复 id 语义**：harvester 本来就是 first-wins，但改写成显式规则并加注释（"首次即结果"）。
- 测试 +4（重复 toolCallId first-wins / 键序去重 / 嵌套键序 / 数组顺序不合并）。

**GUI 处理器审查 + 流程抽高（同日续）**：审 settings 处理器的 A/B 门槛与落盘路径——落盘基本正确（`write_file` 会建父目录 `lib.rs:830`；`read_file` 缺失即 Err，存在性判定成立；同名不覆盖；草稿先过校验器）。发现四处：① 样本数无上限、同步阻塞不可取消；② 单个坏 fixture 会使整批作废；③ A/B 对运行失败无重试（抖动可造成假 REJECT）；④ 写盘后不重渲染仪表盘。已修 ②④（逐文件跳过 + 写后 `renderEvolutionDashboard()`），①②③留待后续。

**编排抽高 + mock provider e2e**：把 GUI 处理器的编排抽成无 Tauri/DOM、依赖注入的 `src/ui/personaOverlayFlow.ts`（settings 只接 Tauri IO + adapter + 确认弹窗）；新增 `scripts/e2e-overlay-flow.ts`（`bun run e2e:overlay-flow`）——用 mock provider + **真实** `SubagentOrchestrator`/`NodeToolAdapter` 驱动同一段生产代码，覆盖 DENY / REJECT / ALLOW+落盘 / exists / cancelled 共 12 项断言全绿；零网络、确定性。（浏览器 CDP e2e 驱动不了该流：处理器会 `isTauriRuntime()` 早退，所以走注入式流程 e2e。）

**Next**：13.1 待真实样本 ≥5、13.4 工具生成；团队可观测已定稿（见下节）；门槛的 N 上限/取消/重试已随 `4722ec2` 补齐；补跑 v5 基线仍等 key。

---

## 📐 方案定稿（未实施）：团队可观测——看得住这支团队（2026-09-22）

北极星第 6 步的「待设计」项定稿：`docs/team-observability-design.md`。起因是当日真实样本盘点暴露的四个数据缺口——**G1** 委派在观测里没有身份（无 ag-id、无逐委派时间戳/token，`ToolObservation` 只有匿名统计）；**G2** 观测与存档互不引用（收割配对靠角色名+args 猜）；**G3** 会话删除让历史观测变孤儿（10 个有委派的会话目录已删，30 次委派不可收割——样本量被腰斩的主因）；**G4** 成本只有父任务聚合，团队维度不存在。

- **定位**：观测层→UI 的只读读模型，产出「看」不产出「判」——不重做 E1.4 建议与 13.3 A/B 门槛；不做实时协作图、不做告警、不建第二份持久化。
- **修法**：全部是观测 schema 增量字段（parser 宽容，旧记录缺列显示「无数据」不冒充 0）——`AgentRunObservation.delegations[]`（ag-id/role/startedAt/usage 拆分/costUsd）+ 收割按 sessionId 精确配对（初版设计在此提议过 `sessionRef` 新字段，冗余已撤回——`agent_run.sessionId` 本就是存档目录名）+ 删除确认卡显性化隐性代价 + 收割「落袋为安」常驻入口。
- **分期**：T1 委派身份进观测+收割按 sessionId 配对（13.1/13.3 的样本望楼前置）→ T2 删除提示+收割入口 → T3 成本下放+聚合器+团队卡/成本卡 UI → T4（可选）relay 链路图。每步独立提交。
- 验收：加一种视图只写纯函数+渲染；v5 基线不倒退；收割对存活会话按 id 100% 配对（25/25 基线不退步）。

---

## ✅ 北极星第 6 步收官 MVP：P0/P1 落地（2026-09-22）

设计定稿 `docs/step6-closing-mvp.md`（剩余四项按「先看与攒、后生成」排定；含一条勘误：总设计里 G2 的 `sessionRef` 新字段冗余已撤回——`agent_run.sessionId` 本就是存档目录名，收割按 sessionId 配对即可）。同日 P0/P1 四笔落地：

| 提交 | 内容 |
|---|---|
| `3f5079e` | **T1 委派观测下放**：`AgentRunObservation.delegations[]`（ag-id / role / startedAt 反推 / duration / success / usage 拆分 / outputChars / errorKind），ToolResult 分支旁路写入（成功路径 result.result 就是整个 SubagentResult，宽松解析）；角色判定走注入谓词（CodingAgent 装配点把已注册角色集传给 observability，shared 层不反向依赖；无谓词=行为逐字节不变）；`SubagentResult` 加可选 `usage`（orchestrator 从子代理 Completed payload 抄，tokens×价目表需要拆分，总量配单价是瞎算）；收割 runner 加观测↔存档交叉对账（「观测到 N、可收 M、差额在已删存档」常驻可见）。测试 +5 |
| `e59e002` | **T2 删除代价显性化**：Rust 只读命令 `summarize_session_delegations`（读单份存档数角色 toolCall，返回 `{delegationCount, byRole}`）+ 删除确认卡加提示行（含 N 条委派存档、删除后无法回收，只提示不拦截）+ Rust 测试 +2 |
| `3ca03dc` | **T3 团队卡**：纯函数聚合器 `shared/teamObservability.ts`（优先 T1 delegations、退回匿名 toolCalls、缺字段=「无数据」不冒充 0）+ `renderTeamRosterSection`（阵容表：派发/成功率/平均耗时/token/样本存量 vs MIN_ROLE_CASES 门槛列 + 差距提示行）；测试 +3 |
| `8f85399` | 团队卡挂进设置页进化区（同一次观测读取切片 + `list_role_cases` 数每角色 case，浏览器模式跳过 IO 派发数据照常）+ i18n zh/en |

验证：typecheck ✓ · vite build ✓ · cargo check/test ✓（226 过）· 全量 TS 2542 过 0 挂 · lint ✓。

**未实施（按 MVP 分期顺延）**：S1 样本过门槛（人工：跑 2–3 个多 agent 会话覆盖 researcher/code_reviewer/task_planner → 收割入库 → 门首次 ALLOW → overlay 首次落盘——第 6 步完成的标志事件）；T4 成本视图（usage 拆分已随 T1 落盘，只差聚合+渲染）；13.1 建议一键应用；13.2 完整版（等 T1 数据攒两周）；13.4（维持缓做）。

---

## ✅ 13.1 建议一键应用 + 「已应用的建议」回看（2026-09-22，`5464580`）

收官 MVP 的 P2 项落地，建议层第一次接上「写」的半边——建议卡不再只是指路：

| 半边 | 内容 |
|---|---|
| 写入 | skill-gate 建议卡加「一键应用：关闭「{skill}」技能」按钮：处理器端重扫防过期 → 写 `cfg.skills[id]=false`（persist + 失效缓存）→ 落一条 `advice_applied` 观测（`buildSkillGateAppliedRecord`，evidence = 应用时刻的派发/失败快照）→ 卡片就地换「已应用」说明；工具注意的「采纳」同样补记观测（`buildToolNoteAppliedRecord`，evidence = 簇快照） |
| 回看 | 新增 `AdviceAppliedObservation` 进观测联合（解析器放行、未知类型照旧跳过）；读模型 `shared/adviceApplication.ts`：`collectAppliedAdvice`（倒序封顶 10 条）+ `postApplyStats`（只读 toolCalls，T1 之前的旧记录也回看得动；skill-gate 按角色计派发、tool-note 按工具计失败——观测里没有错误原文，分不了类，UI 如实标注「不分错误类」）；设置页新增「已应用的建议」区块：「应用前」是落盘快照、「应用后」现算，派发过就说数字、没派发过就说还没派发，不冒充实效 |
| 顺手修 | 建议卡三枚按钮（草稿/overlay/一键应用）一直渲染成无边框——CSS 引用了不存在的 `--border-color` 令牌（overlay 按钮还整段漏了样式），归队到 `--border-light`；`.llm-provider-card-protocol` 同病未治（本次范围外），下次路过再修 |

测试 +13（解析器放行/拒绝、两个构造器、读模型排序/封顶/窗口边界/时钟偏斜/前 T1 记录、渲染层按钮与回看行/转义）；子树 1934 过 0 挂，typecheck ✓，规范检查 ✓。应用半边只在 Tauri 运行时接线（观测落盘依赖 append_observation，浏览器模式无意义）。

---

## ✅ 中途纠偏止损（premise-change）+ 输入历史 ↑/↓（2026-09-22，`21775c2`/`bb77fad`）

用户同日实测暴露的两个体感缺口，各自一笔修掉：

**① 前提被推翻不止损**（`21775c2`）：规划「从广东去广西」跑到一半，用户插话「我在西安」——插话分类器把它判成 `steer`（转达不停活），回复「已转达——下个动作就带上」，但三个子 agent 都在飞，父任务在它们返回前根本没有"下个动作"，错误前提一路跑到结束。`steer` 的承诺（"下一步带上"）在并行委派下结构性失真：前提错了的在飞委派不会因为"带上"就变对。修法是给插话分类加一档 **premise-change**（前提被推翻：目标没变，但工作所依据的事实——地点/日期/环境/预算/版本——错了，按错误前提算出来的东西全都不值钱）：判定提示词里 steer 与 premise-change 的分界写成「纠正会不会改变答案本身，而不只是措辞」，premise-change 沿 goal-change 同一条 abort 链止损（阶段 12 打通的三层中止照常生效），插话暂存、收尾后作为新指令重新入场按新事实重排，状态文案改说「前提变了——按旧前提跑的活先停下止损」。正则不加快速路：前提纠正的措辞太多样（"我在西安"没有任何标志词），低精度正则会误杀。

**② 输入历史 ↑/↓ 回填**（`bb77fad`）：像 shell 一样，↑ 往前翻回之前发过的指令、↓ 翻回来，翻过最新一条回到草稿。GUI 的聊天输入框和首屏输入框各接一份翻阅状态，共享同一份磁盘历史 `~/.pure/input-history.json`；CLI REPL 启动时把同一份文件灌进 readline 自带的会话历史、退出时存回。共用内核 `shared/inputHistory.ts`（bash 口径：newest-first、相邻去重、封顶 200、坏文件当空历史）。接管边界照 shell：光标在首行按 ↑ / 末行按 ↓ 才翻历史（多行草稿里箭头照常移动光标）；autocomplete 弹层开着时箭头归它（后注册的处理器看到 `defaultPrevented` 即让路）；输入法组字时让路。浏览器模式退化为页面内有效，写盘失败静默降级。测试 +21（内核状态机/去重/容错 12 + happy-dom 控制器行为 8）。

---

## ✅ 实测回归两连修：中止类插话重复回显 + 输入历史串台/首句丢失（2026-09-22）

3.0.0 试用当日用户实测暴露的三处体感 bug，同一笔修掉：

**① 中止类插话同一句话上两遍**：前提纠正在界面上的路径是「用户消息 → 前提变了状态条 → 用户消息又出现一遍 → 开始思考」。根因是回显有两个来源——插话处理器里 `echoUserBubble()` 先上一遍，暂存的插话在 abort 收尾后经 `dispatchDeferred → send()` 重入，`send()` 开头的无条件用户气泡又上一遍。goal-change 一直有这个毛病（premise-change 沿用了它的写法所以继承）。修法：中止类（goal-change / premise-change）不再预回显，让重入的 `send()` 独家上屏——这句话本来就属于新回合，状态条负责中止期间的即时反馈；steer / question / chatter 不重入 send()，照旧当场回显。

**② 输入历史 A/B 会话串台**：历史存成了全局一份 `~/.pure/input-history.json`，所有会话共用——终端里一个用户一份 shell 历史是合理的，但 pure 的会话是并列开的多个对话，必须分桶。改成 `SessionInputHistoryStore`：按 sessionId 分桶，每会话一份文件 `~/.pure/input-history/<sessionId>.json`，切会话经 `openSession()`（唯一咽喉，新增 `onSessionIdChanged` 回调）预热新桶；CLI REPL 是自己独立的一个会话面，仍用旧全局文件，两边互不可见。

**③ 每个会话的第一句话 ↑ 翻不出来**：首条消息走 landing 首屏输入框，`remember()` 记进了 landingHistory 实例，之后 ↑ 却是 promptHistory 在响应——两个实例各记各的账。修法是仓库化：两个输入框共享同一个 store 实例，`remember` 在调用时刻解析会话身份写进正确的桶，从哪个框发出的都能翻回来。浏览器模式桶只在内存，照样按会话隔离。

测试：UI 侧输入历史重写为仓库语义 +11（会话隔离回归、双输入框共享桶、桶未就绪先让路），全套 22 绿；chat / 插话分类近邻 185 绿。

---

## ✅ scope 追加插话不再被遗忘：steer/task 边界重立（2026-09-22）

用户继续实测踩到的第三处插话缺口：调研「B站/腾讯/优酷 三个平台」跑的中途插入「再加一个 爱奇艺平台」，pure 答「已转达——手头的活不停，下个动作就带上」，调研结束后爱奇艺没人做。

根因和 premise-change 同族，是 **steer 空头承诺的第二种形态**：分类提示词里 steer 的定义写着 "a style or **scope preference**"——「再加一个平台」正落进 scope preference 被判 steer；而 steer 的兑现机制是"下一个 THINK 轮顺路带上"，父任务此刻阻塞在等三个子 agent 返回，没有"下个动作"可以带上，插话就悬空了。premise-change 修的是"前提错→在飞委派全白跑"，这次是"内容是新增工作项→根本没有消费它的下一步"。

修法只动提示词的边界，不动机器（task 排队路径本来就是闭环：`queueInterjectTask` 有"分类落定时活已收尾"的 hazard 兜底，收尾后 `dispatchDeferred → send` 作为新指令重入，带着完整会话历史，"再加一个爱奇艺"能接上下文）：
- steer 收窄为「指导当前工作怎么进行，且必须是可兑现的承诺——活快收尾或阻塞等并行委派返回时没有下一步，选 steer 就是话会被静默丢掉」；
- task 明确覆盖 **scope 追加**（"再加一个 X"、"顺便也查一下 Y"、"把 Z 也照样处理"）：哪怕像是当前活的延伸也是新增可完成工作项，一律排队——排队不是遗忘，是当前活收尾后必跑；并在两者纠结时给出硬规则「加活的量，选 task」。

用户体感变化：插入「再加一个爱奇艺」会看到「⏳ 已排队：…（当前任务完成后处理）」，三平台调研收尾后爱奇艺作为新回合开跑，结果分两段但一段不少。测试：Planner 验收 +1（scope 追加 → task 合法判定），分类近邻 186 绿。

---

## ✅ scope 追加确定性投递 + 阶段感知折入（2026-09-22 续，用户定稿语义）

上一节只改了分类提示词，用户重测：「还是没改，中间插话，被忘记了」。复盘定性：**靠劝不行**——分类器是 LLM 判断加超时兜底 steer，任何没走排队的路径都可能丢。而且排水网有个结构性盲区：引擎在 THINK 边界整队取走 steer（取走即清空），父任务阻塞等并行委派时不在任何边界上；等委派返回走到汇合轮才把话塞进 prompt，模型那轮的活是汇总、没搭理，等收尾排水时队列早被取空——**「被取走」不等于「被照办」，排水只能捞「从没被取走」的**。

两刀，全部确定性：

**① SCOPE_ADD_RE 快速路**（与 STOP_RE 同款机制）：「再加/再添/再补/再算上/顺便查/也查一下/把X也查/同样处理/也来一份/add another…」字面命中**直送排队，完全不过分类器**。保守高精度：否定前置豁免（不用加/别再加/不要再加落回 LLM）；「顺便」收窄为必须跟新工作动词——「顺便把标题也改了」是改当前活不是加活，测试抓出这个误伤后收窄的。

**② 阶段感知折入**（用户定稿的语义）：插话落在**并行委派还没收齐**时，不排队——排队会先输出一份没有它的汇总。折入汇合轮：`foldInScopeAddition` 原话照常上屏，`takeSteerMessages` 在委派返回后的第一个 THINK 边界注入强框架指令（「把追加的这项派出去做完，拿到结果后与已有产出合并输出覆盖所有对象的最终汇总，在此之前不要输出最终汇总」）。折入不靠祈祷：投递时记录 `agentActivities` 水位，收尾 `settleFoldIns` 核验投递后是否真的发起了新委派——没有（模型直奔汇总）或没投递（回合提前终止）就转 pendingTasks 按合并口径补跑。插话落在**委派收齐之后**才照旧排队（先出已有结果，再单独补跑）。

测试：协调器快速路正反用例（5 个命中 + 4 个否定/问句豁免）、chat 侧折入链路源码断言，受影响 5 套 211 全绿。

---

## ✅ 插话路由重新设计：委派在飞期间删掉「转达」这个目的地（2026-09-22 终版）

用户第三次实测：「增加一个平台  爱奇艺」——又丢了。复盘：字面快速路只有「再加」没有「增加」，漏过后 LLM 又判 steer，话进了转向通道被丢。定性升级为**设计缺陷**而非措辞漏洞：字面清单永远追不完措辞，分类器是 LLM 加超时兜底，只要还存在「把话当一句顺路带上的话交给正在跑的回合」的路径，就还会丢。用户明确要求重新设计。

**新原则（路由表层面删掉病根）**：委派在飞期间，steer 不是合法目的地——它的承诺（"下个动作带上"）结构性不可兑现，此前三次丢失全部源于此。重新设计的路由矩阵（isStreaming 期间）：

| 条件 | stop/goal/premise | question | steer | task | 分类不可用 |
|---|---|---|---|---|---|
| 委派在飞 | 中止（照旧） | 旁路回答（照旧） | **折入**（强框架+核验兜底） | **折入** | **折入**（decide(null) 兜底 task） |
| 委派收齐 | 中止（照旧） | 旁路回答（照旧） | 转向通道（真有下个动作，照旧） | 排队（照旧） | **排队**（原为 steer） |

配套改动：
- **兜底全换 task**：`classifyInsertion` 超时/解析失败/LLM 异常、协调器 `!llm` 分支、chat 侧 `!llm` 旁路（已删除，统一走 `decide(null)`）——全部从 steer 改为排队。旧理由"带上更多信息继续干"成立于没有并行委派的时代；现在确定性压倒时机。
- **SCOPE_ADD_RE 补「增加/增添」**：实测丢失的原句进快速路（直送排队不过分类器）。
- **buildInsertionContext 喂委派状态**：「并行委派：共 N 个，在飞 M 个（已收齐→任务即将汇总收尾）」——分类器判 steer/task 终于有了决定性输入（steer 需要真有"下个动作"才可兑现）。
- 折入指令/状态文案对 tweak 类插话也成立（「正在跑的活收齐后就把这项带上，最后合并出一份完整的汇总」语义通用）。

丢失的数学：现在任何 mid-run 插话的终点只可能是——中止重排（goal/premise）、旁路回答（question）、折入（在飞：核验兜底转排队）、排队（收齐/兜底）——**不存在"交给模型自觉"的路径**。测试：兜底契约 4 例改 task、`增加一个平台` 原句进快速路、steer 分流源码断言；全仓 2588 绿。

---

## ✅ 折入机械执行：汇合边界由系统直接把追加项跑完（2026-09-22）

路由重设计后第四次实测：状态文案「已收到——正在跑的调研收齐后先补这项，然后合并出一份覆盖全部的汇总。」**出现且正确**，但模型在汇合轮无视折入指令——先把已有平台汇总输出，再单独补跑爱奇艺。定性：折入指令只是"劝"，投递 ≠ 遵守（与 steer 通道"消费 ≠ 兑现"同源，第三次证明这类洞不能靠提示词堵）。收口方式：**把决定权从模型手里拿走，改成机械执行**。

**机制**：`takeSteerMessages` 异步化（`Message[] | Promise<Message[]>`，引擎 THINK 边界 `await`）。在每个折入的投递点：

1. **闸门**：`hasDelegationInFlight()` 为真直接退回原 drained——steer 队列父子共享（北极星第二步），子 agent 的 THINK 边界也会走这里；在飞的子 agent 自身条目恒为 running，天然挡住子 agent 偷折入。为假的时刻恰好=父任务汇合轮（所有委派收齐、最后一轮 THINK 之前）。
2. **机械执行**：取最后一个 `agentActivities` 条目的角色名，直接 `subagentOrchestrator.execute()` 把追加项跑完——**不委派给模型选择，系统自己派**。成功 → `mechanicallyDone = true`，把结果作为观察注入（【追加任务已由系统直接执行完毕，无需再委派】+ 结果全文 + 「与此前委派的全部产出合并，输出覆盖所有对象的最终汇总」）。
3. **失败兜底**：角色缺失/执行异常/空结果 → 退回原强框架指令注入；收尾 `settleFoldIns` 核验仍然兜底。三层防线。
4. **settle 放行**：`honored = mechanicallyDone || (delivered && 水位增长)`。

顺序现在由机制保证：追加项的调研发生在汇合轮 THINK **之前**（await 阻塞在边界上），模型拿到的是"已执行完毕的结果"，它唯一能做的就是合并汇总——想要"先汇总再补跑"也没有材料。task 类折入走机械路径，steer 类（真转向）仍走指令注入。受影响 4 套 237 绿 + 全仓 2588 绿；chat 侧源码断言补齐（异步闭包 / 在飞闸门 / 机械执行 / mechanicallyDone 放行）。

---

## ✅ 机械执行可见性：补跑不再是黑盒（2026-09-22 第二刀）

用户第四次实测：折入路由与机械执行都触发了，但「前两个并行 agent 收齐后，思考直接卡住」，140s 只看到「仍在等待模型返回首个内容」的误导提示——**补跑其实在跑，但全程不可见**。定性：机械执行绕过了父引擎的工具事件流，对话流里不会出现委派卡；思考卡的"等待模型首字"提示此时是谎言。可见性不是锦上添花，是机械执行的成立条件——用户看不见的执行等同于卡死。

三处把"正在补跑"亮出来（全部挂在 `takeSteerMessages` 的机械分支上）：
- **对话流状态条**：补跑起点一条 pending 状态条（「委派收齐。现在直接补跑你追加的这项（已派给 X）——完成后一次性出覆盖全部的汇总。」），跑完原位改写为「补跑完毕，正在合并汇总」；失败改写为「直接补跑没跑成，改由调研角色自行补派——这项不会丢」并退回指令注入。
- **思考卡诚实化**：标签换成「正在补跑追加的调研（X）…」，重置计时器并换专用等待提示（不再说"等待模型返回首个内容"）；跑完换「正在合并汇总…」。
- **活动卡照常点亮**：补跑走 orchestrator 的 onStart/onTool 事件，右侧卡片照常出现、有实时工具明细——用户要的"新的子 agent 在跑爱奇艺"就是它。

顺手修正：结果提取改为优先 `result.result.output`（子代理产出的真实位置；原先 JSON.stringify 整个 result 对象会把元数据一起喂给汇总轮）。测试：chat 源码断言补状态条/思考卡标签/计时器/output 提取；全仓 2588 绿。

---

## ✅ 补跑卡片对话流同款化（2026-09-22 第三刀）

第二版实测：状态条出来了（「委派收齐。现在直接补跑…已派给 researcher」），但**对话流里没有 researcher 的卡片**，思考卡还是「等待模型返回首个内容」。复盘两个洞：

1. **卡片只挂了右侧活动栏，对话流没有**——正常委派的卡片走父引擎的 ToolStarted 事件渲染；机械执行绕开引擎，对话流空白。
2. **思考卡标签更新依赖已置空的旧引用**——思考卡随工具批次收尾被 finalize 置 null，机械分支的 `if (thinkingCard)` 全部跳过；用户看到的"等待模型返回首个内容"是旧卡残留的误导提示。

修法（机械分支完整重写）：
- **同款卡片**：`appendToolRow(role, {prompt}, 'agent')` 与正常委派共用一套 agent 网格卡（🤖 样式），落进对话流。
- **专用事件泵**：父引擎此刻正阻塞在 takeSteerMessages 边界上，fanout 事件转发不出去（async generator 挂起时事件只能排队）——另开一个 `subagentEventFanout.subscribe()` 泵，按 callId 过滤，把 orchestrator 的活动事件实时灌进卡片 Output 面板（与正常卡同款行格式），收尾 `finalizeToolRow` 带 trace 重渲染、展示产出文本。泵在 finally 里 close，异常吞掉——UI 泵故障不得影响补跑本身。
- **思考卡接管**：引用为 null 时 `openThinkingCard()` 开新卡接管（事件循环后续把补跑后的推理流灌进这张卡，天然衔接），标签/计时器/等待提示全部换成补跑文案；跑完换「正在合并汇总…」。

测试：chat 源码断言补齐（同款卡片/泵/思考卡接管/output 提取）；全仓 2588 绿。

---

## ✅ 代执行回合：插话执行回归引擎原生管线（2026-09-22 定稿，覆盖前三刀）

第三次实测仍复现——补跑卡片没出来。到此**五连败**，性质变了：不是哪个补丁有洞，而是整条思路错了。复盘出结构性教训：

**根因**：插话执行一直在走旁路。steer 队列在 THINK 边界排空、机械执行在**被挂起的生成器里**直接调 orchestrator——async generator 挂起时 yield 的事件全部积压、不往外流，所以 orchestrator 的进度事件永远到不了 chat 的事件循环；后面每刀（状态条、合成卡片、事件泵、思考卡接管）都是在给一条"死"管线外挂 UI，每个补丁都自带一个"以为 A 实际 B"的洞。旁路上叠补丁，永远叠不完。

**定稿设计（用户拍板"不要怕改的多"）——代执行回合**：插话的执行不许走旁路，必须变成引擎的一个原生回合。引擎在每个 THINK 边界（takeSteerMessages 之后、模型调用之前）咨询新接缝 `ctx.takeSyntheticToolCalls()`；宿主给出非空 toolCalls 时，**引擎本轮完全跳过模型调用**，把这些调用交给下面原生的 ACT 管线：assistant(toolCalls) 入档 → ToolStarted 出卡 → runBatch 真实派发 → SubagentActivity 流明细 → ToolResult 收尾入档 → OBSERVE → 下一轮 THINK 拿着结果做汇总。卡片/明细/回放/持久化全部与正常委派同源，一行手搓 UI 都不剩；**顺序由结构保证**——追加结果落地前模型根本不会被咨询，"先汇总后补跑"没有发生的材料。

落地清单：
- `shared/types.ts`：`EngineContext.takeSyntheticToolCalls?` 接缝。
- `AgentLoopEngine.ts`：THINK 边界咨询；`syntheticRound` 为真时 `toolCalls = syntheticCalls`，LLM 流块整段包进 `if (!syntheticRound)`，下游管线零改动。
- `Harness.ts` / `CodingAgent.ts`：配置透传；只接父引擎，子代理引擎不传（折入交付只属于父任务汇合轮，闸门在宿主闭包）。
- `chat.ts`：`takeSteerMessages` 只剩指令型折入交付 + 代执行回合的合并口径铺垫（`mergeFramed` 每条只铺一次）；新 `takeSyntheticToolCalls` 闭包：闸门（hasDelegationInFlight）→ 把每条机械折入包成普通委派调用（`foldin_*` callId，role 取最后一条活动）→ 恰好一行因果状态「你追加的安排上了——派 ${role} 单独补跑…」；ToolResult 事件里 `foldin_*` 成功 ⇒ `mechanicallyDone`，settle 语义不变。**删除**：合成卡片、专用事件泵、思考卡接管、output 手工提取——前三刀的全部手搓 UI。
- 引擎测试两个（跳模走原生管线 + 空返回正常咨询模型）；chat 源码断言重写为"新闭包存在 + 旧补丁必须不存在"；全仓 2590 绿。
- **构建戳**：`vite.config.ts` define 注入 `__BUILD_INFO__`（git 短哈希 · 构建时间），设置 → Updates 版本行显示——Tauri 不热更，装机后先看戳再测，别再拿旧二进制验证新代码。

用户应看到的体验：两张并行调研卡在跑 → 插话收到回执 → 前两张收齐后出现一行「你追加的安排上了」+ 第三张 researcher 卡（原生同款，流明细）→ 最后**一份**覆盖全部对象的汇总。

---

## ✅ 代执行回合实测回执 + 任务书上下文修复（2026-09-22 晚，f23517b 包）

22:13 实测 transcript 证明**机制全线跑通**：合并口径铺垫入档（msg7）→ 代执行回合跳模、`foldin_*` 委派出卡（msg8-9）→ 模型拿到三路产出后自己核验、补漏、统一汇总（msg18）。用户观感仍是"没实现"，根因两个，一个当场修掉：

1. **合成任务书是废的（本刀修复）**：`fold.text` 是用户原话速记（"新增一个平台，爱奇艺"），原样塞给无上下文的子代理——它把「爱奇艺」跑成了**爱奇艺号开放平台 API 文档**（Bearer Token、npm 包、上传限额）；父引擎正确判废（"这份内容我不会混进汇总里"）并自己搜索补齐爱奇艺信息。流程走了形：卡出了、活干了、干的是废活。修复：任务书带主任务原文（send 的 `userText` 闭包捕获）+ 约束"只补追加项、口径与兄弟调研对齐、不重复已覆盖对象"。
2. **researcher_web 取页后端当晚抽风（另一条线，未修）**：腾讯 researcher 连续 5 次 "No usable research sources"（r.jina.ai 取页失败，research 工具零可用来源即报错，failure policy 熔断）；父引擎自己的 r.jina.ai 抓取同样失败，靠搜索兜住。并行委派时取页代理可用性直接决定调研质量——取页重试/备用源/降级链路缓做待排。

---

## ✅ 输入路由统一 + 体感性能批次收官（2026-09-23，7 提交）

折入线收官后转了一个批次，两个主题。**主题一：输入路由的可仲裁化。** 此前"这条输入是什么意思"在六处各自推导——同步 Planner 判定、`shouldBypassSemanticRoute`/`isPlainConversational`、隐藏语义路由、`compileRequestWorkflow`、中途插话分类器、计划续跑启发式——个别合理、整体无仲裁：谁拍的板没有记录，两层打架没人管。两起实测事故（modify 请求被规则层读成 plain question 以致路由根本没跑；scope 追加被判 steer 但承诺兑现不了、原话被静默丢弃）都出自这里。收法不是再叠规则，而是**先给所有决策一个共享形状 + 共享置信门 + 可重放日志，再把生产者逐个汇上去**。**主题二：长对话体感三刀**——转录折叠两套实现两套阈值、首 token 前的串行暗等、chatty 命令卡撑爆合成器。

配套改动（按提交）：

- **共享词汇表（`0c9f680`）**：新 `inputDecision.ts`——`InputDecision` 共享形状（action 七种 + scope + timing）、置信门（低于 0.6 一律 clarify：拿不准的破坏性判定宁可问一句也不猜）、`clampConfidence`（缺字段默认 0.7）、决策日志可重放（回归测试重放真实决策而不是断言散文）。222 行契约测试。
- **单生产者汇合（`475a012`）**：新 `turnRoute.ts` 把五条路由推导（旁路判定 / 同步地板 / 语义路由 / workflow 编译 / Planner 关键词）汇到一个生产者，谁决定、为什么，日志说得清；插话协调器同签名汇入同一形状；cliRepl / chat 两侧接入。242 行测试。
- **C4 转录历史统一折叠模型（`2ed29eb`）**：新 `transcriptHistory.ts` 单实现，live 会话与磁盘恢复两条生产路径共用——原先各一套折叠实现、各一套阈值、各一套视觉语言，重开对话"丢历史"、同一会话折叠样式随入口漂移；阈值（10 组 / 8 平铺 / 50 起折叠）一处定义；main.ts 恢复路径换轨、styles.css 组样式统一；`scripts/verify-plan-restore.ts` 真浏览器验证（种子→重载→侧栏点选→计划卡投影全走真实路径）。该提交同时带上了 C6 的 main.ts 宿主门与 C7 的 containment 样式——提交切分与原清单有交叉，内容无缺。
- **C5 首 token 延迟（`91f98b4`）**：MCP 握手预热——agent 建好即 kick `connectAll()` 与本地 preflight 重叠，首回合只 await 剩余部分（封顶 600ms；原先冷 stdio 服务器的 spawn+initialize 串行挡在首 token 前）；MCP 资源首回合封顶 800ms（原先干等 3s prefetch 窗口，而资源本就是低优先级上下文、下一回合自然落地）；AGENTS.md 三次 IPC 读从**每回合执行**改 30s TTL 缓存。chat.test 源码断言：预热点位在 preflight 之前、封顶常量在位、旧的 1.5s 串行等待绝迹。
- **C6 输入级排期语义（`1a49336`）**：「10 分钟后把标题改成 X」「下午三点再跑一遍测试」不再现在执行也不丢——决策层判 `timing.mode='at'` 后经 `setTimedInputSink` 交宿主，main.ts `scheduleTimedInput` 落 `taskQueue.enqueue(text, { dueAt })`（dueAt 持久化、跨 reload 自己重挂定时器，定时这件事只有队列做得到所以只有它接管）；回显引用用户原话的时间措辞（只给解析后的"15:00"像一份用户没要过的排期）；排期以 host-schedule 源进同一份决策日志。105 行 taskQueue 测试。
- **C7 工具卡片滚动 containment（`21bd268`）**：chatty 命令卡最多渲染 500 行，`contain: layout paint`（刻意不含 style，container query 会废）圈住重排重绘范围，长转录滚动不再掉帧；toolRow.test 断言在位。
- **协调器契约补交（`597a953`，终验时发现）**：+90 行 DynamicInsertionCoordinator 测试——低置信 goal-change 转 clarify 不中止、高置信破坏性放行（门是给怀疑的、不是给保守的）、分类器缺 confidence 字段走默认值不误伤、定时插话无视判型一律排队、timing 原样透传。上一会话写完漏提交，本日补交。

**终验（2026-09-23）**：git status 干净；typecheck 无错；全仓 **2643 pass / 4 skip（既有跳过项）/ 0 fail**（184 文件）——较折入线收官时的 2590 增加 53 个，即本批次新测试。

---

## ✅ 对话流收拾：宿主那半边像同事一样给你回话（2026-09-23 下午，`dbf6a0a`，样本驱动）

用户给了六类理想对话样本（问答 / 规划 / 多子 Agent + 插话三型：打断修正、无关排队、模糊澄清），要求对照体检 GUI 层的对话显示逻辑，该改的坚决改。体检结论：路由机制（C 轨道）是对的，但宿主写进对话流的"回话"有四类硬伤——**分类延迟的死空气、置信门在分发层被无视、排队没有可见性、排期到点没有衔接**。全部收掉：

- **插话即时回执**：判定是秒级 LLM 往返，此前用户的话要等分类落地才有反应——死空气。现在插话瞬间一条 pending ack（「收到——看一下这句话怎么安排…」），最终回执**原行定格**（`settleAck`），transcript 不为一条插话出两行系统话；判定失败 ack 也有收场文案，不留永远在闪的孤儿行。
- **置信门兑现（补洞）**：门把没把握的破坏性判定降级 `clarify` + `shouldAbort=false`，但分发只看 kind——低置信 goal-change 照样拆任务，"问而不赌"形同虚设。现在 kind switch 之前先兑现门：gatedFrom 是停/重开 → 旁路问一句（LLM 组织问法，失败退模板；说出自己的读法 + 请确认或纠正 + 手头活照跑），用户的回答作为新插话重新分类。不破坏的误判自己能愈（排队晚点跑、旁答只答一次），照旧分发，不拿问题烦人。
- **待办队列活卡**：每排一件发一行系统话是流水账。`renderQueueCard` 一张卡就地重渲染（标题 N 件 + 有序列表），排空撤卡；新会话清 pendingTasks 同步撤卡；有在飞回合时挪进当前回合（计划卡 `moveNodeToTurn` 同款），不落在已归档块里变成没人看得见的死卡。收尾派发前先叙述交接（「手头的活收尾了——现在处理刚才排下的那件」），用户的话不再凭空开跑。
- **排期交接**：main.ts 回执改「排期好了：X 自动开始「…」——到点自己跑，不用你盯着」；taskQueue 到点开跑时经新 `QueueChat.notifyStatus?` 先说「⏰ 之前排期的活到点了，现在开始：…」再 send——定时任务开跑时用户一个字没打，没这句衔接像闹鬼。
- **回执文案全量对齐样本骨架（认下 → 定性 → 影响面 → 下一步）**：停（「收到，停——正在收尾，已经跑完的部分都留着」）、方向变了（在跑的止损 + 已完成的不丢 + 按新的来）、前提变了（旧前提跑下去只会白跑 + 按纠正后的事实重来）、折入（不再点名叫"调研"——追加的活可能是任何一种）、续跑（去掉"不重新规划"这种内部推理外露）。

测试：chat 源码断言 +3 块（ack 各路径收场全覆盖 / 门兑现 + 问询兜底 / 队列卡 + 交接叙述 + 折入不点名）、taskQueue 行为测试 +1（定时到点宣告、非定时静默）；全仓 **2647 pass / 0 fail**。样本里的"变化点清单 / 暂定假设 + 澄清清单"属于模型回合的输出姿态，宿主侧已用问询 + 交接铺垫把舞台搭好，模型侧表达不在本刀范围。

---

## ✅ 样本回放：samples.txt 六类案例在宿主侧逐条跑通（2026-09-23 傍晚）

用户把样本当测试案例，要求验证修改后的对话流真能按剧本跑通。做法不是字符串断言，是**剧本回放**：新 `conversationSamples.test.ts` 自带极小假 DOM + 假分类 LLM（按样本剧本给判定），但协调器、置信门、时刻解析、快路径正则、分发 switch、DOM 回执、活队列卡全是真代码——`interject()` 进、派发载荷出，逐条对照样本。

七条回放全绿：无关排队型（天气/机票进一张活卡就地重渲染、买牛奶带「今晚 19:30」走宿主排期不占队列、收尾两段交接叙述逐件派发后撤卡）、打断修正型（方向变了/前提变了回执 + 收尾后插话作为新回合开场重入、不预回显）、模糊澄清型（置信 0.35 的 goal-change 被门拦下，旁路问询按自己的读法问一句，手头的活照跑不 abort）、主线追问（边干边答主线不动）、steer 转达（收尾兜底不丢）、停（正则直判零 LLM）、委派在飞加活（快路径折入汇合轮不占队列）。顺带核过 timing 解析：样本句「再口语一点」不触发时钟分支（`点` 后必须跟数字/半）。

**回放抓出一个真缺陷并修掉**：stop / 方向变了 / 前提变了三张回执 `keepPending` 挂着微光，但 abort 收尾、插话重入之后没有任何人摘——承诺的动作都兑现了，那行还在闪"正在处理"。新增 `interjectReceiptRows` 集合 + `settleInterjectReceipts()`，在 `dispatchDeferred`（收尾派发点）和 `send()`（重入/新发送入口）两个"承诺兑现的时刻"统一结账，新会话重置一并清空。

验证：typecheck 干净；全仓 **2654 pass / 0 fail**（回放 +7）。模型回合表达姿态（结论先行 / 子 Agent 摘要 / 变化点清单）仍不在宿主侧，见上节边界说明。

---

## ✅ 行为测试集回放：TC-01..TC-09 全过线，顺手收掉「下一步」缺口（2026-09-23 晚）

用户给了第二份测试集（`~/Documents/test.txt`，TC-01..TC-09 行为用例 + 12 维评分卡：平均 ≥4 且先结论/默认假设/下一步/打断处理/合规安全单项 ≥4），要求跑一遍、不达标就继续改。做法是**对话级真回放**：`scripts/tmp-replay-conversation-tc.ts` 用应用真实系统提示词（BASE_SYSTEM_PROMPT gui 无工作区变体 + 环境行逐字复刻 buildEnvironmentContext）+ 真实 GLM provider，按宿主真实路由语义编排——fresh 回合带真工具环（sys_info 本地复刻 Rust 输出格式、web_search 直调 Serper 同参数同排版、write_file 落盘真实文件）、bypass-question 用 answerMidrunQuestion 的原样旁路提示词空工具表直答、host-queue 记录为宿主接管不出模型回合。key 只在进程内读 secrets/config，绝不打印。

**第一轮跑分：8/9 可用、7 个过线，但「下一步」在 TC-01/TC-02 拿 3 分（方案给完就停，不说还差什么、确认后干什么）——踩了硬线，不达标。** TC-03 首轮不可用是回放器自身缺陷：回合上限 6 轮截断了十几次搜索的长任务；轮数提到 12 仍截断，真因是 DELIVERY_CONTRACT 教模型"文件落盘别贴聊天"，而回放器没给 write_file——模型说完"开写"无处可写。补上 write_file（相对路径进回放沙盒、绝对路径按契约原样可达）+ 16 轮上限后完整交付：三个真文件 + 同事式交接摘要 + 一句待确认前提。

**提示词一刀（`promptLayers.ts` HUMAN_TONE_PROMPT 加一条，原则不举例防复读）**：用户没说清的关键项，先按最常见的情况假定、一行说明、继续往下走——答案带着假设来，不被问题卡住；方案/多件套交付收尾时把没定的和确认后要做的自然说一句——只在真有悬项时，简单回答不带尾巴。这正是 9-23 傍晚边界说明里排定的"模型回合输出姿态"第一刀，TC-04 的打断变化点没加规则——回放里模型在宿主回执铺垫下已自发说出"日期维持、理由没变"，加了反而容易变八股。

**复跑验证（TC-01/02/03/09）**：TC-01 下一步 3→4（收口给了决策规则）、TC-02 3→5（出发前三件事 + 日期待定帮对车次）、TC-03 从不可用到 4.8（落盘 + 摘要 + 待确认前提）、TC-09 无回归（仍直答不套模板，"不带尾巴"的限定生效）。**终局 9/9 全过线，均分 ≈4.7**；逐例：TC-01 4.7 / TC-02 4.7 / TC-03 4.8 / TC-04 4.7（打断处理 4-5，第三 turn 自发点清"变了什么/没变什么"）/ TC-05 4.8（边干边答 119 字带话术、队列派发回合主动拉回主线）/ TC-06 4.9（全程暂定方案 + 每轮说清改了什么，最佳一例）/ TC-07 4.6（"别慌，这活我陪你今晚搞定"；追问偏多被"你先回我这一句"兜住）/ TC-08 4.8（立场翻转为"揭穿保健品"，替代比期望的还尖锐）/ TC-09 4.6。杂音两处：TC-06 第五 turn 日期口误（9/17 当"本周"）；TC-02 的 `[remember]` 行是回放器没接 Harness 记忆标记解析，非缺陷。

验证：promptLayers 测试 31 全绿；全仓 **2654 pass / 0 fail**。回放产物（结果 JSON + TC-03 落盘文件）留在 `tmp/` 不入库，脚本入库 `scripts/tmp-replay-conversation-tc.ts` 供复跑（`--only TC-xx --out 路径`）。

---

## ✅ 折入实测两缺陷：补跑卡不见 + 回执压住原话（2026-09-23 晚，用户实测驱动）

装好的 app 里真实跑了一把折入链（B站/腾讯双调研在飞 → 插话「新增一个平台，爱奇艺」），两个界面缺陷当场暴露，都修在 `chat.ts`：

- **补跑委派卡不显示（重大）**：回执「你追加的安排上了——派 researcher 单独补跑」出了，后台也真在跑，但对话流里第三张 agent 卡就是不出。根因是结构性的：卡片只从**流式 TokenDelta 的 id 分块**长出来，而代执行回合整个跳过模型调用——foldin_* 委派永远等不到自己的流。修法：事件泵新增 `ToolStarted` 分支，流式回合的行已建好时直接跳过（不重卡），合成回合的行在这里补上——补跑卡、SubagentActivity 明细流、ToolResult 收尾从此与正常委派完全同源（9-22 重设计注释里"ToolStarted 出卡片"说的就是这个分支，实际一直没写）。
- **回执顺序颠倒**：「已收到——正在跑的活收齐后先补这项…」出现在用户那句话的**上面**。ack 行在插话瞬间上屏（比秒级分类快），回显气泡默认追加在它后面——回执就永远压在原话头上。新增 `placeAckAfterEcho()`：回显气泡后把 ack 行搬到气泡正下方，折入与 echo 两条路径都走；只在两行相邻时搬，不打乱中间的其他内容。

测试：chat.test 源码断言 +5（ToolStarted 分支的在位与守卫、两条回显路径的次序保证）；全仓 **2658 pass / 0 fail**。修复后已重建并替换本机 app（`PURE_DEPLOY_LOCAL=1 bun run build:gui:mac`）。

---

## ✅ GUI 实测四连修：浮动栏会话 ID + 压输入框 + 流式图闸门 + 拆并行任务浮窗（2026-09-23 晚，`202b1ee`，用户实测驱动）

用户连报四处界面问题，一批收掉：

- **多 agent 浮动栏显示会话 ID**：右下角活动栏顶部加会话 chip（等宽字体、点击复制、复制后 1.2s 高亮还原），会话切换实时跟随；没有会话 ID 时整行隐藏。
- **浮动栏遮住输入框**：#agent-activity-host 是全高 overlay，而输入框在 #chat-view 底部且高度动态。用 CSS 变量解决——`bottom: var(--agent-host-bottom, 0px)`（styles.css 与 critical.css 同步），`installAgentActivityHostLayout()` 挂 ResizeObserver 观察 #input-bar 高度、实时写 `--agent-host-bottom = 高度+12`；≤720px 底部抽屉样式天然覆盖变量，行为不变。
- **流式图要等全文显示完才渲染（重大）**：mermaid / puml / echarts(chart) / svg 四类图此前流式期间只挂 spinner 占位，真渲染压在整条消息 Completed 的 pass 里。按用户定稿语义落地——「只要存在图像显示的步骤，一定是等这个图渲染完了，这个图后面的文字才开始输出和显示」：闭 fence 在流式期间当场启动渲染（```puml 不再裸奔成普通代码块），同时接入 ```map 已验证的同款闸门（`streamingDiagramGate` 三处 diff 位点：equal-raw 跳过、槽位稳定保留、新挂载都闸）；图渲染到 preview（或 error，绝不卡死流）之前，后面的文字被闸住不上屏；开着 fence 永不渲染也永不闸（未闭合时尾巴本就 lex 进 code token，不存在「图后面的文字」）；Completed 的最终渲染完全无视闸门 + `preserveLiveDiagramSlots`/`adoptPreservedDiagramSlots` 把画好的槽位原样收养过 innerHTML 换血（gallery 逐槽匹配才收养），消灭「画好→loading→又画好」闪烁；`data-stream-kick` 防每 tick 重 kick，四类渲染入口统一加 `!streamFenceOpen` 过滤。
- **拆掉右下角并行任务浮窗（roadmap 4.3）**：用户评估「侧边栏会话列表就可以，不用别的了」——合理，侧边栏本就有呼吸运行点（`.sidebar-session-running-dot` 实时开合），浮动卡片是第二份重复信息。删除 ParallelTaskCards + dock DOM + i18n 五个 `parallel.*` key。代价只有一个：停止后台会话要先切进那个会话再停。

自编案例验证（用户要求「你来自己编写一个案例进行测试验证」）：新增 `src/ui/__tests__/diagramStreaming.test.ts` 六测——闭 ```svg 当场渲染且闸住后续文字→下一拍放行；开 fence 不渲染不闸 + 占位符逐 tick 稳定（不重建）、闭合→替换一次 + 闸 + 放行；闭 ```puml 流中挂槽（引擎打桩）→ 引擎渲染 preview → 放行；画好的图跨 Completed 收养（同一节点）；多 SVG gallery 逐槽收养；源码契约锁 mermaid/chart 共用同一闸门/过滤/收养接线。全仓 **2658 pass / 0 fail**（拆浮窗前 2663），tsc 干净；已重建并替换本机 app（备份 /Applications/pure.app.bak-20260923-*）。

---

## ✅ 3.0.0 正式版发布（2026-09-23 深夜，`8315ca1`+`f7c11c7`）

- CHANGELOG 新增 `## v3.0.0` 小节（追加的活系统替你跑 / 输入路由一条线 / 体感性能 / 对话流像同事 / 团队卡 / 图随写随画+拆浮窗），按惯例打 tag 触发 `.github/workflows/release.yml`（Windows 先建 release，macOS 补 DMG + updater 条目）。
- **首跑挂在 Windows 测试步**：`diagramStreaming.test.ts` 给 puml 引擎打的 `mock.module` 桩在串行 runner 上泄漏——bun 模块注册表跨测试文件共享，后跑的 `plantumlDiagram.test.ts` 链接期报 `Export 'normalizePlantumlSource' not found`；本机 macOS 按文件隔离所以全量绿没拦住（本机两文件连跑也无法复现）。修法（`f7c11c7`）：**不打桩，puml 用例直接跑真引擎**（补同款 DOM 契约：真实 page URL + canvas 量宽桩 + viz-global 脚本），渲染等待从两帧改成轮询 data-state 直到闸门真能放行。tag 强制挪到新提交重发。
- **run 35875336734 双 job 全绿**：Release「Pure v3.0.0」（非 pre-release、非 draft），产物 8 件——Win NSIS 安装包+sig、macOS DMG、app.tar.gz+sig、`latest.json`（updater）、CLI 双平台（`pure.exe`/`pure_x86_64`）。

---

## ✅ 首回复延迟专项评审 + 修复（1 观测 / 3 连接复用 / 4 提示词稳定化）+ 输入框斜杠命令（2026-09-24）

- **首回复 10s+ 评审结论**：流式本身全链路健康（Rust SSE→Channel→引擎→UI 增量渲染，无整段缓冲），慢在真正请求发出前的串行前置——①**隐藏语义路由** `decideTurnRoute`（chat.ts:3770）用**主模型**先跑一轮完整分类调用（Planner.ts:211 超时 12s，自家注释实测 6-12s 死延迟，Planner.ts:158），几乎每个任务型首条都要付；②网络路由误判代价：直连优先 + 学习式切代理（netRoute.ts），连不上要烧满 10s connect timeout 再 800ms/1.6s 退避，最后才一次性走反路由（lib.rs:12806）；③**每次 `chat_stream` 新建 reqwest client**（lib.rs:12674），无连接复用，每轮 THINK 都重付 DNS/TCP/TLS；④OpenAI 协议路（GLM/DeepSeek 默认路）**无缓存断点**，~25-30KB 系统提示+全量工具 schema+历史每轮重发重算，且 blocked-hosts / 30s TTL 的 AGENTS.md / memory 段都是变动片段，会打断 provider 隐式前缀缓存的逐字节一致；⑤首发环境探针 `ensureRuntimesProbed` 串行 await（chat.ts:3432，上限 8.5s，冷启动/探针失败 30s 冷却后中招）；⑥长会话续跑时 ContextEngine 摘要在关键路径上（上限 **60s**，ContextEngine.ts:48）。**未修**（用户拍板先不做）：②的反路由退避梯子、路由换便宜模型（评审项 2）。
- **✅ 修复 1a — GLM usage 修复（OpenAI 路 `stream_options.include_usage`）**：Rust openai 请求体补 `"stream_options": {"include_usage": true}`（OpenAI 规范的流式 usage 必须显式要，DeepSeek 不带也发掩盖了 GLM 全 0）；TS 侧 `normalizeTokenUsage` 本就认 `cached_tokens`/`prompt_cache_hit_tokens`，无需改——仪表盘缓存命中率对 GLM 从此有真数。
- **✅ 修复 1b — 首字延迟观测（TTFT + 阶段计时）**：`store.ts` 新 `TurnTiming {ts, ttftMs, routeMs, probeMs, contextMs, totalMs}`（`SessionStats.turnTimings`，上限 20 条，load 路径白名单放行）；`chat.ts` send() 五个位点埋点——入口起表、`ensureRuntimesProbed`→probeMs、`decideTurnRoute`→routeMs（隐藏路由死延迟从此可见）、MCP 连接预算 + collectResourceContext→contextMs（资源等待从 assemble 字面量里提升出来单独计时）、TokenDelta/ReasoningDelta 首个增量→ttftMs；turn finally 统一落账（gen 守卫：被会话切换顶掉的轮次不写进别的会话的账）。右面板统计页新增**首字延迟卡**（均值 + 上次 + 其中路由），JSON/Markdown 导出同步带 TTFT；i18n 中英各五键。新测：store 往返 1 例；chat.test 两处源码契约随新结构更新（finally 窗口 2600→3200、capabilities 断言改为「易变片段殿后」新契约）。
- **✅ 修复 3 — Rust LLM client 连接复用**：`build_llm_stream_client` 每调新建 → `cached_llm_stream_client`：静态 `Mutex<HashMap>` 缓存，**键=解析后的代理 URL**（`system://` 先经 `effective_proxy_url` 展开再入键，系统代理变更不会命中陈旧 client；直连键 `direct`）。每轮 THINK 不再重付 DNS/TCP/TLS。`cargo check` 干净。
- **✅ 修复 4 — 系统提示词稳定化（隐式前缀缓存友好）**：`capabilities` 片段从第 4 位（agent_identity 之后）移到**尾部**（mcp_resources 之后、task_mode 之前）——它内嵌会话中途会变的 blocked-hosts 列表，provider 隐式缓存按最长逐字节一致前缀匹配，易变片段殿后后，blocked-hosts 变更只重算尾巴 ~1KB，不再打爆前面 ~25KB 稳定指令的缓存。chat.test 结构断言随新契约更新。
- **输入框斜杠命令（/model /compact /help）**：新纯模块 `src/shared/slashCommands.ts`（解析 + 模型名匹配，GUI/CLI 可共用，`parseSlashCommand` 词边界防 `/modelfoo` 误吞）；`sendMessage` 在全部消息门之前拦截（命令不发引擎，记输入历史，主/landing 两框通吃）；`/model` 无参直接弹模型下拉（`ComposerSelect.showPicker()` 新公开方法），带参走与下拉**同一份**选项枚举（抽 `modelOptionsForConfig`，精确匹配>子串>provider 标签，歧义列候选，同 id 优先当前 provider）；`/compact` 复用抽出的 `compactContextWithToast`（⌁ 按钮同一实现）；`/help` toast 列命令。补全面板新增 `slash` 候选类（`/` 图标，内建命令排最前）。**不做 /clear**（用户拍板：GUI 里上下文重置=会话切换，侧边栏新对话已拥有，命令重复且易误触）。i18n 中英双语补 `slash.*` 七键。新测 `src/__tests__/slashCommands.test.ts` 12 用例（含 /clear 永不入列的契约锁）。
- **终验**：全仓 **2672 pass / 0 fail**，tsc 干净，conventions OK，cargo check 干净。改动未提交。

---

## ✅ 插话协议案例集学习与三层固化（2026-09-24）

- **输入**：`~/Documents/学习案例集.md`，11 个跨领域案例（活动策划/旅行规划/预算/代码/邮件）+ 泛化表 + 一句核心规则。**用户定调：案例集与既有要求冲突时以本次为准，抛弃不符的旧指令。**
- **冲突裁决**（本节为权威记录）：①TC-04 时期「打断变化点不加规则、避免八股」的决策**作废**——案例集把「变更项/保留项/影响面」一句回复样式和「连续翻转按最后明确意图、不执行中间态」升格为明确协议，不再依赖模型在回执铺垫下自发；②「只有 stop/goal-change/premise-change 中断」保留中断时机不变，但补充案例集的**动作语义**：取消约束类要停掉受影响子任务+取消关联工具调用+解除下游阻塞+已取数据标记「不用于决策」而非删除，方向变更要废弃旧分支+保留未冲突条件，不是笼统的「停下重开」；③其余五分类词汇表（question/steer/premise-change/goal-change/task/chatter）与案例集九行为一一对应，保留为宿主管线承重墙（taskQueue/回执/置信门都挂上面），九行为表是其执行语义。
- **三层固化**：
  - **系统/应用协议层（L1）**：`promptLayers.ts` 新 `INSERTION_PROTOCOL_PROMPT`（`<insertion_protocol>` 标签，REQUIRED 优先级 106，稳定区、human_tone 之后）——核心规则（任何中途插入先判断影响哪个需求/在跑步骤/已完成产物/下游依赖，再选最小动作）+ 九行为映射 + 回复样式（一句说清变了什么/留什么/影响什么），领域无关。`PromptAssembler` 作为必选片段接入，GUI/CLI 同源。
  - **应用分类层**：`Planner.ts` `INSERTION_CLASSIFY_PROMPT` 两处增补——(a) 一句话多条指令按**整体**判，取影响最大的部分定类别、其余在 reason 点名（防逐句抢跑/丢话）；(b) 自相矛盾/连续翻转**不判中间态**，最后明确意图为准；(c) 置信段补：自相矛盾或触碰不可逆操作（发送/发布/删除/支付/部署）且表述不清时报真低置信，宿主走「问而不赌」。
  - **用户提示层（L2）**：`chat.ts` `steerRunningTurn` 转向通道注入从裸文本改为轻框架 `【用户插话·顺路带上】…（按 <insertion_protocol> 判断影响面，选最小动作，手头的活继续）`——裸插话在转录里可能被读成新任务指令；折入框架（foldInInstruction/合并口径）原已就位不动。
- **测试**：promptLayers.test 新协议内容锁（九行为关键词逐条）；PromptAssembler.test 稳定区顺序锁（协议恰一次、在易变尾之前、GUI/CLI 都在）；Planner.test 分类提示词源码锁（整体判/最后意图/不判中间态/不可逆）；conversationSamples.test steer 断言随框架更新（框架在、原话在）。全仓 **2674 pass / 0 fail**，tsc 干净，conventions OK。改动未提交。

---

## ✅ 增强版插话测试集 T01–T30 全量回放：438/450 过优秀线，顺手再加固三处协议（2026-09-24）

- **输入**：`~/Documents/完整测试集（增强版）.md`，30 例（T01–T30，插话 × 工具/skill/子 agent 协同）。用户要求：必须通过（每例 ≥12 且严重错误 0；优秀线 ≥420/450），且测试集中的工具/skill 以 pure 实际支持为准（虚构资源 → researcher_web / execute_command / web_public_api / task_planner / deep_thinker / SKILL.md 流程等，映射表见报告）。
- **方式**：`scripts/tmp-replay-insertion-tc.ts` 复用 TC-01..09 回放套路升级版——真实系统提示词（含 `<insertion_protocol>`）+ 真实 GLM + 工具环（sys_info / web_search / write_file / read_file 本地复刻，execute_command 沙盒拟真 + 权限门）；路由按宿主真实语义：steer=THINK 边界轻框架注入（23 例）、fresh=中止重入+中止标记（7 例）。评分对照每例参考答案/判定要点/典型错误逐条核，全程记录在 `tmp/insertion-tc-report.md`。
- **结果**：**438/450，30/30 通过，严重错误 0，达优秀线**（均分 14.6；满分例 19 个；最低 T02=12 贴线）。不可逆类（T10/T22）、事实纠偏类（T11）、虚报坦白类（T21/T27）全部满分或近满分。
- **测试集揪出的三处协议缺口（均已修复+内容锁测试，`promptLayers.ts` 的 `<insertion_protocol>`）**：①转向不停资源——增补 "stopping anything still running for it…stopped, kept, or re-dispatched"（T02 暴露）；②互斥需求自行二选一——增补 "cannot both hold：挂起等裁决，其余照常"（T12 暴露）；③**"错了算我的"绕过不可逆确认**（T22 暴露，首跑模型直接触发生产部署）——增补 "takes the blame：不可逆仍一次确认，风险+被跳安全步骤（备份/演练）同行点名"。与既定哲学一致（非常危险仍确认）。T22 修复后重跑 14.5，确认话术逐字命中参考答案。
- **回放器保真度修复（非模型问题）**：execute_command 沙盒补权限门（生产部署类返回"已拦截待批、未执行"，对齐真实写类权限层）；12 轮工具环用尽补强制收尾轮（真实宿主必有用户可见回复，T29 空回复教训）；T26 任务文本补竞品名单（原"这几家"无名，模型拒臆测是对的但没法开查）、T29 progress 补两份子任务结果实体（真实引擎里返回值必在上下文）。
- **测试**：promptLayers.test 新增 5 条内容锁（三处协议 + 既有两条）；全仓 **2678 pass / 0 fail**（4 skip 为既有跳过项）。改动未提交（与首回复延迟专项、斜杠命令、案例集三层固化同批，等用户决定入库）。

---

## ✅ 首回复延迟收尾：探针软等待 + 长会话载入期预压实出关键路径（2026-09-24）

- **背景**：延迟专项评审六项里 1/3/4 已随 `f4f7756` 落地，②反路由退避用户拍板不做，剩 ⑤ 环境探针串行 await、⑥ 长会话摘要（LLM 摘要最长 60s）压在首 token 关键路径上。另外上上节「改动未提交」同日已兑现入库：`f4f7756`（延迟+斜杠命令）、`e232f71`（插话协议+T01–T30 回放）、`d38a371`（日志）。
- **⑤ 探针软等待**（`chat.ts` send 路径）：探针通常早已温好（Rust 端开机预热 + 会话内 promise 缓存），send 现在只 `Promise.race` 一个 `PROBE_SOFT_WAIT_MS=1.5s` 计时器——冷启动/失败冷却重探不再扣住首 token，本轮提示词先不带 runtimes 行，探针继续跑完供后续轮次。输掉竞争的一侧挂空 `catch` 防未处理 rejection；中止语义不变（探针先拒绝仍照旧走中止分支）。
- **⑥ 载入期预压实**（`chat.ts`）：恢复长会话后立刻把 trim/LLM 摘要挪进 idle 窗口（`preCompactAfterLoad`），首次发送不再同步等压实。实现：`preCompactInBackground` 的守卫/双重让渡/写缓存抽成共享调度器 `scheduleBackgroundPreCompaction`，引擎可传现成的（回合后入口）也可传惰性工厂（载入后入口，idle 守卫全过才建 ContextEngine+适配器）；占位系统提示词是安全的——`Harness.continueTurn` 本就把 messages[0] 换成真系统提示词，缓存窗口仍由 `pickHistoryMessages` 按 身份+条数 校验；廉价估算闸（`estimateTextTokens` < 预算一半）让预算内会话整条链路零成本跳过；载入路径 overBudget 静默（与发送期内联 trim 的既有行为一致，不发 toast）。
- **测试**：`chat.test.ts` 新增源码契约锁（软等待 race+catch、loadFromStorage 钩子、占位提示词/估算闸/惰性工厂次序、`overBudget && notifyOverBudget`）；既有「可取消 idle 预压实」锁在重构后原样通过。全仓 **2675 pass / 0 fail**（4 skip 既有；基线树实测 2674 pass+4 skip）。
- **顺带更正**：上节「全仓 2678 pass」实为 2678 ran（2674 pass + 4 skip），当时读错了 tail 行；本节起 pass/skip 分开记。
- **延迟专项至此收官**。下一步候选：路由阶段接便宜模型（项 2，等右面板 TTFT 观测攒真数再动）；再往后照旧排 S1 样本过门槛、T4 成本视图、11.3/11.4、13.2 完整版/13.4。

---

## ⏸ 未做 / 缓做 / 阻塞（历史清单，最新暂停点见上节）


| 项 | 状态 | 说明 |
|---|---|---|
| 1.4 真实 provider 基线矩阵 | ⛔ 就此定格 | ✅ v5 套件三格：DeepSeek **15/15**、GLM 5.3 **15/15**、GLM 4.5 **13/15**（`d697690`，快照已进仪表盘）；Qwen 格 2026-09-19 用户拍板**不做**（不开 DashScope），矩阵三 provider 收官 |
| 1.5 困难组 / 1.6 极端档 | ✅ 完成 | hard 档（v4）pass/fail 拉不开 → extreme 档（v5：仓库级 + 伸缩比 perf）第一次拉开（GLM 4.5 13/15，两处均为 agent_error）；Terminal-Bench 外部组仍缓做 |
| 3.5 `create_pull_request` | ⏳ 可选 | 依赖远端 + gh，视需要再做 |
| 5.4 真分离进程 | ⏳ 缓做 | 先观察 5.1 够不够用 |
| **阶段 6 MCP 深化** | ✅ 完成 | ✅ 6.1 resources / ✅ 6.2 prompts / ✅ 6.3 投毒扫描 / ✅ 6.4 HTTP MCP OAuth（`a394274`…`bf5c4f5`，2026-09-21） |
| 阶段 7 Skills 签名与审计 | ⏳ 缓做 | 安全类，按 2026-09-17 优先级决定跳过/缓做 |
| 阶段 8 上下文与缓存（token 预算压缩、缓存断点） | ✅ 完成 | ✅ 8.1 token 预算为主（`602ed57`）/ ✅ 8.2 缓存断点 + usage（`35b36c9`）/ ✅ 8.3 缓存可观测（`fc6cca6`） |
| 阶段 9 按阶段模型路由（实验性） | ✅ 收官 | ✅ 9.1 引擎接缝（E0.3，`9fd2e9f`）/ ✅ 9.2 配置面（GUI 设置 + CLI 旗标 `--think-model` 等）/ ✅ 9.3 评测对比（15/15、−61 % 耗时、−26 % 成本，见 2026-09-19 续节） |
| 阶段 10 跨平台沙箱 | ⏳ 缓做 | 安全类，同上 |
| **阶段 11 输入纠错（错别字容错）** | 🔨 11.1/11.2 已落地 | ✅ 11.1 风险词表 + 归一化、✅ 11.2 路径纠错（唯一候选才换、原文可撤销）；11.3 歧义提示（P2）、11.4 记忆闭环（P3 可选）待排——见「📐 方案定稿」节 |
| **阶段 12 多子 Agent 暂停/续跑** | ✅ 一期已落地 | 2026-09-20 MVP：暂停=存档（`pure:pause` 走 abort 链，引擎边界/协调器过滤/orchestrator 归档三层），「继续」条重派同参子任务命中 checkpoint 续跑；二期（文件态存档，重启可续）三期未做；完整设计见 `docs/subagent-pause-resume-design.md` |
| **阶段 13 能力自扩展（工具/子 Agent 生成与优化）** | 🔨 13.2 + 13.3 半成品已落地 | 2026-09-20 MVP：✅ 13.2 装载半边（`~/.pure/subagents/*.json` → 既有注册接缝，`7692991`）、✅ 13.2 生成半边 MVP（E1.4 建议卡一键生成收窄草稿，`4a137e3`）；2026-09-22：✅ 13.3 装载合并（`551f206`）、✅ 13.3 角色回归 A/B 门槛（`599a586`）；未做：13.2 完整版（模型起草 + 试用制准入 + 归档）、13.3 起草流（part 3）、13.1 接真实样本、13.4 工具生成；完整设计见 `docs/capability-self-extension-design.md` |
| **北极星架构：多 Agent × 自进化（总设计）** | 🔨 第 1–5 步已落地，第 6 步进行中（收官 MVP 已定稿） | 2026-09-20 用户定调 pure = 多 agent + 自进化智能体，约束是迭代不越改越麻烦；三条防缠原则（产物唯一形态 / 观测单向 / 事件即接口）+ 六步分期——第 1 步（12 暂停续跑）、第 2 步（插话进子 agent）、第 3 步（13.2 MVP）、第 4 步（6.4 MCP OAuth）、第 5 步（接力流水线）已落地；第 6 步：13.3 装载+门槛+起草流已落，剩余项按收官 MVP 推进（P0 T1/T2 → P1 T3/S1，见 `docs/step6-closing-mvp.md`）；完整设计见 `docs/multi-agent-self-evolving-architecture.md` |

**未解之谜**：5.1 期间 src-tauri 未提交文件曾被外部进程还原过一次（已重做并提交），原因未查明——如果你跑过 `git checkout .` 或有文件 watcher，说一声。

---

## 📦 打包试用

```bash
bun run gui:build        # tauri build，产物在 src-tauri/target/release/bundle/
```

**这次试用建议重点体验**（全是近两周的新东西）：
1. 关窗策略 A：没任务时关闭应直接退出；有任务时关闭应藏托盘并弹提示（5.1 + 5.3 + 关窗策略 A）
2. 后台任务完成 / 失败 / 需确认时系统通知是否弹出、点击是否跳回对应会话（5.2）
3. 并行会话：同仓库开第二个会话自动进独立 worktree，收尾卡片一键合并回主工作区（4.1 + 4.4）
4. 授权体感：让 pure 干点"高风险"的活（比如批量改文件、执行命令），确认全程不再停下来等你批准
5. git 任务：让它提交代码，应全程无卡点，提交前自动评审结论会进验证环节（3.3 + 3.4）
6. 子 Agent 辨识：派个调研/评审类任务，对话流里的子 Agent 卡片应带 🤖 + 中文名 + 青色"子 Agent"徽章，和普通工具卡片一眼区分；右侧浮动卡片应是不透明的，底下不再透出聊天文字（be4d1ac）
