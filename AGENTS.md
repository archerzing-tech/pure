# 项目协作语言约定

在当前项目的每一个会话中，助手应根据用户输入所使用的语言，采用一致的语言进行思考（内部推理）与回复（对话呈现）。

- 若用户输入为中文，则思考过程与对话回复均以中文呈现。
- 若用户输入为其他语言，则对应使用该种语言。
- 保持整个会话内语言一致，不因中间穿插个别外语词汇而切换主要语言。

# 多 Agent 判定方案

多 agent 不是"对付大任务的工具"，而是**质量架构 + 规模架构**的双重手段：规模（并行加速、上下文隔离防刷屏）与质量（角色制衡，做的人不能兼裁判）。判断依据是**角色制衡的必要性**和**任务结构**，不是"任务叫什么名字"（代码/方案/文档都只是表象）。

## 触发判定（满足任一即启用多 agent）
- **T1 职能分离**：任务内含 ≥2 种职能视角，且兼任会产生盲区/互相妥协。普适维度，与任务大小无关。
- **T2 可并行**：能拆出 ≥2 个互相独立、可并行的子块。用于加速。
- **T3 上下文隔离**：过程/产物会撑爆主会话（长输出、多中间文件）。防刷屏、防幻觉。

只要命中 T1/T2/T3 任意一条，就**不该一个人干到底**。

## 职能视角库（按职能取用，不按任务类型）
- **规划者** Architect/Planner：拆结构、定标准、出方案
- **生产者** Coder/Writer/Implementer：只产出
- **检验者** Tester/Validator：独立跑通、出 pass/fail 证据
- **评估者** Reviewer/Critic：读产物，查质量/安全/风格，可打回重做（质量门禁）
- **调研者** Researcher：独立搜集事实 baseline，先给生产者供料
- **对抗者** Red-team：刻意找反方论证/漏洞

例：写文档 = 生产者 + 评估者（审校）；分析决策 = 生产者 + 对抗者；设计 = 生产者 + 评估者；翻译 = 生产者 + 检验者（校译）。不限代码/方案。

## 编排模式选择
1. **角色制衡流水线（顺序）**：生产 → 检验 → 评估，评估不过则回炉生产者（闭环，最多 N 轮）。正确性优先时用。
2. **并行 worker**：独立子块同时做，最后合成。
3. **调研前置**：先派调研 agent 出事实 baseline，再生产 agent 在其上构建。
4. **混合**：无文件修改权的评审/验证角色可并行跑（Reviewer + Validator 同时）。

## 工程原则（落地铁律）
- **单响应**：只有 Orchestrator 跟用户对话；子 agent 是干活的不是回话的。
- **Fresh context**：每个 worker 拿独立上下文，只传必要信息，不传完整历史。
- **Scale effort to complexity**：由我（orchestrator）自己估计难度、决定派几个，**不等用户开口**。
- **冲突上报**：多 agent 结论矛盾时暴露分歧，不静默选边。
- **质量门禁**：检验/评估不过不往下走。

## 不启用多 agent 的例外
仅当：任务**单线、短小、一口能说完、且不含任何独立检验需求**时，才自己串行做。

## 决策流程（接到任务时的执行顺序）
1. 这个任务内含几种职能视角？→ ≥2 种且兼任有盲区 ⇒ 命中 T1，拆角色
2. 能拆成独立并行子块吗？→ 能 ⇒ 命中 T2，并行派 worker
3. 过程/产物会刷屏吗？→ 会 ⇒ 命中 T3，流水线隔离
4. 任一命中 ⇒ 先规划角色与编排，再派发；全不命中 ⇒ 自己干

# 代码生成与界面呈现

- 在**构建 / 搭建项目**类任务中（pure 自主生成大量项目文件、目录结构时），代码应**直接写入文件**，不要在聊天界面以代码块完整呈现源码全文；界面只保留关键改动摘要与最终结果。
- 当用户**明确要求写出某段代码 / 脚本**（交付物本身就是代码）时，应在界面正常呈现该代码。
- 需求分析是必要的，但只阐述关键决策与约束；不要过多阐释非紧要、非关键的内容，避免冗余铺垫。
