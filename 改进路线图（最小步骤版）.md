# 改进路线图：最小步骤执行清单

> 配套《差距分析：pure 与主流 Agent（截至 2026-08）》。原则：**每一步独立可发布、独立可回滚，做完一步用一步**。量级：S ≤ 半天，M = 1–2 天，L = 3 天以上。
>
> 通用纪律（每步都适用，对应深度版文章第六章的三样东西）：一个行为测试 + 一条写给未来读者的注释 + 一条带根因的 changelog。做不到这三样的步骤，宁可拆小。
>
> **优先级调整（2026-09-17 用户决定）**：后续改进不特别关注安全与权限加固，重点放在智能化、自动化、效率。涉及安全/权限的步骤（如 7 的 skills 签名、10 的跨平台沙箱）默认缓做或跳过；既有权限机制保持现状。
>
> **追加（2026-09-20）**：新增**阶段 11 输入纠错（错别字容错）**——用户体感驱动的体验类阶段，不改变 1–10 的既有顺序，排在当前待办第一位（P0 半天、零误伤）。

## 排序逻辑

先做**低成本 × 高确定性 × 能立即 dogfood** 的（评测、hooks、git 写工具），再做**架构性但地基已好**的（并行、异步），最后做**生态与实验性**的（MCP 深化、模型路由）。每阶段结束时项目都处于"可发布、比上周更好用"的状态。

---

## 阶段 1：评测基线（第 1 周）——投入产出比最高

| # | 步骤 | 验收标准 | 量级 |
|---|---|---|---|
| 1.1 | 在 `evals/` 新增 5 个 fixture：多步长任务 ×2、失败恢复 ×1（故意埋一个会失败的命令，验证 FailurePolicy 兜住）、权限拦截 ×1（验证高危操作被门住）、长会话压缩 ×1（50+ 轮后追问第一轮的附件路径） | `bun run eval:baseline --agent mock` 全绿；每个 fixture 有 fixtureHash | S |
| 1.2 | evals 挂进 CI：`eval:baseline --strict` 作为第四道门禁 | CI 红了能挡 merge | S |
| 1.3 | CHANGELOG 发布模板加一节「评测基线」 | 从下个版本起每个 tag 带数字 | S |
| 1.4 | 真实 provider 基线矩阵：DeepSeek / GLM / Qwen 各跑一轮全量 fixture，数字记进 `evals/BASELINE.md` | 三个 provider 各有一行可对比的分数 | S |
| 1.5 | （月度滚动）从 Terminal-Bench 开源任务里挑 5 个终端任务手动接入，作为「困难组」单独记录 | 5 个容器化任务可复现跑通 | L，可拆 |

## 阶段 2：用户 hooks（第 2 周）——最便宜的扩展点

| # | 步骤 | 验收标准 | 量级 |
|---|---|---|---|
| 2.1 | 定义 `~/.pure/hooks.json` schema（`on_pre_tool` / `on_post_tool` / `on_turn_complete` → shell 命令），加载器复用 AGENTS.md 的三层合并链路 | schema 有类型定义 + 加载有测试 | S |
| 2.2 | HookRouter 接用户 hooks：shell 执行器 + 超时（默认 10s）+ stdout 注入为事件上下文 | 一个示例 hook（如 post_tool 后自动 `bun test`）端到端跑通 | M |
| 2.3 | hooks 自身过权限门：首次启用的 hook 弹确认卡，"始终允许"按 hook 粒度缓存 | 未批准的 hook 不执行且有日志 | S |
| 2.4 | 文档 + 3 个官方示例 hook（自动测试、提交前 lint、通知） | README 一节 + 示例可复制 | S |

## 阶段 3：git 写工具 + 提交前评审（第 3 周）

| # | 步骤 | 验收标准 | 量级 |
|---|---|---|---|
| 3.1 | `toolDefs.ts` 加 `git_commit` / `git_branch` schema + `TOOL_TAGS`（medium）+ `TOOL_METADATA_TABLE`（isWrite）——编译器强制三件套齐 | typecheck 过；未配 tag 会编译失败（这步本身就是验证） | S |
| 3.2 | 双端实现：NodeToolAdapter / TauriToolAdapter 各加 case（先走 shell git，不新开 Rust command） | 确认卡能看到 commit 内容预览（复用 `buildWritePreview`） | S |
| 3.3 | （2026-09-17 按用户决定简化：不加新守卫）`git_commit`/`git_branch` 设 riskLevel low，NORMAL 全自动放行不弹卡；PLAN/DONT_ASK 仍经 WRITE 标记拦截；`git push`/`git reset --hard` 维持 execute_command 现状，不加 commandGuard | 测试：NORMAL 不询问直接放行、PLAN 仍拦、medium 写入仍弹卡（对照） | S |
| 3.4 | 提交前自动评审：L1 工作流规则——涉及 `git_commit` 时先跑 code_reviewer 子代理，评审结论作为证据进 VERIFY | 提交被评审拦下的场景有 eval fixture（对应 1.1） | M |
| 3.5 | （可选）`create_pull_request`：依赖远端与 gh，放最后，视需要再做 | — | M，可延 |

## 阶段 4：worktree 并行会话（第 4–5 周）——GUI 主场

| # | 步骤 | 验收标准 | 量级 |
|---|---|---|---|
| 4.1 | 会话 ↔ worktree 绑定抽象：同一 repo 开第二个会话时自动 `git worktree add` 到独立目录，会话元数据记录映射 | 两个会话各自改同一文件互不干扰 | M |
| 4.2 | taskQueue 单飞语义按 worktree 粒度化：不同 worktree 可并行，同 worktree 内保持现有单飞 + 插话契约 | 现有队列测试全绿 + 新增跨 worktree 并行测试 | M |
| 4.3 | GUI 多任务卡片：并行会话各一张卡，状态/耗时/spinner 独立（复用子代理进度卡的成熟经验） | 3 个并行会话 UI 不串台 | M |
| 4.4 | 收尾合并流：会话完成后提示 diff 预览 + 一键合并回主工作区 / 删除 worktree | 未合并就关闭时有留存提示 | S |

## 阶段 5：本地异步执行（第 6 周）

| # | 步骤 | 验收标准 | 量级 |
|---|---|---|---|
| 5.1 | 最小异步：关窗 → 隐藏到托盘继续跑（不是分离进程，先利用现有窗口生命周期） | 关窗后任务继续、进度不丢 | S |
| 5.2 | 系统通知：turn 完成 / 需要确认卡 / 失败三种事件触发系统通知 | 点击通知跳回对应会话 | S |
| 5.3 | 托盘菜单：运行中任务列表 + 快速取消 | 托盘可见所有在跑会话 | M |
| 5.4 | （后续再评估）真分离进程模式：主进程退出后任务仍跑，靠 checkpoint 恢复——先不做，观察 5.1 是否够用 | — | L，缓 |

## 阶段 6：MCP 深化（第 7–8 周）

| # | 步骤 | 验收标准 | 量级 |
|---|---|---|---|
| 6.1 | MCP resources：`list/read_resources` → 作为上下文 fragment 注入（走 PromptAssembler，priority 参照 skills=30） | 连一个有 resources 的 server，内容进上下文 | M |
| 6.2 | MCP prompts：server 提供的 prompt 模板在输入框可调用 | — | S |
| 6.3 | 安全扫描第一版：连接时对工具名/描述做投毒特征检查（规则库起步），结果在连接卡展示 | 对公开的投毒样例能报警 | M |
| 6.4 | HTTP MCP OAuth（device code 流起步） | 接一个需要 OAuth 的官方 server | M |

## 阶段 7：Skills 签名与审计（穿插做，第 8 周）

| # | 步骤 | 验收标准 | 量级 |
|---|---|---|---|
| 7.1 | 安装时把 source/hash/声明的权限写进 lock 文件（`skills-lock.json` 已有 hash 先例，扩展字段） | 重装/更新时 hash 变更有提示 | S |
| 7.2 | 安装确认卡展示来源、hash、权限需求 | 未签名技能有醒目提示 | S |
| 7.3 | 可信来源 allowlist → 「已验证」标记 | — | S |

## 阶段 8：上下文与缓存（第 9 周）

| # | 步骤 | 验收标准 | 量级 |
|---|---|---|---|
| 8.1 | ContextEngine 压缩权重调整：从「20 条窗口为主」改为「token 预算为主」，tool-call 组原子与 pinned user 语义不变 | 现有压缩测试全绿 + 长会话 fixture（1.1 那个）分数不降 | M |
| 8.2 | PromptAssembler 输出缓存断点（anthropic 协议 cache_control）；DeepSeek 侧验证上下文缓存命中（计费对比） | 同会话第二轮起输入费用下降可测量 | M |
| 8.3 | 缓存命中写进 observability 元数据（hash 旁边加 cache 标记） | — | S |

## 阶段 9：按阶段模型路由（第 10 周，实验性）

| # | 步骤 | 验收标准 | 量级 |
|---|---|---|---|
| 9.1 | EngineContext 支持按状态选 adapter：THINK/VERIFY 可配不同 provider+model，默认全同 | mock 测试：两阶段用不同 mock adapter | M |
| 9.2 | 配置面（cliConfig + GUI 设置）+ 文档标注实验性 | — | S |
| 9.3 | 用阶段 1 的评测矩阵对比：强模型 THINK + 便宜模型 ACT 是否保持分数 | 有数据再决定是否转正 | S |

## 阶段 10：跨平台沙箱（第 11–12 周，收尾）

| # | 步骤 | 验收标准 | 量级 |
|---|---|---|---|
| 10.1 | 非 macOS 平台显式降级提示：YOLO 模式开启时警告「此平台无内核级沙箱」 | Windows/Linux 开 YOLO 必见提示 | S |
| 10.2 | Linux bubblewrap 最小包装（写限 workspace+tmp、网络仅出站，对齐 Seatbelt 语义） | 同一套沙箱测试跨平台跑绿 | L |
| 10.3 | Windows 受限令牌 + Job Object | — | L |

## 阶段 11：输入纠错（错别字容错，2026-09-20 追加）——体验类，用户驱动

> 起因：GUI 里用拼音输入法敲提示词，错别字会改变整句话的意思。**要修的不是"错字"，是"静默猜错"**——错字有三处放大器：① **高危判定被绕过**：`assessIntent` 的高危分类是字面正则（`src/coding-agent/Planner.ts:704` 的 `删除|移除|清理|销毁|rm -rf…`），同音错字（"闪除"）直接漏判 → `preflight` 只认 `riskLevel==='high'`（`src/ui/preflight.ts:25`）→ 破坏性发送**不弹确认**；② **目标定位错人**：文件名/符号名错一个字符，模型不报错，会挑一个最像的文件动手改 → 静默改错文件，比不改更糟；③ **模型侧只有软容错**：`TYPO_TOLERANCE_PROMPT`（`src/shared/promptLayers.ts:126`）让它"自己猜 + 说明假设"，猜错无人拦、用户也不读那句假设。
>
> **五条铁律**（写进注释，评审按此打回）：① 语义文本永不静默改写——否定词、数量数字、单位、路径不在自动改写白名单；② 自动改写只有两种资格：无损归一化，或有唯一确定性证据（workspace 内真实存在 + 候选唯一过阈值）；③ 风险判定取 `max(原文, 纠错后)`——宁可多弹一次确认，不可放过一个破坏性操作；④ 可见 + 可撤销：transcript 显示原文 + "已按 X 理解" + 一键还原；⑤ 发送路径纯本地、零模型调用（`src/ui/preflight.ts:5-10` 记着教训——LLM 预分析曾因"从未稳定成功"被移出 send 路径），不引新依赖、不建大词表。
>
> **决策记录（2026-09-20 用户拍板）**：L1 标识符层取"**自动替换 + 可撤销**"，不取"只提示"——提示会被直接回车无视，等于没做；代价是阈值必须调校，任一条与门不满足即降级为只提示，绝不猜。

| # | 步骤 | 验收标准 | 量级 |
|---|---|---|---|
| 11.1 | **P0 ✅ 已落地（2026-09-20）**：`src/ui/inputRepair.ts`（纯函数、无 DOM，照 `preflight.ts` 写法）——无损归一化（不可见字符、Unicode 空格、全角 ASCII→半角、保留中文标点）+ 风险同音词表展开（删除/清空/重置/覆盖/销毁/不可逆/迁移/重构/`rm -rf`/force push/destroy 等的错字变体 → 规范词）；`checkPreflight` 改为对"原文 + 展开文本"取更高风险 | 单测："闪除这个目录" → `riskLevel==='high'` 且 `checkPreflight` 返回 gate；归一化幂等；`src/__tests__/preflight.test.ts` 全绿 | S |
| 11.2 | **P1 ✅ 已落地（2026-09-20）**：标识符纠错——路径/文件名 token 在 workspace 内不存在 + 编辑距离唯一命中过阈值 → 替换为真实路径；索引走现成 IPC `glob_files` + `buildContextCandidates()` 缓存；transcript 显示原文 + "已按 X 理解" chip，点击用原文重发 | 唯一候选才替换、双候选不动、不存在路径不动、否定句不动；`chat.send` 需带 `repairs` 进消息结构（`chat.ts` + `store.ts`），本方案唯一跨层改动 | M |
| 11.3 | **P2**：低置信歧义只提示不改——composer 下方 inline hint（复用 `InlineAutocomplete` 的 popup 定位与 Tab/Esc 交互），候选作为附注随消息带给模型，让澄清发生在模型侧而不是篡改用户的话 | Tab 采纳 / Esc 忽略 / 直接回车按原文发送，三条路径各一个测试 | S |
| 11.4 | **P3（可选）**：纠错记忆闭环——agent 发现"用户说的 X 不存在、我猜的是 Y"时写 `confidence:'low'` 词条，走**现有** E3.1 草稿确认机制（`src/adapter/memory/correctionDrafts.ts` / `src/harness/LessonReflector.ts`），确认后成为本工作区词条 | 不新建存储；同一句跨会话 dedupeKey 一致 | M |

**为什么先做 11.1**：半天、零误伤——只改风险判定，不碰用户一个字，先把最贵的坑（错字绕过破坏性确认）堵上。**归一化里刻意不做"折叠连续空格"**：会让用户粘贴的代码缩进失效，"匹配稳定"不值得这个代价。

---

## 执行规则

1. **一次只开一个阶段的一个步骤**，做完即发布（patch 版本），不攒。
2. 每步的三样东西（测试/注释/changelog）随步走，不留"回头补"。
3. 任何一个阶段做到一半发现有更大的坑，**记录进差距分析文档然后跳过**，不现场扩坑——扩坑是新条目，重新排优先级。
4. 阶段 1.5、3.5、5.4 是明确的"缓做"项，除非用户反馈逼到脸上。

**第一步就是 1.1：加 5 个 eval fixture。** 半天量级，做完整个路线图就有地基了。
